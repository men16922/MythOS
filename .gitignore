.DS_Store
.venv/
node_modules/
src/mythos_ui/node_modules/
src/mythos_ui/.vite/
src/mythos_ui/.vite-temp/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

.env
outputs/*
!outputs/.gitkeep

# Audio Assets
*.wav
!resources/neo-seoul/audio/**/*.wav
*.mp3

# Local Docker volume data (docker-compose.local.yml)
.docker/

# AI Agent Junk — 슬래시 없이(디렉터리 + worktree symlink 모두 매칭; 트레일링 슬래시는 symlink 미매칭 → git add -A 가 추적해버림)
.claude
.agents
.gemini
.playwright-mcp
.antigravitycli/
screenshots/

# Tooling Caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# VS Code remote tunnel CLI artifacts
/code
/vscode_cli.tar.gz

# Overnight loop runtime artifacts (bin/overnight/run.sh) — keep the harness, drop the state
bin/overnight/logs/
bin/overnight/STOP
bin/overnight/DONE
