# 2026-05-30 Post-MVP Plan

상태: `[x]` Completed planning snapshot

## Context

로컬 MVP(M0-M10)는 완료되었다. Streamlit playable demo, CLI vertical slice, Visual Service, Ollama narrative mode, Echo carry-over, PostgreSQL/MinIO persistence가 검증된 상태다.

이 계획은 MVP 이후 첫 작업 묶음의 날짜별 스냅샷이다. 최신 rolling plan은 `docs/NEXT_PLAN.md`를 따른다.

## Goal

현재 “작동하는 Streamlit 데모”를 “반복 사용하기 편한 데모”로 개선하고, 이후 Narrative Runtime Depth 작업으로 넘어갈 수 있는 기반을 만든다.

## Phase 11 Scope

작업:

- `[x]` Store layer에 player 목록 조회 API 추가.
- `[x]` Store layer에 player별 loop 목록 조회 API 확인/보강.
- `[x]` Streamlit player selector 추가.
- `[x]` player id 직접 입력은 manual/advanced path로 유지.
- `[x]` Streamlit loop selector 추가.
- `[x]` active/archived loop 상태 표시.
- `[x]` resume UX 개선.
- `[x]` 기존 player 생성, new loop, choice, free action, archive flow 회귀 확인.

검증:

- `[x]` `make test`
- `[x]` `make test-db`
- `[x]` `make smoke-local`
- `[x]` Browser Streamlit player select flow
- `[x]` Browser Streamlit loop resume flow
- `[x]` Browser archive/Echo carry-over regression

## Expected Outputs

- Streamlit에서 기존 player를 목록으로 선택 가능.
- Streamlit에서 기존 loop를 목록으로 선택/resume 가능.
- active/archived 상태가 UI에서 명확히 보임.
- `STATUS.md`, `PROGRESS_LOG.md`, `COMPLETED_SUMMARY.md` 업데이트.

## Result

- Store layer에 `list_players()` 추가.
- Streamlit saved player selector 추가.
- Streamlit saved loop selector와 resume flow 추가.
- Manual player/loop 입력은 expander로 유지.
- Visual assets와 Echo 영역을 분리.
- Runtime action spinner 추가.
- Browser로 player select, loop resume, archive, next loop Echo carry-over 회귀 확인.

## Out Of Scope

- Next.js UI 전환.
- Redis async visual job 구현.
- Variation Engine 구현.
- Cloud provider migration.

## Follow-Up Candidate

Phase 12에서는 Variation Engine / Novelty Controller와 `world_memories`, `narrative_shards` runtime 반영을 시작한다.
