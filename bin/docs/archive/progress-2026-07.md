# Progress archive — 2026-07

## 2026-07-04 — StS-style route expansion: 10 layers + campfire-before-boss + junction hints
- Status: Completed locally (uncommitted, `make check`-equivalent unittest **738** green ×3 re-runs). Driven by live feedback: full 6-layer run ≈ 10min → expand; node-map reference = **Slay the Spire** ([[node-map-reference-sts]] memory).
- **10 layers**: 3 bridge layers inserted (소각로의 여파 / 꿈의 잔향 / 접근전 — light story anchors `ash_street`/`wet_dawn_street`/`wall_recon`, width 2 pools) + **폭풍 전야** mandatory-rest layer before the boss (StS campfire; full heal + stability). ~50 story turns ≈ 17min/run. kai side-arc `min_layer` 4→5 (anchor shifted).
- **Junction risk/reward hints (StS #3)**: ChoicePanel destination rows now show per-type tradeoff copy (market=barter·40% heal, rest=full heal, patrol=combat risk…) in KO/EN.
- **Market economy**: `market` added to arc_3/arc_4 pools; 100-seed — every loop now rolls 1-5 dynamic markets (median 3) + the night_market anchor; scrap sink (restart_core 5-scrap) reachable mid-game.
- **Title pools expanded** (story5/clue8/combat·patrol·market·rest·event 7 each, KO+EN glossary): 100-seed duplicate titles **0**/100 at 26 nodes/map.
- **Unused-asset pass**: real idle assets = 7 of 196 (skills/* were dynamic-path false positives; candidates/variants are pipeline rejects). Wired 3 into the new bridge anchors (`concept/04-reconstruction`, `scenes/subway_control_hub`, `scenes/opening_escape_alt`) — also skips Imagen on those turns. Left deliberately idle: superseded `scenes/kai_meet|lin_yue_meet` (portrait-inconsistent), `data_incinerator` (dup of concept/05), `se-rin-biker`.
- Fixed en-route: anchor-less stub layers degenerate under the dynamic seed builder (story filler breaks the anchors-only stub invariant) → bridge layers carry light anchors; route-content validator requires anchor `beat` ids.
- Next: `[manual]` live run of the 10-layer loop (pace ~17min, gauge pressure over the longer tail, market/hint feel), then commit+redeploy.

## 2026-07-04 — Party survivability batch + lin-yue sprite keying (live feedback round)
- Status: Completed locally (uncommitted). `make check` **738** green (+7 tests). Driven by human live-play feedback (동료 Down 영구 이탈, 부활/휴식 부재, 힐 자기대상 고정, "잔해 수습" 선택지 무의미, lin-yue 배경 흰색 잔존).
- **Downed ally rejoin**: hp<=0 members are no longer excluded from later fights — they rebuild at max(1, max_hp//4) (`combat_service._build_allies`).
- **Rest recovery**: every narrative commit heals player + living members `REST_RECOVERY_HP`(2) toward max (`_apply_rest_recovery` in `_commit_scene`); downed members stay down.
- **Ally-targeted heals**: engine already exposed `friendly_targets` but the SPA always sent the enemy target — added SUPPORT TARGET row in `CombatControls` (default = most wounded friendly) + `friendly_targets` typing; heal/shield skills now direct at the chosen friendly.
- **Revive consumable**: NEW `restart_core` (재기동 코어, rare; market 5 scrap) — engine `revive` effect reboots the first downed ally at max(bonus 5, max_hp//3); not consumed with nobody down; KO/EN combat-log lines + glossary EN name.
- **Choice→item channel activated**: `world_delta.grant_items` was wired end-to-end but never prompted, so pickup choices were flavor-only. Added JSON-contract examples (KO/EN), a per-turn grantable-id GM note (`_grant_items_note`, consumable/material only), commit-side whitelist+cap (`_filter_grant_items`) and id→item-def materialization (`_materialize_inventory_items`).
- **lin-yue sprites**: enclosed white background pockets (hair-ornament/arm gaps; edge flood-fill couldn't reach) keyed out via hysteresis neutral-white component removal + halo feather across all 5 sprites; VFX/silver ornaments preserved (magenta-composite verified). Kai's whites are his canonical white-silver body — untouched.
- Verified: `make check` 738 green; focused new tests (rejoin/rest/revive×2/grant×3); GAMEPLAY.md party-survivability rules + live-QA checklist items added.
- Next: `[manual]` live feel QA of the new party loop + grant frequency (남발 여부), then commit/redeploy with the next bundle.

## 2026-07-04 — Claude handoff QA: defeat-ending-screen fix + Cloud Run redeploy `00008-7sx`
- Status: Committed `89e1935` (74 files, the whole 07-04 local bundle + fixes) and 🚀 **REDEPLOYED to Cloud Run rev `mythos-api-00008-7sx`** (100% traffic, env carried over). `git push` still pending (human-only, main ahead 11).
- AGY browser QA (run `0704-render-130048`, Chrome DevTools, console/network clean): skill dependency graph + Rank copy, Clues placeholder cleanup, achievements dashboard = PASS. Its "Kai missing from Bonds" FAIL was a **false alarm** — tutorial seed is Se-rin-only by design since `183bd5f` (Kai joins narratively via Restarting Kai).
- **Real regression found + fixed**: a run-ending combat defeat (boss climax/permadeath) never showed the ENDED screen — the server resolved erasure + `ending_image`, but `/combat/action` carried no ending fields, the SPA merged only `combat` (stale phase kept EndedPanel from rendering), and the defeat button exited straight to main. Fix: response `ending` block (`combat_server.py`), snapshot `loop_phase`+`ending` merge (`useCombatRest.ts`), result panel yields its exit to EndedPanel (`StoryPanel.tsx`), stale boon offer no longer blocks the ENDED screen (`BoonOffer.tsx`). 2 regression tests.
- Ending-art verification: all four endings resolve to their dedicated 1254² images (seeded archive check), and a live browser boss defeat renders `forced-erasure.png` unbroken with EN narration (`outputs/qa_boss_defeat_ending_art.png`).
- E2E checklist repaired for the CBT UI (save input → Save modal, run history → Codex tab, `/api/v1/memory` mock) and upgraded to the new defeat contract (asserts unbroken ending art; EndedPanel owns the exit). Live-QA checklist slimmed to human-only items.
- Verified: `make check` **731** green (2 new tests); checklist E2E green; post-deploy `00008-7sx` — health/SPA 200, invite gate no-key 401 / admin key 200, ending-art + cutscene statics 200.
- Next: human live QA **on the deployed cloud** per `docs/test/neo_seoul_live_qa.md` 🔴 (Night Market→Restarting Kai, boss buildup→**victory** ending, companion cuts, KO/EN feel); then human `git push origin main`.

## 2026-07-04 — Companion/cutscene UI completion + ending-art follow-up
- Status: Completed locally; not committed, pushed, or deployed. `make api-cloud` remains live at `http://127.0.0.1:8000` for QA.
- Changed: authored KO/EN affection-1 cutscenes for Kai, Lin-yue, Tae-o, Han, and Su-ah; all six companions now expose seven gallery entries. Promoted two portrait-consistent Se-rin artworks as dedicated cutscene assets.
- Changed: route choices and operation-map destinations now share numbered/color-coded links; the skill tree is a dependency graph with depth columns, prerequisites, status totals, icons, and KO/EN copy.
- Changed: memory serialization now loads localized companion directives, fixing Korean gallery titles in a fresh EN loop.
- Changed: generated and adopted dedicated 1254² art for all four Neo-Seoul endings; ending state now carries `ending_image`, including boss-defeat `endings/forced-erasure.png`, and EndedPanel renders it.
- Changed: skill ranks now modify combat potency/focus/cooldown; empty placeholder clues are rejected and legacy duplicates hidden with a purpose hint; Kai's awaken/tool route adds him to `_party`, and Companion Bonds labels current party members even at affection 0.
- Verified: `make check` green (**729 tests**, skipped 2), including content/image integrity, mypy 148 files, ESLint, and production frontend build; focused rank/clue/Kai/ending-asset regressions green.
- Verified: Chrome DevTools EN play reached live combat with English narration/choices/UI; skill graph and all seven English gallery titles rendered, all 22 observed API requests were 200, and the console was clean. Screenshots: `outputs/chrome_skill_tree_en.png`, `outputs/chrome_companion_gallery_en.png`.
- Blockers: none for the local implementation. Cloud Run remains rev `mythos-api-00007-6h8` and does not include this bundle.
- Next: Claude performs browser visual QA for four ending arts, Rank details, Clues cleanup, and Kai `IN PARTY`, then commits/pushes/redeploys.

## 2026-07-04 — Route-scene lifecycle contract + duplicate-choice hardening
- Status: Route lifecycle committed locally as `40bcc6b`; achievements dashboard completed locally. Neither bundle is pushed/deployed.
- Changed: added load-time/`make validate-content` causal validation for unique beats, defaults, gate producer order, and perspective self-cycles; deleting a producer scene now fails fast. Night Market deterministically produces `lin_yue_deal`; four circular perspective selectors were rewired to prior flags.
- Changed: hard gates can no longer be bypassed by route/junction fallback; full/dynamic builders preserve ungated forward options and stale maps repair to an eligible node. `Perspective N` UI copy is now `Story paths N` / `서사 분기 N`.
- Changed: choice requests carry `scene_id`; duplicate REST/WS frames return the current snapshot idempotently, and the React sender has an immediate in-flight lock. Cross-loop relationships now hydrate into live state and archive only the run delta.
- Changed: Memory Constellation now shows cumulative runs/wins/clues, 4 companion recruitment milestones, 6 data-mirrored companion upgrades, and unlocked trait chips with KO/EN copy and responsive progress bars. The E2E gate verifies the dashboard structure and language switch.
- Verified: before→after seeded measurement (both builders, 100 seeds each): `lin_yue_deal` 0→100, Kai gate 0→100, locked-node visits →0; duplicate choice 200→404 became 200→200 with the same turn/scene.
- Verified: `make check` (**724 tests**, skipped 2), `make smoke-local`, and `make test-e2e` green. Direct Playwright QA rendered the achievement dashboard in KO/EN and captured `outputs/e2e_achievements.png`; E2E resolves blocking boon/echo offers before tab/choice interaction.
- Next: commit the dashboard checkpoint, then human push/redeploy; fresh-loop live QA for Night Market→Restarting Kai, rapid/repeated choice input, and cumulative achievement progress. Design: `docs/plans/2026-07-04-route-scene-lifecycle-hardening.md`.

## 2026-07-04 — Live-QA round 3: pre-boss stability archive root cause + companion growth system
- Status: Completed (`make check` EXIT=0, **713 tests** at close; local cloud API live; **🚀 REDEPLOYED to Cloud Run rev `mythos-api-00007-6h8` 100% traffic** — health/SPA 200, invite gate verified, prod `/scenarios` shows glass-library locked; env carried over, no new migration. `git push` still pending, human-only).
- **(1) Boss preempted AGAIN — new root cause (`/diagnose`, `loop_26adffc3`)**: the run died at rn10 (one node short of the boss rn11) at stability=3/tension=100 with a full boon build — "Journey's End" with no fight. Evidence: events show the LLM grinds **−5 stability on nearly every scene**, so pre-boss `stability<=10` is the COMMON case, not the rare "player is being deleted" erasure the 07-03 design assumed; `_defer_tension_archive_before_climax` deliberately bailed on `stability<=10` (session.py:2467). Fix: renamed to `_defer_threshold_archive_before_climax` and it now defers **both** bare thresholds while the boss node is ahead (explicit LLM `end_condition` still ends; boss fight owns the end: victory→perspective ending, defeat→erasure). Regression test flipped to the live numbers (`test_pre_boss_stability_collapse_deferred_until_boss_reached`). DECISIONS 2026-07-04.
- **(2) Companion growth system (user-approved 3 hooks, all data-driven, no-op when absent)**: NEW `companion_growth.py` folds three channels into `_build_allies` → `build_ally_combatant(bonus_stats/bonus_hp/extra_skills)` (stats fold BEFORE derivation so defense/speed/focus scale): ① **bond tiers** — cross-loop `relationships` affection, 2 pts/tier (max 3), per tier +2 HP/+1 STR/+1 PER; ② **achievement kit upgrades** — `combat.allies[].upgrades` gated on the same meta-progression milestone axis as recruitment (authored for all 6 neo-seoul allies, e.g. won≥4 → Se-rin learns glitch_blink); ③ **party boons** — 3 new `target="party"` BOON_POOL entries (squad_sync/guardian_protocol/link_overdrive, tag `bond`) buff allies only, excluded from the player channel (`party_boon_bonus`). 10 tests incl. upgrade-content integrity (skills exist + numeric requires, all scenarios) + baseline-unchanged guard.
- **(3) Live-QA checklist refreshed** (`docs/test/neo_seoul_live_qa.md`): 🔴 section rebuilt for the 07-04 marathon items (boss buildup→fight→ending, loot persistence, Lin-yue recruit+join, market barter, boons/echo, save-slot restore, BGM, tutorial party, map-horizon repro, balance feel), 07-03 leftovers split out, stale boss-item text removed.
- **(4) Companion sheet UI + glass-library CBT hold (same day, follow-up round)**: snapshot NEW `companions` field (`serializers._companion_roster` → `companion_sheet`) — met companions with base-vs-growth-split stats/HP/skills/bond-tier/upgrades; CHARACTER-tab focus card rebuilt to the player-card layout (portrait-frame overflow bug fixed — raw `<img>` had no constraining class) with `StatBars` (extracted shared component, amber bonus overlay), skills/growth chips, i18n ctab keys ×4. **glass-library disabled for CBT**: `unlock.disabled` hard-block honored by `scenario_unlock_met` BEFORE the live-play bypass → `/scenarios` reports locked (selectbox-disabled, live-verified); gating test flipped to assert the hold. Boss-defeat meta accrual confirmed by code read: `update_meta_progression` runs on every archive (defeat too) — runs_completed+1, per-run wins/clues accrue.
- **(5) Mechanics-annotation scrub + perception made real (same day)**: (a) live leak "(Agility check)" in a choice label — the GM prompt forbids tabletop phrasing but the LLM ignored it; `parser._clean_player_text` now strips EN/KO parenthetical mechanics tags (`check/roll/save/DC n/판정/체크/굴림`) deterministically at the single commit choke point (ordinary parentheses survive; regression test). (b) **perception was combat-inert** (to-hit = STR/AGI, crit = nat-20 only) while boon/echo/UI descriptions promised "accuracy/crit" — `CombatEngine._perception_mods` now grants baseline-neutral to-hit `+(per-5)//3` and crit-range widening `(per-5)//5` in `_attack`+`_skill_attack` (default per-5 units unchanged → old balance intact; encounter-balance invariant re-verified green).
- Verified: `/diagnose` before/after (old test asserted archive → new test asserts live at stability 3); `make check` EXIT=0 **713 tests** green; API restarted (`api-cloud`), `/scenarios` live shows glass-library locked. Remaining `[manual]`: companion card visual QA + bond/growth balance feel. Known gap: no cumulative-achievements UI (meta totals/milestone progress invisible — candidate: ProgressDashboard section).

## 2026-07-04 — Live-QA round 2: 3 root-cause fixes (route clock · cache clobber · boss buildup)
- Status: Completed (`make check` **697 tests** green at close; API restarted, Redis snapshot cache flushed). All found via human live play on the Gemini cloud stack.
- **(1) Combat rounds inflated the route clock** (`loop_dbbd07cb…`): each combat round consumes a scene `turn_index`, and the route paced on the raw index — a few long fights fast-forwarded `target_layer`, so ~6 narrative scenes hit the IX boss with 0 clues ("빌드업 없이 바로 보스"). Fix: NEW `_story_turn` counter (narrative commits only) drives `advance_route`/`extend_route`/`junction_options` + the preview; legacy loops fall back once to the scene index. Regression test locks it.
- **(2) Snapshot cache clobbered combat results** (loot "표기도 안 됨"): 4 victories granted `drone_scrap`×2 each (events prove it) but final state had NO `_inventory` — `_prepare_choice` trusted the Redis-cached snapshot's loop while combat commits never refreshed the cache, so the next narrative choose() committed the stale pre-fight state (also a zombie-combat vector; tests run cache-disabled, hence 688 green while live broke). Fix: write path always loads the store-authoritative loop/scene; `_commit_combat_turn` now refreshes the cache. Equipment/consumable systems were fine — their items were being wiped.
- **(3) Boss buildup** (user directive): entering the final node no longer starts combat instantly — the climax parks (`_pending_boss_combat`), the arrival commit stays a narrative confrontation beat with choices, and the fight fires on the player's next choice (still wins precedence, supersedes zombies, defers threshold archive). 3 boss tests updated to the two-step flow.
- **(4) Market exchange (scrap sink)**: data-driven `combat.market_exchange` (2 scrap→nanopatch / 3→stim_shard / 6→mesh_vest) trades at market route nodes — `exchange_material` service + `/market/exchange` API + `market` snapshot field + docked `MarketExchange.tsx` card. **Lin-yue is the canon vendor** (`market_vendor`; night_market already carried `event: lin_yue_deal`) — UI header + GM scene directive place her network at every market. Guards: market-node-only, no mid-combat, insufficient-material reject; glass-library (no route_map/config) = clean no-op. 4 tests.
- **(5) Loop length**: `DEFAULT_TURNS_PER_LAYER` 4→5 (6 layers ≈ 30 narrative turns; "한 루프가 짧다" feedback — half of which was the route-clock bug). bug#4 steering gate now coincides with the layer-1 boundary (5==5), cleaner than before. Stat display: `/20` scale + amber bonus bar segment.
- **(7) Lin-yue combat ally + UI round**: Lin-yue promoted to a recruitable combat ally (`allies.lin_yue`: ranged support, EMP blaster, covering_noise+patch_protocol; her side-arc completion sets `met_lin_yue`+affection → joins fights; removed from `relationship_subjects` per the declare-once invariant). **5 combat sprites generated via codex Imagen** (portrait-referenced, 512x768) + local flood-fill background keying → RGBA (codex's image tool can't emit alpha). Also: epiphany skill names → amber; CHARACTER tab bond rows click → companion portrait card (back-to-player); novelty-guard retitle/tail now language-aware (KO leak in EN fixed); IX-clear reward (`ix_vanquisher` trait + signal_blade starting item, data-driven `clear_reward`); market vendor = Lin-yue (canon `lin_yue_deal`) with GM directive + barter dock header; scene-image pacing guardrails (combat R1-only, high-tension every-2-turns); appearance prompt injection gated to brief-visible characters. `make check` **697** green.
- **(6) Manual save slots + BGM**: manual saves were upserting the per-loop autosave bookmark ("세이브 하나뿐") — each manual save is now a distinct slot carrying a **full state snapshot**, and `POST /save-slots/load` (`load_save_slot`) actually **restores that moment** (saved scene re-staged as latest, no scene deletions; autosave bookmark unchanged; cache refreshed). Opening entry now force-enables BGM (`enableBgm`) even if a past session persisted it off. 3+1 tests. `make check` **696** green.

## 2026-07-03 — Game-completeness pass (CBT polish): boss fairness + in-run build + tutorial party
- Status: In progress toward "게임 완성도" goal. 3 slices done + verified (`make check` **678 tests** green, API restarted live). Player-perspective reframe: most CBT testers judge on loop 1 (30-60min), so within-loop experience > cross-loop meta.
- **#1a Boss fairness (measured, `sim_boss.py`)**: the IX fight was balanced for a 3-4 unit party — solo 0% / Se-rin-only 8% winnable. Added data-driven party-size scaling (`build_encounter` `min_party`): escort drones drop for a small party. Re-measured: solo 16% / Se-rin 28% / +Kai 78% / +Han 90%. Balance invariant (party ≥0.50 / solo ≤0.95) still holds. Regression test `test_boss_escorts_scale_to_party_size`.
- **#1b In-run build boons (`boons.py`)**: a light roguelite build layer — loop start + each combat victory offer a seed-deterministic 1-of-3 boon (stat deltas that fold through `_player_combat_stats` → HP/focus/defense/damage), this-run-only. `choose_boon` service + `/boons/choose` API + `boons` snapshot field + `BoonOffer.tsx` overlay (i18n KO/EN, CSS). 6 tests. Lets a small party scale into the climax through build choices.
- **#4 Tutorial party (partial)**: first loop (`runs_completed==0`) guarantees the Se-rin + Kai party (+ `met_*`/`tutorial_loop` flags) → the first-time IX climax is fair (78% vs 28% solo-Se-rin). Test `test_first_loop_seeds_tutorial_party`.
- **#2 Ending hook**: the end screen now sells the loop — a "carried into the next loop" block (this run's combats won + build boons + a meta-accrual line), so a loss is still progress and CBT players feel the "one more loop" pull (`EndedPanel` in `StoryPanel.tsx`, i18n KO/EN + CSS).
- **#3 Loop-aware world + Echo inscription**: each loop records `_loop_index`; on repeat loops the synopsis tells the GM the anomaly is recurring (Outer Wilds/Deathloop framing). **Echo-as-loadout** (`echoes.py`): a carried Echo can be inscribed at loop start for a deterministic run stat modifier (`/echoes/inscribe` API, `echoOffer`/`echoInscribed` in the boons view, BoonOffer overlay 2nd phase). Memory becomes mechanical. 5+2 tests.
- **#4 Achievement-gated recruitment (+tutorial)**: cumulative milestones (`total_combats_won>=3→han`, `runs_completed>=2→su_ah`, `total_clues>=5→lin_yue`, `won>=6→tae_o`) unlock a companion in `unlocked_allies`; `attach_side_anchors(unlocked_companions=...)` gates each companion meet-arc so Se-rin+Kai are the always-available tutorial party and the rest are earned (no chicken-egg — keyed on play, not on having met). Progression + side-anchor-gating tests.
- **Depth extensions**: (i) Echo inscription → **themed effects** (5 flavored archetypes e.g. "IX 패턴 간파"=+perception/crit, not a bare stat); (ii) boon pool **6→12 with tag synergy** (2+ of a tag pours a bonus into its stat → focused builds); (iii) loop-aware **IX recognition dialogue** (5 lines escalating by loop index, injected into the synopsis). +synergy/echo-effect/IX-line tests.
- **ALL FOUR SYSTEMS + DEPTH EXTENSIONS COMPLETE + VERIFIED**: `make check` **688 tests** green, API restarted live. Earlier this session (bug fixes): soft-defeat combat cooldown + boss-climax terminates (see below).

## 2026-07-03 — IX boss no-show ROOT CAUSE: zombie active-combat (not tension-archive)
- Status: Completed (`make check` EXIT=0, **667 tests** green; unpushed). Live cloud (Gemini) repro of the boss-not-firing bug, diagnosed per `/diagnose` with before/after evidence.
- Root cause (CORRECTS the recorded theory): the tension-100 auto-archive was the *symptom*, not the cause. Evidence from the live loop (`loop_99ac4fe6…`): a turn-5 `patrol_ambush` was left `_combat.active=true, outcome=null` (never resolved to a finish) and narrative play continued on top of it (turns 6-26). Because `_begin_requested_combat` is gated on `not CombatService.is_active(loop)` (`session.py`), the zombie combat kept `is_active` permanently True, so the IX climax was silently skipped when the route pointer reached the boss node (rn11, turn ~20). Nothing owned the loop end → tension climbed unguarded to 100 → "Journey's End". Proven by injecting the real serialized `_combat` into a `_commit_scene` repro: boss fired NONE→ix_confrontation once cleared.
- Upstream defect: `choose`/`stream_choose` had no `is_active` guard, so a narrative choice mid-combat generated a story scene and orphaned `_combat.active=true` (how the turn-5 zombie formed). `combat_action` already guarded the inverse.
- Fix (2-layer + invariant): (A) `_redirect_to_active_combat` — `choose`/`stream_choose` re-sync to the live fight instead of generating narrative when combat is active (prevents new zombies + rescues existing ones by forcing the abandoned fight to resolve). (B) a deliberate route-node combat (patrol/boss climax) now supersedes a stale mismatched `_combat` (`_without_combat_state`), so the authored IX climax launches even if a zombie lingers (rescues in-progress loops at the boss). (C) 2 regression tests in `test_session_combat.py` (boss supersedes zombie; narrative choose mid-combat doesn't orphan).
- Verified: focused repro before/after (real route_map + real `_combat`) + local cloud API restarted so the fix is live. The dead loop (`loop_99…`) is `ended`/unsalvageable — needs a fresh playthrough to re-verify live at the boss.
- **Round 2 — loop→combat→boss clean-play (same day, live Gemini QA `loop_52ba…`, `make check` 670 green):** live play exposed a cascade once the boss could fire — (a) **death-spiral**: a soft defeat adds +15 tension → the cooldown's high-pressure bypass re-throws ambient combat every turn, and 0 wins keeps the risk cap at the lowest tier so the same downgraded `patrol_ambush` recurs endlessly ("무슨 선택지든 전투로만"); (b) **boss never terminates**: `_combat_soft_defeat` treats a boss loss as recoverable, so the unwinnable IX fight re-throws forever ("패배해도 계속 이어짐"). Fixes: **boss climax now ENDS the run** — `_is_boss_climax_encounter` + `_resolved_boss_climax_loop` resolve victory→perspective ending / defeat→erasure (with a guaranteed victory fallback), minting an Echo, never a soft defeat; **soft-defeat recovery beat** — `SOFT_DEFEAT_COMBAT_COOLDOWN_SCENES=3` suppresses ambient combat (tension can't bypass) for 3 scenes after any soft defeat (route-node combat unaffected). Frontend: combat-finish now scrolls the result panel to the top (`scrollTopRef`) instead of staying pinned to the bottom. +3 regression tests. Balance items flagged for human: IX HP vs solo-Se-rin party (only Se-rin recruited → other companions are optional side arcs), and progression only accrues on victory.
- Next: human live play-QA on a fresh loop (boss fires → resolves to an ending; combat pacing sane); decide boss balance / party-size; push `main`.
## 2026-07-02 — CBT feedback: UI accent hierarchy + visual provider-label truth fix + cloud stack live
- Status: Completed (`make check` 623 green; committed `7b8ad88`, `6afa6d9`, unpushed). Interactive session, Vertex cloud stack live.
- Changed (UI, `6afa6d9`): first external CBT feedback — the all-green terminal palette gives no visual hierarchy. Added 2 accents (green stays primary): `--head` amber `#ffb454` for section/panel headers (PARTY/TARGETS/SKILLS/STATUS/OPERATION MAP/TACTICAL BOARD + codex/tab/objective kickers) → landmarks instead of `--term-dim`; `--threat` magenta `#ff5fd0` for the ENEMY roster title + enemy card borders (adversary signature, sets up IX). Gauge progressive danger colors (warn/danger) deliberately left intact (flattening TENSION to magenta would remove info). `roster-section--enemy` modifier added in `CombatRoster.tsx`. White-header variant = set `--head #f2fff9`.
- Changed (visual, `7b8ad88`): asset records logged the request's local-FLUX default labels even on a Vertex Imagen generation (found live: Imagen ran, logs said `flux_local_mps`). `VisualService.generate` now reconciles provider/model_id from the active provider's `provider_label`/`model_label` before any record/log; curated bypass + label-less providers unaffected. +2 regression tests.
- Fixed (UI overlap, `52f7aa1`, playtester report): the combat command console (`#combat-controls`) was `position:sticky; bottom:12px; z-index:12` + `margin-top:auto` — when roster + console exceeded the viewport the bottom-pinned console slid up over the roster (scrolling down un-stuck it). Now flows normally below the roster (16px flex gap); live-verified 0px overlap at scroll top.
- Verified: `make check` EXIT=0 623. UI live-verified in Chrome via 3 injected mockups → applied hybrid → hard-reload from built CSS (headers `rgb(255,180,84)`, ENEMY `rgb(255,95,208)`, enemy border magenta, TENSION fill still amber-progressive). Cloud stack (`make api-cloud` + `visual-worker-cloud-bg`) live: narration `VertexGeminiJSONProvider` ~4-5s.
- Blockers: push is human-only (private-repo classifier) — main **ahead 26**.
- Next: human — push + redeploy; live-play checklist. Optional: swap `--head` to white if amber reads too warm.

## 2026-07-02 — bug#4 layer-1 curated-image directive fix + CBT move committed + stale-note cleanup
- Status: Completed (`make check` EXIT=0, **621 tests** green; committed `ac7f9d1..3b7d26b` (4), unpushed).
- Changed: (1) **bug#4 fixed** (`3b7d26b`) — the opening prologue suppresses route steering until turn 5, but layer 1's only fresh turn is turn 4 (inside the window), so a layer-1 anchor's curated-image directive (night_market) was **never emitted** and turns 5-7 got the anti-repeat "move forward" directive against a never-established scene. Diagnosed per `/diagnose` (per-turn simulation over the real neo-seoul route map, 3 hypotheses, before/after: turn-5 emitted False→True, layers 2+ unchanged). Fix: NEW `ROUTE_STEERING_START_TURN=5` shared by the gate + `fresh_node`; regression test `test_layer1_anchor_image_hint_fires_on_steering_resume_turn`. (2) CBT root→`docs/cbt/` move committed (`ac7f9d1`; the 2 teaser videos ~124MB stay local + gitignored — YouTube is distribution). (3) stale `test_assets.py` skill-exclusion note cleared in STATUS.md + QA-seed box ticked (`fb04d51`). (4) serena config schema sync (`6c92c5b`).
- Verified: `make check` EXIT=0 621 green (new regression test included); route suite 28 green; diagnostic sim re-measured after fix.
- Blockers: push is human-only (private-repo classifier) — main now **ahead 22**.
- Next: human — push + redeploy; live-play checklist (`docs/test/neo_seoul_live_qa.md`). Remaining deferred: glass-library EN glossary (on hold), dev-log KO literals (dev-only).

## 2026-07-02 — Pre-CBT hardening: boss climax + 4 combat/route bugs + EN Korean leaks + refactor
- Status: Completed + locally live-verified (`make check` 620 green; committed `ebc7905`..`5d837ee` (12), **unpushed**).
- Fixed (combat/route): (1) **IX boss climax never fired** — the "Confront IX" route node's combat ran through the ambient pacing gate (`_gate_next_combat`), whose risk-cap (max 4) always downgraded the risk-5 boss and whose cooldown suppressed it; and even after bypassing the gate, ambient combat (encounter-map contact / LLM `start_combat`) coinciding on the boss-entry turn stole precedence, so the parked node never re-fired. Route combat now takes precedence AND bypasses the gate. (2) encounter-map contact left "engaged" when the gate downgraded / a boss overrode it → phantom re-trigger loop; resolve the original contact. (3) `_combat_snapshot` used the neo-seoul-default `options.scenario_id` not `loop.state` → wrong combat data on non-neo-seoul resume. (4) `_roll_loot` `KeyError` on a loot entry missing `item`.
- Fixed (EN Korean leaks, QA finding): code-generated Korean reaching EN players — ending titles/narrations, combat title/objective/fallback, save-slot prefix, codex/lore, validator/parser/choice-impact fallbacks, factory defaults, opening/arc titles (neo-seoul `en.json` glossary +36 / phrases +2, all via `localize_for`); `GET /loops/{id}/scenes` had no localization (added `lang`+`localize_for`, scenario from loop); 2 frontend literals (onboarding status, image placeholder) → `DICTS[getLang()]`; **skill-tree error toasts** localized (`learn_skill` detail via `localize_for`, `LearnSkillRequest.lang`, frontend sends `getLang()`, `ff0da98`). Free LLM prose unchanged (fresh EN loops clean; legacy KO prose not glossary-translatable).
- Refactor: extracted the next-combat decision into `_resolve_next_combat` (+ unit test). Regression tests: boss climax fires under gate + wins coinciding ambient, loot skips malformed, precedence.
- Live-verified (local, no charge): fallback API HTTP scan (begin/choose/combat/skill-error/save/memory/scenes) = **0 Korean**; `/combat/begin ix_confrontation` builds IX+2 adds. The scan surfaced + fixed **4 IX-combat leaks** the 2026-06-30 boss content never got glossaried (weapon `최적화 빔` + 3 encounter-meta fields, `57a89e3`). **AGY browser QA** (`scripts/live-qa/run-agy.sh` probe/fallback, Chrome DevTools) = `PASS_CANDIDATE`, 0 findings, git-invariant — main screen EN render confirmed (shots `outputs/live-qa/20260702-033059-probe/`). Live-QA checklist slimmed 276→47 lines (human-actionable only).
- Verified: `make check` EXIT=0, 620 tests. Two subagents (adversarial bug-hunt + EN Korean-leak audit) drove the findings.
- Follow-ups (deferred): bug#4 curated-image directive unreachable at first act-1 layer (`scenario_context` fresh_node); glass-library has no EN overlay glossary (on hold); dev-log ~32 KO literals (dev-only). Human/live: IX boss route-fire + feel + phantom-loop + natural-ending render (checklist).
- Next: human — push `main` (unpushed) + redeploy so testers get the boss fix + EN cleanup.

## 2026-07-01 — CBT recruitment assets finalized + edited teaser video + itch.io setup guide
- Status: Completed.
- Changed: Moved CBT files from root to `docs/cbt/` (localized English/Korean posts, teasers). Updated final YouTube link (`https://youtu.be/rpWdpMAqfnw`) and Google Form link (`https://docs.google.com/forms/d/e/1FAIpQLSfAbQWTge08B9fHnEJQL0QYMWeoQ6AD-tS6HFSYMJppEkjuww/viewform?usp=dialog`) across all files. Reframed `CBT_RECRUIT_SOLORP.md` to be conversational and targeted at solo RPG/GME players (emphasizing AI Oracle + automated bookkeeping + grid combat). Created `docs/cbt/ITCH.md` detailing Itch.io project page setup.
- Verified: Edited raw 6-min teaser video into a 2.5-min trailer `docs/cbt/Mythos_Teaser_Edited.mp4` using `h264_videotoolbox` hardware encoding. Extracted high-quality UI/Combat preview screenshots. Checked links and format integrity.
- Blockers: None.
- Next: Human/non-blocking: push to origin, post the thread in `#showcase-your-game` on AI Game Dev Org Discord (giving feedback to 2 other games first), and monitor initial playtest submissions.

## 2026-06-30 — IX boss DESIGN (claude lane): real climax fight, data-driven
- Status: Completed (`make check` 611 green, committed; unpushed). Overnight `[auto:claude]` IX boss design lane (plan `docs/plans/2026-06-30-ix-boss-fight.md`). The Neo-Seoul climax was narrative-only (boss route node spawned generic enforcer/mech); now it's a real Administrator IX fight.
- Changed (`resources/neo-seoul/scenario.json`, data-driven, no engine edits): bestiary `administrator_ix` (boss-tier hp 38 / def 13 / armor 2, ranged) + unique weapon `ix_optimizer_beam` (1d10, range 5, armor_pen 1); encounter `ix_confrontation` (IX + sentinel_drone + purge_drone adds, 10×7 arena → procedural hazard-rich terrain); route `combat_encounters.boss` → `["ix_confrontation"]` (single → deterministic resolve). Placeholder sprites at the 6 IX contract paths (cp suppression-mech; codex art lane overwrites).
- Scope decisions: (a) enemy AI consumes only `primary_weapon()` — bestiary `skills` are NOT used mechanically, so IX distinction is via stats/unique weapon/adds/large arena, **not** player-tree skills (adding enemy skills to `combat.skills` would pollute the Codex tree + add inert icon burden). (b) Did **not** add `ix_confrontation` to the Director `start_combat` list — boss fires only at the route boss node, never mid-story. (c) `enforcer_standoff`/`mech_siege` stay declared + balance-tested (mech_siege still in `combat` pool; both Director-reachable).
- Verified: balance tuned empirically via the deterministic greedy sim — `ix_confrontation` party(se_rin+kai)=0.68 / solo=0.00 (FLOOR 0.50 / CEIL 0.95), by far the hardest encounter (all others ~0.98). NEW `test_boss_node_resolves_to_ix_confrontation_with_ix_present` (boss node → ix_confrontation, IX boss-tier + present). `make check` EXIT=0 611.
- Next: codex art lane generates real IX portrait/5 poses/skill icons to overwrite placeholders.

## 2026-06-30 — Post-combat EN fix (redeploy) + CBT recruitment assets + IX boss plan
- Status: deployed (rev `mythos-api-00003-fzb`), `make check` 610 green, committed `649aeea..de95632` (unpushed).
- Changed: **post-combat continue stayed Korean even in EN** — `useCombatRest.continueAfterCombat` sent a hardcoded Korean action + no `lang` on the WS choose, so the LLM regenerated KO + data unlocalized. Now uses localized `sess.postCombatAction` + `lang=getLang()` (`86d3970`). All generation paths now thread lang. Redeployed.
- Added: `CBT_TEASER.md` (2-min teaser shot script, admin link, no-keys-on-screen) + `CBT_RECRUIT_POST.md` (r/Playtesters **Unpaid Playtest** text post, embed video + Google Form). IX boss-fight plan `docs/plans/2026-06-30-ix-boss-fight.md` + NEXT_PLAN 2-lane (`[auto:claude]` design + `[auto:codex]` art, shared sprite-path contract).
- Verified: deployed post-combat-shape choose (EN action + lang=en) → English narration + **0 residual Korean** in served snapshot. Cost: Cloud Run scale-to-zero idle ~$0; stopped leftover local cloud-pointed api/visual-worker.
- Findings (not yet fixed): **IX boss is narrative-only** (no boss enemy/encounter — route boss node spawns generic enforcer/mech) → boss plan added. **Korean on resume/load = persisted pre-fix prose** (free narration isn't re-translatable at serving; only DATA is) → fresh EN loops are clean; old loops keep KO (wipe Neon to reset).
- Next: human — push `main` (FF-merged, ahead of origin) · billing alert + Vertex daily quota · feedback Form · run overnight to consume the IX boss task.

## 2026-06-29 — 🚀 GCP closed beta DEPLOYED LIVE (Cloud Run + Neon + Vertex)
- Status: LIVE + end-to-end verified. URL `https://mythos-api-1004528040791.us-central1.run.app` (revision mythos-api-00002-9wf, us-central1). Agent ran the deploy via gcloud (user ran the IAM/SA/bucket prereq block + Neon signup — safety rules forbid agent IAM/account/billing changes).
- Infra: dedicated SA `mythos-run` (aiplatform.user + cloudtrace.agent), GCS bucket `mythos-assets-…` (objectAdmin), **Neon Postgres 18** (`migrations/001-007` applied via psycopg libpq). Deploy = lean Dockerfile (prebuilt static), `--allow-unauthenticated` + invite-gate, min-instances 0 / max 3, env via `--env-vars-file`.
- Verified LIVE: health/SPA 200 · gate (no-key 401 / key 200) · **real Vertex Gemini EN narration** + Neon persist · EN default + English tab title · admin key uncapped (12/12 200) vs tester capped (11th=429).
- Changed (code, this batch): EN combat-log i18n (K6, `mythos_combat/log_i18n.py` + `CombatState.language`); EN default flip (`getLang`/`resolveInitialLang`/`index.html`); BGM first-gesture auto-on; **admin keys** cap-exempt (`MYTHOS_ADMIN_KEYS`, cyrb53 port verified vs node); browser tab title EN. `make check` 610 green.
- Keys: 8 tester + 1 admin (`admin-43dc07f266c2`, uncapped) in `INVITE_KEY.md` (gitignored). Cost guards live: invite gate + loop cap 10 + scale-to-zero.
- Blockers: agent can't push (private) / can't run IAM·billing·destructive DB TRUNCATE (classifier-blocked) → user does those. Test-data cleanup (Neon TRUNCATE) left to user (optional, harmless).
- Next (human, non-blocking): billing budget alert + Vertex daily quota · feedback Google Form · distribute `?invite=` links → r/playtesters · `git push`.

## 2026-06-29 — EN default flip (global-first) + BGM auto-on
- Status: Completed (`make check` 609 green, Chrome live-verified, committed `05aae47`, unpushed). EN is now end-to-end (UI+narration+combat log via K6), so the product default language flips to English.
- Changed: `resolveInitialLang()` (`i18n/lang.ts`), `getLang()` (`api.ts`), and `index.html` `<html lang>` now default to `en`; `?lang=` + stored choice still take precedence (KO users + toggle unaffected). Playwright E2E pinned to `?lang=ko` (asserts the KO baseline). BGM: enabled-by-default + NEW one-time first-gesture (pointerdown/keydown) autostart in `App.tsx` → BGM turns ON at first interaction on any entry path (autoplay-policy-compliant; still toggleable off).
- Verified: fresh visitor (cleared storage, no `?lang=`) → EN boot/UI, language toggle reads `한국어`, `<html lang=en>`; trusted first click → BGM button `START`→`ON`. `make check` EXIT=0 609.
- Next: K9 full ending screen live verify (AGY live-QA candidate) → deploy (human/infra).

_(older 2026-06-29 entries — K6 EN combat-log i18n, CBT invite-identity/save-load/gate — archived to `bin/docs/archive/progress-2026-06.md`.)_
