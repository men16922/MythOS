# Progress Log

Last updated: 2026-06-28

This file keeps **only the latest incremental summaries** (latest 5 items). The long 2026-06 detailed log (including per-stage route-node session detail) is in
`bin/docs/archive/progress-2026-06.md`, the 2026-05 log in `bin/docs/archive/progress-2026-05.md`.

## 2026-06-28 — GCP closed-beta ③: Cloud Run containerization (lean) + WS verified + env-driven storage
- Status: Completed (artifact + local verification). The FastAPI backend now containerizes into a lean Cloud Run image; the actual `gcloud run deploy` stays human/infra-gated. Next §3 deploy-prep step after the live-validated providers.
- Key insight (GCP_PLAN §1): narrative=Gemini and image=Imagen are API calls, so the container OMITS the heavy local ML stack (torch/diffusers/mflux/transformers/accelerate/safetensors/sentencepiece/huggingface-hub) + streamlit. Verified `mythos_api.create_app()` imports cleanly with all of them blocked (the lazy-import discipline holds).
- Changed: NEW `Dockerfile` (python:3.11-slim, `requirements-cloud.txt` lean deps + `pip install --no-deps -e .`, copies src+resources, CMD `python -m mythos_api` host 0.0.0.0 + `$PORT`→`MYTHOS_API_PORT`), `requirements-cloud.txt`, `.dockerignore`, `make cloud-image`/`cloud-run-local`. Wired `mythos_api.service.get_storage_adapter()` → env-driven (`MYTHOS_STORAGE_BACKEND=gcs`→GCS else MinIO) + NEW `SigningStorageAdapter` union; `app.py` storage annotations widened (6 sites). `tests/test_api.py` +1 (backend selection).
- Verified: `docker build` OK — **287 MB** image. Ran the container DB-free with cloud env (`MYTHOS_NARRATIVE_PROVIDER=gemini`/`MYTHOS_VISUAL_PROVIDER=vertex`/`MYTHOS_STORAGE_BACKEND=gcs`): boot clean, `/` (SPA) 200, `/api/v1/health` 200, WS `/api/v1/loops/stream` **101 handshake** + clean close. `make check` green — **586 tests OK**, mypy clean.
- Next (follow-up before deploy): image WRITE-path storage `gcs` branch in `visual_orchestration.py`/`visual_worker.py` (read/sign path done). Then human: `gcloud run deploy` + DB decision (Neon/Cloud SQL) + GCS bucket.

## 2026-06-28 — LIVE Vertex Gemini validated (real call) + thinking-budget root-cause fix
- Status: Completed. First real Vertex call (human ran `gcloud auth application-default login`; `.env` has the project). The narrative provider works end-to-end against `gemini-2.5-flash`, and a live-only bug was diagnosed + fixed. Installed `google-genai` 2.10.0 (`pip install -e .[gemini]`).
- Diagnosis (diagnose-first, CORE_MANDATES §5): first live smoke returned a valid Korean scene but `outcome=provider_repair` (not `success`), latency ~18s. Reproduced over 5 runs: gemini-2.5 is a **thinking model** — reasoning tokens draw from the SAME `max_output_tokens`; ~2/5 runs hit `finish=MAX_TOKENS` with `thoughts_tok≈1965` and the JSON truncated to ~130 chars → first-parse fail → repair round-trip. With `thinking_budget=0`: **5/5 first-try parse**, faster, cheaper.
- Changed: `gemini_provider.py` — NEW `GeminiConfig.thinking_budget` (env `GEMINI_THINKING_BUDGET`, default **0 = off**) threaded into `_generation_config` (`thinking_config={"thinking_budget": N}`, shared by generate + stream). `.env.example` doc + `[gemini]` install note. `visual_service.py` — `# type: ignore[attr-defined]` on the now-resolved `genai.Client` (SDK installed). Tests: +thinking_config assertion + configurable-budget + default-0.
- Verified: live director smoke 3/3 `success`, latency **18s→~5s** (no thinking, no repair). `make check` green — **585 tests OK**, mypy clean (129 files). **Imagen also live-validated**: `VertexImageProvider` generated a 1024×1024 on-prompt cyberpunk PNG in ~6.9s (`imagen-3.0-generate-002`). GCS adapter not live-tested (no bucket provisioned yet).
- Next: minimal closed-beta deploy (Cloud Run/WS, DB decision, GCS bucket) — human/infra-gated.

## 2026-06-28 — GCP closed-beta adapters: Vertex Imagen `VisualProvider` + GCS `StorageAdapter` + Gemini env reconcile
- Status: Completed (code, gate-verified). Both remaining GCP_PLAN §8 cloud adapters done (narrative provider was the 1st); the local FLUX/MinIO paths stay the default (behavior-preserving). Also reconciled `GeminiConfig` to the google-genai SDK's native env names after the human wired a real Vertex project into `.env`.
- Changed: `visual_service.py` — NEW `VertexImageProvider` (Imagen via google-genai, lazy import + injectable client, aspect-ratio mapping from request dims, per-request `vertex_image_model` override; cloud cost driver per GCP_PLAN §1) + NEW `GCSStorageAdapter` (`gs://` URI, v4 signed read URL, non-`gs://` passthrough — MinIO analogue) + `default_visual_provider` honors `MYTHOS_VISUAL_PROVIDER=vertex|imagen` + NEW `default_storage_adapter` (`MYTHOS_STORAGE_BACKEND=gcs|filesystem|minio`, default minio). `gemini_provider.GeminiConfig` now reads env at **construction** (`default_factory`) and accepts SDK-native aliases (`MODEL`/`PROJECT_ID`/`GOOGLE_GENAI_USE_VERTEXAI`) beside the `GEMINI_*` names — the import-time read would have missed a late-loaded `.env`. NEW `tests/test_vertex_visual.py` (13). `pyproject.toml` `[gcs]` + aggregate `[gcp]` extras (Imagen reuses `[gemini]`'s google-genai). `.env.example` image/storage cloud block + SDK-native Gemini names.
- Verified: `make check` green — **584 tests OK** (skipped 2), mypy clean, ruff/eslint/tsc/vite. New tests use injected fake clients (no Google SDK): Imagen generate writes PNG + aspect ratio, model override, no-images/missing-SDK errors, env-name reconcile; GCS upload→`gs://`, signed URL, passthrough, malformed-URI/missing-SDK errors; both env factories.
- Blockers: none. Cloud calls still need a live GCP service account/ADC (human) — all paths SDK-free via fakes. Adapters are exported + env-gated but existing call sites still default to MinIO/FLUX (closed-beta deploy flips the env).
- Next: human live-test Gemini+Imagen against the real Vertex project now in `.env` (CLOSED_BETA_FEEDBACK_STRATEGY §2). Remaining GCP_PLAN §8: Cloud Run containerize + WS verify, DB decision — both human/infra-gated.

## 2026-06-28 — Gemini/Vertex narrative provider wedge FINISHED + gate-verified (priority ②, GCP closed beta)
- Status: Completed. The Gemini/Vertex `JSONProvider` scaffold (was uncommitted, gate-unverified) is now fully wired, tested, and gated — the cloud product-LLM path for the GCP closed beta. The local Ollama dual-model path is byte-unchanged (default).
- Changed: NEW `src/mythos_narrative/gemini_provider.py` — `VertexGeminiJSONProvider` (controlled generation via `response_schema`=`SCENE_GEMINI_SCHEMA`, a `to_gemini_schema()` conversion of `SCENE_JSON_SCHEMA` handling `["x","null"]`→nullable + recursion + enum/required) + `GeminiConfig` (env-driven model/use_vertex/project/location/api_key/temp/max-tokens) + `build_narrative_provider()` (selects by `MYTHOS_NARRATIVE_PROVIDER`, default ollama) + lazy `google-genai` import + injectable client + **`stream()`** (`generate_content_stream` → director streaming path via `NarrationFieldExtractor`). `director.py` `__init__` uses the factory (default → OllamaJSONProvider; `GeminiConfig` has no `ollama_model_*` attrs so `_use_dual_model()` is False → controlled-gen JSON parses first-try, no parser/repair). NEW `tests/test_gemini_provider.py` (16 tests). `pyproject.toml` `[gemini]` extra (`google-genai>=1.0.0`). `.env.example` `MYTHOS_NARRATIVE_PROVIDER` + Gemini/Vertex vars (model, use_vertex, project, location, api_key, temp, max-tokens).
- Verified: `make check` green — **568 tests OK** (skipped 2), mypy clean (128 files), ruff/eslint/tsc/vite. New tests cover schema conversion (nullable/array/enum/no-source-mutation), `_split_messages` system/user split, factory selection (default→Ollama, gemini|vertex→Vertex), controlled-gen round-trip, missing-SDK actionable error, **director legacy-path first-try parse (1 provider call, outcome=success, no repair)**, and streaming (provider chunks + director final scene).
- Blockers: none. Live Vertex call still needs a GCP service account / ADC (human, no creds in-repo) — construction + parsing are SDK-free via the injected fake client.
- Next: human live-test against a real Vertex project (CLOSED_BETA_FEEDBACK_STRATEGY §2) → minimal deploy → r/playtesters. Human-gated parallel: neo-seoul KO live-QA A·F sign-off → EN default flip (one-liner).

## 2026-06-28 — EN/KO: glass-library localized → BOTH scenarios fully EN (code, make check green)
- Status: Completed. glass-library (2nd scenario) now renders English end-to-end in EN mode — EN prompt 0 Korean lines (turns 0/1/2/5), KO byte-preserved. Both shipped scenarios fully localized.
- Changed: `scenario_context` `DEFAULT_STAT_VOICES_EN` + `DEFAULT_ENCOUNTERS_EN` code defaults (+ `_default_stat_voices`/`_default_encounters` language selectors) — glass-library ships no directive `.md` so it used the Korean code defaults; now language-aware (neo-seoul still overrides via its `.en.md`). NEW `resources/glass-library/story_bible/bible.en.json` (17 entries, structural fields identical) + `resources/glass-library/i18n/en.json` (name/brief/archetypes/session_intro/ui_copy/arcs/endings/npc) — both authored via subagents, verified parity. The `/scenarios` API + bible loader already loop over all scenarios, so glass-library is served localized with no extra wiring. `test_language_plumbing.py` updated (overlay-empty check uses a nonexistent lang; glass overlay asserted present).
- Also localized the UI helper/hook string tail (was outside the `.tsx` scan): `choices.ts` (cost/req labels + bilingual `cleanChoiceLabel`), `skillState.ts` (learn/upgrade/processing), `combatText.ts` (enemy intent), `useSessionControls`/`useDataLoaders` status + skill-notice banners, IntroPanel/BootIntro defaults, `CombatCinema` skill name (lang-aware). Remaining Korean in source is now only: comments, the intentional brand/toggle (`세계 : 접속`/`한국어`), bilingual data-matching (`STAT_CANON`/`getSkillId`/gauges), dead unused data (`SKILL_REGISTRY.description`), and `logToConsole` dev diagnostics.
- Verified: `make check` green — **552 tests OK**. Both scenarios' EN prompts 0 Korean; zero player-facing Korean UI in EN mode; KO unchanged.
- Remaining (not autonomously doable): S5 model eval (needs live Ollama head-to-head); **default flip — gated on the human's neo-seoul KO live-QA sign-off**.

## 2026-06-28 — EN/KO: neo-seoul fully localized end-to-end (code, make check green)
- Status: Completed. In EN mode neo-seoul (primary scenario) renders English everywhere — UI, API-served data, AND the entire backend prompt: **zero Korean leaks** in the prompt across opening turns 0-5 (verified). KO byte-preserved.
- Changed: DevConsolePanel `t()` (S3 UI now 100% of components). `scenario_context` route note-builders `_route_director_notes`/`_route_junction_notes` gain EN variants (threaded `language`); opening-beat `{shot_title}`/`{shot_body}` fills now prefer the i18n overlay's English cinematic_shots. `story_bible.load_story_bible(scenario_id, language)` — prefers `bible.<lang>.json`, graceful KO fallback; NEW `resources/neo-seoul/story_bible/bible.en.json` (29 entries translated, structural fields `id`/`when`/`tags`/`priority` byte-identical so selection is unchanged). API: `i18n/en.json` extended with `ui_copy.signal_body` + session_intro `rules`/`continue_button`; endpoint merges top-level ui_copy + intro list fields. `tests/test_language_plumbing.py` +2 (bible parity/fallback).
- Verified: `make check` green — **552 tests OK**. EN prompt 0 Korean lines (turns 0-5); KO unchanged.
- Remaining: glass-library (on-hold 2nd scenario, full translation); S5 model eval (needs live Ollama); **default flip — gated on the human's neo-seoul KO live-QA sign-off**.
