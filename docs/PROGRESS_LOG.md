# Progress Log

Last updated: 2026-09-06

## 2026-09-06 — Status-effect stacking implemented (owner "option 2"), browser-verified

- Status: NEXT_PLAN Priority 1 / Combat closed — both `[auto:claude]` bullets `[x]`. Session started from `git log` (`/sync`): last work was the 2026-09-06 overnight close + P0-2 token-usage fix (`c723694`/`7a26d66`); most open items are `[manual]`/`[blocked]`, so the one owner-decided-but-unbuilt item was taken. Design snapshot first (`docs/plans/2026-09-06-status-effect-stacking.md`), then the build.
- Changed: `status_rules.py` is the per-status table (turns cap · stack cap · DoT dice · per-stack armor/defense); `Combatant.status_stacks` sits beside the unchanged turns ledger (missing entry = 1 stack — ~30 test sites + saves keep working), read through `status_stack(id)` (clamps to cap), removed through `clear_status()`/`clear_all_statuses()` (tick expiry, hacked consumption, revive). Caps burn 3 / corrode 2 / acid 2; freeze/shock/hacked single-stack. Applied log line gains ` (중첩 ×N)`/` (×N)` + `detail.stacks`; blip payload carries `status_stacks`; roster chip `burn ×2`, canvas badge count pip, board status pop `×N`; companion AI reapplies a stacking rider until its cap. Detail: `docs/plans/2026-09-06-status-effect-stacking.md`, COMPLETED M80.
- Verified: `make check` green — ruff/format, mypy 199 files, eslint, build, **1375** tests (6 skipped, +12; `StatusIntensityStackingTest` incl. burn+acid+shock+hacked mid-tick-death regression). `/code-review medium` on the diff (6 confirmed / 2 plausible / 0 refuted) folded in before commit — the important one: the stack seed was read *after* the turns write, so a legacy-active status reapplied stayed at 1 stack. **Browser** (`make api`, non-fallback sim `stray_incinerator` solo): two plasma-torch hits → "🔥 burning!" then "🔥 burning! (×2)", DoT 2 → 4, roster `burn ×2`, canvas pip "2", 0 console errors — `outputs/live-qa/20260906-status-stacking/`. Expiry/stack-clear is unit-tested only (Tester died first).
- Blockers: none. Undeployed.
- Next: human feel verdict on stack caps (3/2/2) and the pip legibility — `[manual]`; the rest of NEXT_PLAN stays owner-gated/blocked; `/overnight-seed` before the next unattended run.

## 2026-09-06 — Overnight run seeded and started; P1-2 curve measured; T4 answered; verifier 15 fixed

- Status: owner asked for the remaining priorities to run through the overnight harness. `/overnight-seed`: the two `[auto]` items were not unattended-consumable (P1-2 needs a live engine; adapter rows need engines that are not installed → `[blocked]`), so eight owner-approved seeds were recorded (ruff-format debt ×4 + lint wiring, PROGRESS_LOG compression, 14 `union-attr` ignores, `capture_trace` autoplay test). `MAX_ITER=12 make overnight` (claude actor `claude-sonnet-5`, critic `claude-fable-5-1` — the critic **does** launch on Fable 5.1 and returned PASS, closing the STATUS watch item).
- **Two runner defects surfaced and were handled**: (1) repo verifier `15-regression-validity` failed with an empty reason on every non-MissionSpec contract (bare `StopIteration`) and, being fail-closed, reverted the clean iteration-1 commit — fixed to "not applicable, skipped" (`7cd1146`, test), the commit re-applied (`badcf3b`); (2) my seed 3 named `experiments`/`scripts`, which are outside the claude-lane contract scope, so `10-diff-scope` reverted it — re-applied (`20b7ff3`), lesson recorded in `docs/LESSONS.md`. Runner stopped/restarted around each fix; 3 of 8 seeds landed (`9f41e6a`→`badcf3b`, `9abf015`, `bf50137`→`20b7ff3`).
- **P1-2 done** (`cae2190`, report `experiments/results/20260905-154138-workload-profile/`): `capture_trace` now auto-plays combat / picks boons / resumes after a fight (`b05aacb`; the 08-30 arm had stopped at the first patrol node), so a 44-call clean arm ran on `gemma4:8b-64k` (`.env`'s `gemma4:latest` is not on disk — env override). Share median **68.1%**, early/mid/late 68.2/68.7/67.4%, slope −0.05 pct/pair → **flat**; shared prefix ~10.6k chars constant, prompt 15.2k→15.8k. The bounded synopsis keeps the ratio flat — the plan's "share falls as the synopsis grows" does not hold here. Input:output 15:1 (output median 498 tok, up from 300 after the sampler fix). `workload-profile` gained the curve table/finding (`share_trend`, `73101a2`).
- **T4 answered** (`d53d030`, `docs/reference/2026-09-06-mlx-lm-server-capability-check.md`): `mlx_lm.server` does not read `response_format`; cross-request `LRUPromptCache` exists (`--prompt-cache-size` 10) but sliding-window models (Gemma 3/4, 4:1 layers) re-prefill fully (#980) while Qwen3 keeps the cache → bench B E-A on the Qwen3 ladder; the parser's `json_object` is ignored on MLX.
- Work happened in a separate worktree (`../MythOS-p12`) while the runner owned `main`; merged after each stop. PROGRESS_LOG compressed (seed B, `49779f0`). A second, unrelated Codex runner (plugin **1.5.0**) is running in `~/Desktop/AWS/...`; MythOS stays pinned at 1.4.0.
- **Run closed 01:15 — 7/8 seeds by the runner, 1 by hand.** Landed: `9f41e6a`→`badcf3b`, `9abf015`, `bf50137`→`20b7ff3`, `651ebff` (tests format), `30a1a99` (`ruff format --check` now in `python-lint`), `a960d60` (14 `union-attr` ignores → typed `_require_player`); seed 8 (autoplay test) done in the worktree (`6eeaaed`). A third verifier false positive surfaced: `10-diff-scope` read the NEXT_PLAN tick of the `type: ignore` seed as a *new suppression marker* and reverted `0c9dddb`; fixed to scan code files only (`f420bdc`, test). Format-check spend ≈ $6 for 6 landed iterations; every actor run exceeded the 12-turn contract budget (18–74 turns, recorded only).
- **P1-3 workload card** written from the arm (`docs/reference/2026-09-06-mythos-workload-card.md`): input p50 7,463 / output 498 tok, share p50 68.1% flat, c=1–4 closed-loop.
- **`/overnight-report` done and deployed `mythos-api-00088-v66`** (owner ordered both). Report: gate independently green at `8bf30b9`, 7 runner commits reviewed, no `[recovered]`, no residue, checklist `docs/test/history/0906-0115-overnight-review-checklist.md`. Deploy: 100% traffic, root/health 200, live `/app.js` = local SHA, `/assets/*.js` 200, timeout 3600; the only WARNING+ entries were my own 404 probes of the legacy `/static/` paths. Owner items left: `git push` (ahead 33), §3 verdict, `_weapon_in_range`, prod migration `008`, phone check, turn-budget enforcement decision.
- **P0-2 live gap closed** (`c723694`): a real Ollama turn still logged `prompt_tokens=None` — the dual-model paths (local default) never called `take_usage()`; fixed in `_generate_dual`/`_stream_generate_dual` with director tests, live re-check `prompt_tokens 7534 / output_tokens 1481 / provider_calls 1`. 1363 tests. STATUS gains the `.env` `gemma4:latest`-not-installed risk (silent fallback locally).
- Next: `/overnight-seed` before the next unattended run (backlog 0); P2 bench port stays blocked on the missing `labs/wsl2-vllm-baseline/` checkout.
