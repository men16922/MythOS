# Progress Log

Last updated: 2026-06-28

This file keeps **only the latest incremental summaries** (latest 5 items). The long 2026-06 detailed log (including per-stage route-node session detail) is in
`bin/docs/archive/progress-2026-06.md`, the 2026-05 log in `bin/docs/archive/progress-2026-05.md`.

## 2026-06-28 — EN/KO: glass-library localized → BOTH scenarios fully EN (code, make check green)
- Status: Completed. glass-library (2nd scenario) now renders English end-to-end in EN mode — EN prompt 0 Korean lines (turns 0/1/2/5), KO byte-preserved. Both shipped scenarios fully localized.
- Changed: `scenario_context` `DEFAULT_STAT_VOICES_EN` + `DEFAULT_ENCOUNTERS_EN` code defaults (+ `_default_stat_voices`/`_default_encounters` language selectors) — glass-library ships no directive `.md` so it used the Korean code defaults; now language-aware (neo-seoul still overrides via its `.en.md`). NEW `resources/glass-library/story_bible/bible.en.json` (17 entries, structural fields identical) + `resources/glass-library/i18n/en.json` (name/brief/archetypes/session_intro/ui_copy/arcs/endings/npc) — both authored via subagents, verified parity. The `/scenarios` API + bible loader already loop over all scenarios, so glass-library is served localized with no extra wiring. `test_language_plumbing.py` updated (overlay-empty check uses a nonexistent lang; glass overlay asserted present).
- Also localized the UI helper/hook string tail (was outside the `.tsx` scan): `choices.ts` (cost/req labels + bilingual `cleanChoiceLabel`), `skillState.ts` (learn/upgrade/processing), `combatText.ts` (enemy intent), `useSessionControls`/`useDataLoaders` status + skill-notice banners, IntroPanel/BootIntro defaults, `CombatCinema` skill name (lang-aware). Remaining Korean in source is now only: comments, the intentional brand/toggle (`세계 : 접속`/`한국어`), bilingual data-matching (`STAT_CANON`/`getSkillId`/gauges), dead unused data (`SKILL_REGISTRY.description`), and `logToConsole` dev diagnostics.
- Verified: `make check` green — **552 tests OK**. Both scenarios' EN prompts 0 Korean; zero player-facing Korean UI in EN mode; KO unchanged.
- Remaining (not autonomously doable): S5 model eval (needs live Ollama head-to-head); **default flip — gated on the human's neo-seoul KO live-QA sign-off**.

## 2026-06-28 — EN/KO: neo-seoul fully localized end-to-end (code, make check green)
- Status: Completed. In EN mode neo-seoul (primary scenario) renders English everywhere — UI, API-served data, AND the entire backend prompt: **zero Korean leaks** in the prompt across opening turns 0-5 (verified). KO byte-preserved.
- Changed: DevConsolePanel `t()` (S3 UI now 100% of components). `scenario_context` route note-builders `_route_director_notes`/`_route_junction_notes` gain EN variants (threaded `language`); opening-beat `{shot_title}`/`{shot_body}` fills now prefer the i18n overlay's English cinematic_shots. `story_bible.load_story_bible(scenario_id, language)` — prefers `bible.<lang>.json`, graceful KO fallback; NEW `resources/neo-seoul/story_bible/bible.en.json` (29 entries translated, structural fields `id`/`when`/`tags`/`priority` byte-identical so selection is unchanged). API: `i18n/en.json` extended with `ui_copy.signal_body` + session_intro `rules`/`continue_button`; endpoint merges top-level ui_copy + intro list fields. `tests/test_language_plumbing.py` +2 (bible parity/fallback).
- Verified: `make check` green — **552 tests OK**. EN prompt 0 Korean lines (turns 0-5); KO unchanged.
- Remaining: glass-library (on-hold 2nd scenario, full translation); S5 model eval (needs live Ollama); **default flip — gated on the human's neo-seoul KO live-QA sign-off**.

## 2026-06-28 — EN/KO: API serves localized scenario prose (S1-remaining "API language selector", code, make check green)
- Status: Completed for the golden-path intro content. The `/api/v1/scenarios` endpoint now localizes the player-facing prose it serves to the SPA (Onboarding/Intro/Boot), so the cinematic intro renders English in EN mode — not just the UI chrome.
- Changed: `app.py` `/scenarios` gains a `lang` query param + NEW `_localized_scenario_prose(s, lang)` (→ `_ScenarioProseL10n`) — merges the additive `load_scenario_i18n` overlay onto scenario `name`, `brief`, `ui_copy.session_intro` (title/body/objective/cinematic_shots), `endings[].title` (by index), and `archetypes[].name/attributes/starting_item` (by id); behavior-preserving for `ko`/no-overlay. `i18n/en.json` extended with name + archetypes. Frontend: `apiGetScenarios(playerId, lang)` sends `lang`; `App` passes active lang + re-fetches on toggle; `useSessionControls` threads lang into its leave-refresh. `tests/test_api.py` +1 (EN name/brief/intro/endings/archetypes localized, KO unchanged, id parity).
- Verified: `make check` green — **550 tests OK**. `?lang=en` → fully-English onboarding (name/archetypes) + intro cinematic + brief + ending titles; toggle re-fetches live.
- Also localized the boot splash (`ui_copy.signal_body`) via a generalized top-level ui_copy overlay merge → neo-seoul now has **zero Korean player-facing prose** in EN mode end-to-end (boot→onboarding→intro→play→ending).
- Remaining (low-value / on-hold / blocked): route note-builder *instructions* in `scenario_context` (output already EN via `LANGUAGE_RULE_EN` — bilingual instructions only); code-default NAMING/STAT_VOICES/ENCOUNTERS (used only by glass-library, which ships no directive `.md`); glass-library overlay+directives (scenario on hold); S5 model eval (needs live Ollama); DevConsolePanel (dev-only). **Default flip is gated on the human's neo-seoul KO live-QA sign-off** (flipping now breaks the parallel QA).

## 2026-06-28 — EN/KO S3: UI i18n — all player-facing screens localized (code, make check green)
- Status: Completed for every player-facing screen. Lightweight self-built i18n (no i18next); ko default preserved, English opt-in only.
- Changed: NEW `src/mythos_ui/src/i18n/` — `strings.ko.ts` (key SSOT, ~210 keys) + `strings.en.ts` (typed `Record<StringKey,string>` → missing/extra key is a tsc error = build-time key parity), `lang.ts` (`resolveInitialLang`/`useLang`/`LangContext`), `LangProvider.tsx` (split for clean Fast Refresh). `main.tsx` wraps root; HeaderBar KO↔EN toggle (localStorage). `t()` conversion across **App (status/tab-notices/epiphany banners), Onboarding, Header, TabNav, ChoicePanel, BootIntro, GameAside (route/status/log), StoryPanel (combat-result/tactical-legend/tile/objective/history/ended + bilingual stat-monologue parser), CombatControls, CombatLog, CombatRoster, RouteNarrative, CharacterPanel, SaveHistoryPanel, CharacterTabPanel, CutsceneGallery, CodexPanel, SkillTreePanel, ProgressDashboard**. English is opt-in (`?lang=en`/localStorage); navigator auto-detect deferred to the flip.
- Verified: `make check` green — eslint (0 warnings), tsc/vite, **549 tests OK**. Bundle ships both KO+EN; `?lang=en` renders English UI, toggle switches live.
- Deferred (intentional, non-player or data): DevConsolePanel (dev-only tab), GameAside `_map` legacy branch (slated for removal), BootIntro/IntroPanel scenario `ui_copy` fallbacks (need the API to serve localized scenario — ties to S1-remaining "API language selector"), HeaderBar brand title.
- Next: S2b note-builder framing tail + S4 backfill + S5 model eval + glass-library; then the default flip (UI navigator-detect + backend `en` + API serving localized scenario data).

## 2026-06-28 — EN/KO S2b (golden-path): additive scenario-prose overlay (code, make check green)
- Status: Completed for the neo-seoul golden path. Chose an **additive overlay** over the plan's ContextVar sidecar — the §7.7 highest-risk core-loader refactor is unwise while a human runs KO live-QA against `load_scenario`. Zero KO-path risk (overlay absent → `{}` → KO source as-is).
- Changed: `scenario.py` `load_scenario_i18n(scenario_id, language)` — loads `resources/<scenario>/i18n/<lang>.json`, `{}` when absent. NEW `resources/neo-seoul/i18n/en.json` — brief + session_intro (title/body/objective/cinematic_shots×3) + main_arcs(6) + side_arcs(6) + endings(4) + npc_agendas(5), all English. `scenario_context.py` — `SCENARIO_BRIEF` note + `_opening_continuity_notes` (overlay prose + English framing via NEW `_opening_continuity_lines_en`; KO split to `_opening_continuity_lines_ko`, byte-identical) + `_scenario_structure_notes` (NEW `_localized_named_items` index-merge, per-item KO fallback never drops; npc keys localized). `test_language_plumbing.py` +8.
- Verified: `make check` green — **549 tests OK** (skipped 2). EN: brief/opening-continuity/arcs/endings/npc all render English; EN fallback smoke English; KO byte-unchanged.
- Next: S2b remaining — other note-builders' KO framing (`_route_director_notes` etc.) + bible snippets + glass-library overlay. Output already English (LANGUAGE_RULE_EN) → these are bilingual *instructions* only, not output language. Then default flip + S3 UI i18n.

## 2026-06-28 — EN/KO S2a: English directive layer + code-rule language switch (code, make check green)
- Status: Completed. With `language="en"`, the scripted opening + fallback + naming/stat/encounter directives and the hard output-language rule now render English; ko unchanged (default).
- Changed: `scenario_directives.load_scenario_directives(scenario_id, language="ko")` — `_directive_path` prefers `<name>.<lang>.md`, falls back to `<name>.md` (graceful, no renames), cache key includes lang, companions deduped by base stem. `scenario_context.py` — `LANGUAGE_RULE_EN`/`CINEMATIC_CLARITY_RULE_EN` + `_language_rule`/`_cinematic_clarity_rule` selectors (the KO `LANGUAGE_RULE` hard-forced Korean output — the critical switch); narrative caller threads `language` into the loader + both rules. NEW neo-seoul `directives/{opening,fallback,naming,stat_voices,encounters}.en.md` — structural parity with KO (flags `met_se_rin`/`refused_se_rin`, `start_combat: patrol_ambush`, `{archetype}`/`{player_action}`/`{shot_*}`/stat placeholders, choice suffix/intent all identical). `test_language_plumbing.py` +5.
- Verified: `make check` green — ruff/eslint, mypy 126 files, tsc/vite, **543 tests OK** (skipped 2). Loader parity asserted KO↔EN; unlocalized glass-library falls back to KO directives.
- Next: **S2b** — scenario.json/bible.json prose SIDECAR (`i18n/{en,ko}/<section>.json` + lang ContextVar loader merge). Still-Korean in EN: `_scenario_structure_notes` (arcs/endings), cinematic_shots/session_intro, `scenario.brief`, bible snippets. §7.7 highest-risk (core loader, 30 call sites) — proceed carefully alongside the parallel KO live-QA.

## 2026-06-28 — EN/KO S1: English narrative generation surface (code, make check green)
- Status: Completed. Selecting `language="en"` now produces an all-English generation path (prompts + contract + deterministic fallback); behavior-preserving for `ko` (default unchanged).
- Changed: `prompts.py` — `STORY_SYSTEM_PROMPT_EN` (dual-model storyteller), `DEFAULT_SYSTEM_PROMPT_EN` (legacy single-model), `JSON_CONTRACT_EN` (English field hints, same shape/order), `OPENING_FIRST_SCENE_INSTRUCTION_EN`; language seams `_story_system_prompt`/`_system_prompt`/`_json_contract`/`_opening_first_scene_instruction` (+`_next_scene_instruction` symmetry) all branch on `context.language=="en"`. `fallbacks.py` — `DEFAULT_FALLBACK_EN` (C-17 opening in English) + `DEFAULT_FALLBACK_BY_LANG` + `default_fallback(language)`; `DEFAULT_FALLBACK` kept exported (parser repair stays ko, language-agnostic). `director._fallback_payload` selects fallback by language + `_FALLBACK_CONNECTIVES` (ko keeps exact pre-S1 "당신은 …"/"행동이 적용되었습니다"). `smoke.py` `--language` + `make narrative-smoke-fallback-en`. `tests/test_language_plumbing.py` +9 (EN prompt/contract/opening selection + EN fallback renders English no-Hangul + ko still Korean).
- Verified: `make check` green — ruff/eslint, mypy 126 files, tsc/vite build, **538 tests OK** (skipped 2). `make narrative-smoke-fallback-en` emits fully-English C-17 scene; `make narrative-smoke-fallback` (ko) byte-unchanged.
- Decision: **default stays `ko`.** The default→`en` flip is deferred to S2 (it requires English authored content — directives/scenario/UI — or the golden path renders English prompts over Korean authored notes/UI). Flipping now would also break the human's parallel neo-seoul KO live-QA. The flip is a one-liner once S2 lands.
- Next: **S2 golden-path 저작물 EN(B)** — `opening.en.md`/`fallback.en.md` + loader `*.{lang}.md` 분기 + golden-path scenario/bible 산문 EN (sidecar i18n). Then default flip + S3 UI i18n.

## 2026-06-28 — App.tsx decomposition formally `[blocked]` (2nd blocker, no human resolution); claude lane drained → DONE
- Status: No code change. Graceful overnight stop. The single actionable claude-lane `[auto]` item (App.tsx decomposition) hit the same unresolved blocker a **second** consecutive iteration, so per the overnight twice-blocked rule it is now tagged `[blocked]` and the lane has no remaining candidate.
- Evidence: tree clean, HEAD still `fd2730d` (the Blocker #1 doc commit) — nothing changed since Blocker #1. No human resolution of the question Blocker #1 posed ("confirm paused, or name a slice with a clean boundary"). Stronger signal: the working branch is now `feat/en-ko-s0-language-plumbing` — a human created a new branch for EN/KO localization work, indicating focus moved **off** the App.tsx decomposition track. Re-attempting `useViewModels` would undo a deliberate human revert (`4d88b80`); picking a different slice was already declined in Blocker #1 (unknown revert reason → high re-churn risk on a file the human just intervened on).
- Action: NEXT_PLAN App.tsx line tagged `[blocked]` (my-lane line only, no other lines touched). No other claude-lane `[auto]` candidate exists (QA-seed content-integrity = `[x]` done; `_map` removal = already `[blocked]`; remaining seeds are `[auto:codex]`). `scripts/overnight/DONE` written (reason: all-blocked). Remove the `[blocked]` tag after a human names a slice or closes the track.
- Verified: no source touched; only `docs/NEXT_PLAN.md` (+`[blocked]` tag) + `docs/PROGRESS_LOG.md` (this entry) + `scripts/overnight/DONE`.

## 2026-06-28 — BLOCKER: App.tsx decomposition next-candidate (useViewModels) was human-reverted; track paused pending direction
- Status: Blocked (no code change this iteration). Conservative overnight hold per CORE_MANDATES (`when in doubt, don't`).
- Evidence: the named next-candidate slice — the codex/dev/tab view-model `useMemo` cluster — was implemented as `hooks/useViewModels.ts` (slice 14, commit `d7b3b35`, **`make check` green / 529 tests**), then **reverted 8 seconds later** by `men1692` (commit `4d88b80`). The 8s gap is far too short for AGY browser live-QA (minutes) → reads as a deliberate human rejection, not a gate failure. The revert also restored `NEXT_PLAN.md`/`PROGRESS_LOG.md`, so the "next candidate" note again points at the just-rejected cluster.
- Why not re-attempt: re-creating `useViewModels` verbatim would directly undo a deliberate human revert (highest-risk action for an unattended agent). Plausible revert reason (unconfirmed): the extraction required a **12-field props object** (`memoryOverview/finalizedSnapshot/currentScenario/scenarios/selectedScenarioId/runsHistory/dismissedEpiphany/setDismissedEpiphany/showInGameNotice/skillNotice/setActiveTab/loadCodex`) — coupling moved, not reduced → net-negative extraction. Picking a *different* slice was also declined: the revert reason is unknown, so a new slice risks re-churning a file the human just intervened on (could be a wholesale pause of the App.tsx track).
- Needs human: confirm whether the App.tsx decomposition track is paused, OR name a different next slice with a clean (few-prop) boundary. This is the **only** actionable claude-lane `[auto]` item, so the claude lane is effectively idle until then.
- Verified: tree clean, HEAD=`4d88b80` (the revert), `useViewModels.ts` absent, App.tsx=696 lines, `viewModels.ts` builders intact. No code touched.

## 2026-06-28 — App.tsx decomposition slices 9-13 (overnight, make check green)
- Completed (behavior-preserving hook extractions, App.tsx 876→696): slice 9 `useCombatRest`, 10 `useSessionControls`(+shared `archetypes.ts`), 11 `useSnapshotReceiver`, 12 `useNarrativeStream`, 13 `useKeyboardChoice`. Each `make check` green + AGY live-QA PASS. Per-slice detail: NEXT_PLAN line 119 + git. (Track later `[blocked]` at slice 14 — see the 2026-06-28 blocker entries above.)

## 2026-06-28 — App.tsx decomposition slice 8: extract useDataLoaders hook (make check green)
- Status: Completed. Behavior-preserving extraction; `App.tsx` 927→876 lines.
- Changed: NEW `hooks/useDataLoaders.ts` (126 lines) owns the read-side API loaders moved verbatim out of `App.tsx` — `loadSlotsAndRuns` (save slots + run history + memory overview), `loadCodex` (memory overview + skill tree), `loadSkillTree`, and the `handleLearnSkill` insight-investment mutation. All cross-cutting state + setters passed as props (matches the useSessionLifecycle/useGameSocket props-object pattern). Handlers stay plain (non-memoized) functions, identical to prior in-`App` form. `loadSkillTree` is internal to the hook (only `loadCodex` calls it) so it's not re-exported to `App`. Removed 5 now-unused api imports from `App.tsx` (apiGetMemory/apiGetSlots/apiGetRuns/apiGetSkillTree/apiLearnSkill).
- Verified: `make check` green — ruff/eslint, mypy 126 files, tsc/vite build, **529 tests OK** (skipped 2). Bundle rebuilt (`static/app.js`).
- Blockers: none.
- Next: App.tsx decomposition — next candidate = combat-REST cluster (handleCombatAction/continueAfterCombat/appendCombatLog/handleEquip).

## 2026-06-28 — App.tsx decomposition slice 7: extract useSessionLifecycle hook (make check green)
- Status: Completed. Behavior-preserving extraction; `App.tsx` 1069→927 lines.
- Changed: NEW `hooks/useSessionLifecycle.ts` (288 lines) owns the session lifecycle entry points moved verbatim out of `App.tsx` — `handleStartGame` (fresh run), `handleSimulateCombat` (fallback combat sandbox), `handleResumeGame` (resume existing loop), plus the shared `saveSessionMetadata` resume-token writer. All cross-cutting state setters + hook helpers are passed as props (matches the useGameSocket/useSceneVisuals props-object pattern). Handlers stay plain (non-memoized) functions, identical to prior in-`App` form. Exported `NarrativeHistoryItem` type from `App.tsx` for the hook. Removed 5 now-unused api imports from `App.tsx` (apiConnect/apiActive/apiBegin/apiCombatBegin/apiGetLoopScenes).
- Verified: `make check` green — ruff/eslint, mypy 126 files, tsc/vite build, **529 tests OK** (skipped 2). Bundle rebuilt (`static/app.js`).
- Blockers: none.
- Next: App.tsx decomposition — next candidates = remaining handlers (handleCombatAction/continueAfterCombat combat-REST cluster, or loadSlotsAndRuns/loadCodex/loadSkillTree/handleLearnSkill data-load cluster).

## 2026-06-28 — QA seed: archetype/character stable-id content-integrity invariants (make check green)
- Status: Completed. Locks the 2026-06-27 join-key id migration against silent regressions.
- Changed: NEW `ArchetypeAndCharacterIdIntegrityTest` in `tests/test_content_integrity.py` (3 tests, globs `resources/*/scenario.json`): (C1) `archetypes[].id` unique & non-empty + `combat.archetype_loadout`/`archetype_base_skills` key sets each **exactly equal** the archetype id set (catches missing key = archetype with no loadout/base-skills, orphan key = pre-migration Korean-name dead data) — both scenarios; (C4) `characters[].id` unique & non-empty (neo-seoul; glass-library declares none → inert). Each has a guard-the-guard (`scanned`/`checked` > 0) so it can't go vacuously green.
- C2 was **already covered**: `test_skill_icons_exist` (in `ScenarioImageReferenceIntegrityTest`) already enforces `combat.skills[].id` → `skills/<id>.png` (+PNG magic) for all scenarios; the `test_assets.py` skill-icon exclusion is already gone. No change needed for C2.
- Verified: `make check` green — ruff/eslint, mypy 126 files, tsc/vite build, **529 tests OK** (526→529, skipped 2).
- Blockers: none.
- Next: EN/KO S1 영어 생성 — EN system prompt + `JSON_CONTRACT_EN` + `DEFAULT_FALLBACK_BY_LANG["en"]`, flip default to `en`.

## 2026-06-27 — EN/KO S0: language plumbing (code, make check green)
- Completed: target output language threads end-to-end (`NarrativeContext.language`/`RuntimeOptions.language` → `build_runtime_narrative_context` → `_story_system_prompt` seam); behavior-preserving (default `ko`). `tests/test_language_plumbing.py` (6). Detail: plan §3 + git.

## 2026-06-27 — EN/KO prerequisite: stable archetype-id migration (code, make check green)
- Completed: combat/progression joins moved off Korean display names to stable `id`s (scenario.json rekey + `resolve_archetype_id`/`_ARCHETYPE_ALIASES` read-time normalize, no DB migration) — unblocks EN/KO bulk i18n. `tests/test_archetype_id_migration.py` (8). Detail: plan §7.5 + git.


