# Progress Log

Last updated: 2026-07-10

This file keeps **only recent incremental summaries within the 120-line budget**. Older 2026-07 entries are in
`bin/docs/archive/progress-2026-07.md`; the 2026-06 detailed log in `bin/docs/archive/progress-2026-06.md`, 2026-05 in `bin/docs/archive/progress-2026-05.md`.

## 2026-07-10 (live session #3, claude lane) — companion-appearance sweep: cutscene present-gate + combat-callback leak; DEPLOY 00038-v8d
- Status: Done + DEPLOYED (`mythos-api-00038-v8d`, smoke health/root 200). `make check` **969** green. Closes the companion-appearance thread after a full audit of every surface vector.
- **Cutscene present-gate** (`10efaeb`): owner hit a 3rd Se-rin vector — the `SERIN_FIRST_LIGHT` cutscene ("깜빡이는 신뢰") unlocks on affection>=2 with NO flag gate, and affection carries across loops → it fired in a lin_yue loop (her art over a Lin-yue scene). `next_unseen_cutscene` gains a `present_companions` filter; the in-game caller computes it from the SAME `unlock_flags∩flags` set as combat spawn, unifying "appears only when present this loop." Cross-loop gallery (`evaluate_unlocked_cutscenes`) untouched.
- **Companion-appearance full audit** + combat-callback fix (`0f52e16`): swept every vector — combat/portrait/cutscene/party (all fixed), story-bible (flag-gated, no affection gate → clean), curated images (only the opening anchor is Se-rin, all 6 variants override → clean). Found + fixed a soft narration leak: the post-combat callback directive hardcoded "동료(예: 세린)의 반응" → now present-aware ("함께 싸운 동료가 있었다면", + "don't feature a companion not joined this loop"). NARRATIVE-ECHOES "만난 인물" list left as-is (owner-endorsed carry channel, opening-suppressed, framed as past memory).
- Verified: `make check` 969 (+3 cutscene present-gate tests). Blockers: none. Next: owner clean-loop retest (new player/key → Se-rin absent everywhere until re-met) · `! git push` · Se-rin tester-key stale loop = new loop or DB cleanup.

## 2026-07-10 (live session #2, claude lane) — stat-voice redesign + companion guardrail + Se-rin combat-spawn ROOT FIX; DEPLOY 00035→00037
- Status: Done + DEPLOYED (00035-dfm→00037-cj2, smoke health/root 200). `make check` **967** green. Owner live-confirmed stat voices apply + opening shows no Se-rin; combat-spawn root found + fixed (retest pending on a new loop).
- Stat voices (`51b2264`, owner req): now **choice-gated** — inner-monologue fires only when this turn's choices gate that stat (the (스탯) tag); player max/min only colors competent vs inept. Distinct **persona cast** (성별·성격·말투): 근력=거친 남성 · 지능=중성 AI · 매력=여성 누나 · 민첩=다급 소년 · 관측=냉철 노관찰자. KO/EN directive + code DEFAULT_STAT_VOICES(_EN), byte-parity kept.
- Companion guardrail (`9bfce0d`): `_strip_carried_companion_presence` drops carried met_/trusted_/refused_/ally_<companion> flags + party at every loop start after the first (prior-loop companion must re-introduce via meet-arc; only affection carries). Defensive — fresh loops already clean.
- **Se-rin combat-spawn ROOT FIX** (`31bdceb`, resolves the mid-loop vector the prior entry left open): combat unlocks on `member OR unlock_flags∩flags`; the layer-0 opening anchor hardcodes `trusted_se_rin` and `effect` isn't variant-overridable → han/kai loops inherited it (route_runtime applies mid-loop, bypassing the model-only validator clamp) → she spawned though absent from the opening. `_apply_anchor_variant` now strips se_rin contact flags from non-default variant anchors (default keeps them; her meet-arc side anchor untouched; static+dynamic covered).
- Verified: `make check` 967 (+regression: stat-voice note gating, guardrail strip ×2, variant-anchor flag-strip ×6). Blockers: none. Next: owner live retest (new variant loop → no Se-rin in combat) · `! git push` · Se-rin tester-key stale loop = new loop or 1-time DB cleanup.

## 2026-07-10 (live session, claude lane) — Se-rin combat-spawn is NOT a flag-carry bug (reproduced) + tae_o/han portrait-flash fix + DEPLOY 00034-8xj
- Status: Done + **DEPLOYED `mythos-api-00034-8xj`** (project-ec7809f7, smoke `/api/v1/health`+root 200). **Owner live-confirmed** Se-rin no longer appears in a fresh han loop (Han spawns correctly). `make check` **964** green.
- **Se-rin (session #7 OPEN item) — flag-carry RULED OUT; mid-loop reintroduction is a SEPARATE OPEN vector.** Reproduced (`scratch/repro_serin.py`): a fresh loop-2+ variant under a met+trusted(rel=5) meta carries **ZERO contact flags + empty `_party`** — **no met_/trusted_ flag-carry into new-loop initial state** (`initial_state` = `_initial_loop_scores` `{}` + `apply_meta_progression_to_state` writing neither; engine auto-grant `engine.py:66` is default-opening-only; validator clamps model contact flags in turn≤3). So the owner-decided "stop carrying flags" fix has **no target** (not implemented; would break the tutorial seed). One han loop confirmed clean. **HOWEVER owner then reported Se-rin STILL appearing mid-loop in a variant** (screenshot: narration "…정세린이다", CHARACTER=물거미) → the model reintroduces her during play (story-bible/echo leak or a legit Se-rin node), a vector the repro (initial-state-only) did not cover. OPEN — next: inspect the affected loop state (which turn her flag/presence appears; model `world_delta` vs route node) before any fix.
- **"이번 막" objective — not a bug**: two-tier by design (act-level `player_goal` = constant across an act; node-level "현재 목표" = dynamic per variant/node).
- **tae_o portrait flash — FIXED**: `detectSceneCharacter` shows a portrait on any `characters[].keyword` hit in scene text; tae_o's `사냥개`(hound) matched IX-pursuit ambient prose on the Han route (han's `해커` had the same collision). Dropped both from detection `keywords` (aliases keep them for display). Reviewed all 7 chars (generic-noun judgment + corpus scan) — no other offenders (물거미/rx-09/신호 파괴자/잔향 가공사 are unique coined handles, self-referential only). New source-lock `CharacterDetectionKeywordIntegrityTest` bars ambient common nouns (사냥개/해커/순찰대/드론/시민).
- Verified: `make check` 964 green (+1 regression test); owner live confirm (Han loop). Commit `6e5f13f`. DEPLOYED 00034-8xj (`gcloud run deploy` explicit target).
- Blockers: none. Next: `! git push` (ahead of origin, hard-block → user); live sign-off (tae_o flash real-device confirm + mobile real-device pass).

## 2026-07-09 (live session #7, claude lane) — su_ah "핑" clarity + board-legend opacity; Se-rin combat-spawn diagnosed (design decided, OPEN)
- Status: Two fixes DONE + committed (`d175522`, `make check` 963 green, undeployed); one bug diagnosed with a decided fix, NOT yet implemented (next session).
- Changed (committed): **su_ah "핑" reads as a named actor** in the ghost-ping choices → `opening_su_ah.md`/`.en.md` add a clarity directive (핑 = handmade detection signal, never personify) + render example choices as "핑 신호 / ping signal". **Board-legend overlap** — `.tactical-legend-popup` inherited the base 50%-translucent bg, so STATUS/roster text bled through and looked like overlap → opaque bg + higher z-index.
- Diagnosed (OPEN — owner-decided fix): on production (invite `mythos-d1b0da3e` = `player_0353f4b966705d`, runs=3) han/su_ah variant loops correctly have `_party.members=[]`, BUT Se-rin still spawns in COMBAT because a carried `trusted_se_rin` flag unlocks her ally entry (`combat_service.py:345` `unlock_flags ∩ flags`). i.e. #4/#5/#6 fixed opening/party/starting-party; the last vector is **per-loop met/trust flags carrying across loops** → she auto-fights in a han loop. Owner decision: a han loop should feature Han (via variant meet-arc), Se-rin only if re-met at a mid node; **only the affection BONUS carries (already in `meta_progression.relationships`)**, not the met_/trusted_ flags — "faint familiarity" is the NARRATIVE-ECHOES layer. Fix = don't carry met_/trusted_ per-loop companion flags into a new loop's initial state.
- Verified: `make check` 963 green (ping+legend). Se-rin combat-spawn confirmed via Neon read-only (trusted_se_rin present on the owner's han loops). Deploy hygiene: `make api-cloud` now points at prod Neon (`22fb888`).
- Blockers: none. Next: implement the flag-carry fix (interactive — gameplay logic + feel), then redeploy (ping/legend + flag fix). Overnight `[auto]` backlog is drained (thin candidates only).

## 2026-07-09 (live session #6, claude lane) — fix TRUE ROOT of owner's Se-rin-in-variant-opening: loop-counter mismatch
- Status: Done; `make check` **963** green. Correcting an over-reach: #5 (ally-writeback) is a valid separate fix but was NOT the root of the OWNER's Se-rin issue. Read-only Neon inspect of the owner (`player_1d34fcf029cd64`, 24 loops) found the real cause; a DB "cleanup" of Se-rin would have wrongly deleted her.
- Root cause: **two loop counters disagreed.** `_select_opening_variant` keys on `len(loops)` (owner=24 → serves a loop-2+ **variant** opening), but the tutorial "Se-rin is the guaranteed starting party" gate in `_prepare_start_loop` keyed on `runs_completed == 0`. The owner abandons loops without archiving, so `runs_completed` is stuck at **0** across all 24 loops → every loop was treated as the tutorial loop and **re-seeded Se-rin into the starting party** — even the kai variant loop (verified: `var=kai`, `_party.members=['se_rin']` at turn 0 / 0 combats, so she was there at loop START, not via combat writeback). Variant opening + tutorial Se-rin = the leak.
- Changed: `session.py` gate `runs_completed == 0` → **`len(loops) == 0`** (the player's first loop; same counter `_select_opening_variant` uses). A many-loops/0-runs tester no longer gets Se-rin auto-seeded into a loop-2+ variant. First loop unchanged (tutorial Se-rin + default opening). One line + comment.
- Verified: `make check` 963 green; new `test_se_rin_starting_party_only_on_first_loop_not_replays` (two `start_loop`s on a fresh player: loop 1 has Se-rin in party, loop 2 does not — with runs_completed still 0). Neon read-only confirmed the mechanism (not speculation). DB cleanup NOT run (was the wrong call — Se-rin is the intended companion; the counter fix is correct). `make api-cloud` now points at the prod Neon DB (`22fb888`) so the DEV console sees beta data.
- Blockers: none. Not deployed. #4 solo_opening (deployed 00032) remains defense-in-depth; #5 writeback fix still valid for OTHER story allies (Kai/Lin-yue).
