# Progress Log

Last updated: 2026-06-19

This file keeps **only the latest incremental summaries** (latest 5 items). The long 2026-06 detailed log (including per-stage route-node session detail) is in
`bin/docs/archive/progress-2026-06.md`, the 2026-05 log in `bin/docs/archive/progress-2026-05.md`.

## 2026-06-20 (b) — Overnight `[auto:claude]`: heal/support skills can target an ally in range
- Status: overnight `[auto:claude]` live-finding fix. `_player_skill` applied `heal`/`defense_bonus` only to the **caster**, ignoring `action.target_id` — so `patch_protocol` (range-3 heal) / `nanoshield_projector` (range-3 shield) / `covering_noise` (range-4) could never reach an ally despite carrying explicit `range`. Diagnose-first: confirmed root cause at `engine.py` support branch, verified the three scenario skills' `role`/`range`/`effect` shapes before touching code.
- Changed: ① `models.py` NEW `CombatState.friendlies_of()` (mirror of `hostiles_of`: same-side living units incl. self). ② `engine.py` NEW `_friendly_target(state, player, target_id, reach)` — resolves a living friendly in range, **falls back to the caster** when target is missing/hostile/dead/out-of-range (preserves prior self-only default). `_player_skill` heal/defense_bonus now apply to the resolved support target (+ log names the target, not always the caster). ③ `available_actions` exposes `friendly_targets[]` (id/name/distance/hp/max_hp/is_self) so the UI can direct support skills. Additive dict key — no consumer break.
- Verified: `make check` green — ruff + eslint + mypy (121 files) + frontend build + **461 tests OK** (skipped 2, +4). New `tests/test_combat_engine.py`: ally-heal-in-range, ally-shield-in-range, out-of-range→self fallback, `available_actions` friendly-target exposure. Fault-injection RED proven (stash the engine fix → 2 failures + 1 error: ally untouched + missing `friendly_targets` key).
- Blockers: none.
- Next: `[auto:agy]` 6 skill-icon drafts still gate the `[blocked]` skill/icon-integrity item. claude `[auto]` lane near drained — remaining items are mostly `[manual]` human-feel QA.

## 2026-06-20 — Overnight `[auto:claude]`: skill/ally data-closure invariant batch (seed ②③ remaining slice)
- Status: overnight `[auto:claude]` seed (2026-06-20) — extend `SkillDataIntegrityTest` with the three silent skill-data failure modes not yet locked. Measured first that seed ① (`allies[].skills`⊆skills), ④ (`skill.epiphany`→`combat.epiphanies`), and ②-reference (`requires` resolve) were **already** enforced by existing tests, so this batch closes only the genuinely-open gaps (no redundant re-implementation). Test-only; src untouched.
- Changed: `tests/test_content_integrity.py` `SkillDataIntegrityTest` — `setUp` now derives `item_ids` + `tier_by_id` (missing/non-int tier → 0) + 3 new closures: ① `test_skill_cost_items_resolve` (③ `cost.item`⊆`combat.items`; the lone link `patch_protocol`→`nanopatch`), ② `test_skill_requires_are_acyclic` (② DFS cycle-detect on the `requires` graph — a transitive self-require is unlockable forever), ③ `test_skill_requires_are_tier_monotonic` (② prerequisite tier ≤ gated skill tier; **non-strict** — real tree chains tier-1 `nanoshield_projector`←`covering_noise` / `system_intrusion`←`emp_pulse`, so strict would falsely RED). Class docstring extended to note which seed properties pre-existed.
- Verified: `make check` green — check-skills + check-doc-budget + ruff + eslint + mypy (121 files) + frontend build + **457 tests OK** (skipped 2, +3). Fault-injection RED proven for all three new closures (ghost `cost.item`, a↔b `requires` cycle, tier-2 prereq under a tier-0 skill) via throwaway `SkillDataIntegrityTest` subclasses.
- Blockers: none.
- Next: remaining `[auto:claude]` = heal/support ally-targeting (`engine.py:533`). `[auto:agy]` 6 skill-icon drafts still gate the `[blocked]` skill/icon-integrity item.

## 2026-06-19 (d) — Overnight 루프 `/goal` 통합: 외부 재게이트(phantom 검출) + opt-in 수렴 (repo + plugin)
- Status: Claude Code 내장 `/goal`(v2.1.139+)을 자작 overnight 러너 **내부**에 활용. 먼저 `-p` 호환성 실측(Probe B: 러너 플래그 `--permission-mode acceptEdits --settings … --output-format json` 와 호환, `permission_denials:[]`, runner-classifiable JSON) 후 설계(`docs/plans/2026-06-19-goal-in-overnight-loop.md`)대로 두 워크스트림 구현. MythOS `scripts/overnight/run.sh` + 플러그인 `claude-overnight-harness/templates/scripts/overnight/run.sh` 동시 반영.
- Changed: ① **WS-α (`OVERNIGHT_VERIFY_GATE=1` 기본)**: 새 커밋마다 bash가 `$GATE_CMD`를 **외부 재실행** → RED면 phantom-success로 `git revert`(폴백 `reset --hard`)+`notify`+`consec_fail++`. `status.tsv`에 `gate_exit`/`commit_verified` 2열 추가(`status.sh`는 f5/6/7만 읽어 하위호환). 엔진 무관. ② **WS-β (`OVERNIGHT_GOAL=1` opt-in, `GOAL_MAX_TURNS=12`)**: claude 레인 프롬프트 앞에 `/goal` 디렉티브 주입(green+커밋까지 수렴; soft 바운드라 ITER_TIMEOUT 하드 실링 유지). codex/agy 불변. ③ 플러그인 CHANGELOG `Unreleased` 기록.
- Verified: 실제 run.sh를 격리 git 저장소 + 가짜 claude(에이전트 대역)로 구동(토큰 0). **phantom 검출 0→100%**: `VERIFY=1`(신규)은 RED 커밋 검출·revert→브랜치 GREEN 복구(`phantom` 행 `gate_exit=1`/`commit_verified=0`); `VERIFY=0`(레거시)은 미검출→RED 커밋 잔존. WS-β 주입 확인(claude 수신 프롬프트 첫 줄=`/goal`). `make check-doc-budget` OK(NEXT_PLAN 119/120), `bash -n` 양쪽 OK.
- Blockers: 플러그인 clone(`men16922/claude-overnight-harness`)이 **v0.2.0 + `diagnose` 스킬 부재** — 본 PROGRESS 2026-06-19(b)의 "diagnose v0.3.0 플러그인 푸시" 기록과 **불일치**(이 origin/main 미반영). 버전 번호 충돌 회피 위해 CHANGELOG는 `Unreleased`로만 기록 — **버전 bump/commit/push는 사용자 재조정**(agent push 차단).
- Next: 플러그인 버전 reconcile 후 commit/push(사용자). 선택: WS5 ② 아침 digest 자동화 / ④ iter-output 캡.

## 2026-06-19 (c) — Token/context optimization: operational docs → English + doc-budget gate + Quarkify promotion
- Status: Converted the agent-only operational doc layer to English and hardened doc growth, per `docs/DECISIONS.md` 2026-06-19. Scope = all four levers (CLAUDE.md trim, doc-budget gate, Quarkify promotion, harness diet). Branch `feat/narrative-doc-bgm-logging`.
- Changed: ① English-ified `CLAUDE.md` (142→125 lines, prose compressed, deep package detail deferred to `DESIGN.md`), the `/sync` entry docs (`AGENT_BRIEF`/`STATUS`/`NEXT_PLAN`/`PROGRESS_LOG`; NEXT_PLAN trimmed 125→119), `harness/CORE_MANDATES.md`/`CONTEXT_BRIDGE.md`, `docs/engineering/**` (5 bibles + 5 `mythos/` interpretations), `.claude/skills/*/SKILL.md` bodies (6 skills; Korean triggers kept in frontmatter), `scripts/overnight/PROMPT*.md` (4 files; forbidden-action list + `[리뷰 대상]`/`[출력]` review markers preserved verbatim), `DOCS_POLICY.md`/`README.md`/`engineering/README.md`. ② NEW `harness/check-doc-budget.sh` wired into `make check` (caps: AGENT_BRIEF ≤60, STATUS/NEXT_PLAN/PROGRESS ≤120). ③ Quarkify promoted to default broad-search in `CORE_MANDATES §5` + `CLAUDE.md` + overnight PROMPT (grep reserved for rare literals; still not gated). Skill mirrors re-projected via `harness/sync-skills.sh`. ④ NEW `docs/guides/token-optimization-port.md` — concise self-contained recipe (English, 80 lines) for applying the same optimization to other overnight-harness repos (sibling of `quarkify-port.md`).
- Verified: `make check` green **454 tests** (skipped 2), mypy 121 files clean, eslint + frontend build clean, `check-skills` drift OK, `check-doc-budget` OK. Token before/after (tiktoken o200k): converted set 56,898 → 47,435 (**-16.6%**, -9,463 tokens); per-session fixed-cost set 22,707 → 18,990 (-16.4%). Zero Hangul leak in converted op-docs (triggers excepted).
- Blockers: none. Plugin repo (`men16922/claude-overnight-harness`) PROMPT/skill mirrors = separate user-pushed follow-up.
- Next: optional Phase-2 conversion of on-demand agent-reference docs (`DESIGN`/`GAMEPLAY`/`API`/`COMPLETED_SUMMARY`/`DECISIONS`); then resume narrative QA #2/#5 live feel.

## 2026-06-19 (b) — Harness diagnose-first + narrative architecture doc + BGM/log fixes (live QA follow-up)
- Status: FF-merged & pushed the morning batch (narrative QA 4 items, cutscene backend, quarkify, invariant P/Q) to main (`98d5f5c..44fd4e7`). Then ① harness `/diagnose` diagnose-first (derived from usage report, plugin v0.3.0 alongside), ② live QA follow-up (narrative architecture doc, BGM, api logs). Narrative QA **#1 opening & #3 IX live PASS**, #2 ending & #5 combat awaiting feel.
- Changed: ① `harness/CORE_MANDATES.md` "Diagnose before any fix" + NEW `.claude/skills/diagnose` (4 mirrors) + overnight `PROMPT.md` gate-phase classification (merged to main). Plugin repo (`men16922/claude-overnight-harness`) `skills/diagnose`·PROMPT·CHANGELOG **v0.3.0** pushed. ② NEW `docs/NARRATIVE_ARCHITECTURE.md` (narrative end-to-end map). ③ BGM: `useAudio.ts` preserves toggled track (no snapshot-path recompute) + `App.tsx` IntroPanel `bgmEnabled` gate (fixes desync where sound played with button OFF). ④ api logs: `__main__.py` `configure_logging()` call + uvicorn INFO fixed (separates MYTHOS_LOG_LEVEL=DEBUG token-frame spam), `session._commit_scene` emits per-turn narration + state (stability/tension/flags) + choices at INFO. ⑤ `opening.md` turn0~1 manual choices only (live: no active choices before standing up).
- Verified: `make check` green **454** (repeated). api restart measured — structured JSON logs (final narration + latency 16s) confirmed, 0 token frames. BGM/logs human-confirmed.
- Blockers: none.
- Next: merge branch `feat/narrative-doc-bgm-logging` (human) + narrative QA #2 ending / #5 combat live feel + #4 map choices / #6 skill-tree RPG (separate track).

## 2026-06-19 — Neo-Seoul live QA narrative improvements (4 items) + (morning) cutscene branch merged to main, migration verified
- Status: ① (morning) `feat/companion-cutscene-unlock` (cutscene backend + Quarkify tooling + route/chapter invariant seeds P/Q) merged fast-forward to main & pushed (`8d2fc77..98d5f5c`) + migration 006/007 applied to real DB, persist→load round-trip PASS measured (infra up). tidy-docs (4 completed plans moved to bin/, references repointed, completed docs removed) + `docs/guides/quarkify-port.md` (porting guidance for other repos) included. ② User live QA feedback (`neo_seoul_live_qa.md`) yielded 4 narrative-first improvements (branch `feat/neo-seoul-narrative-qa-fixes`, uncommitted).
- Changed (QA 4 items): **#1** `opening.md` turn0~2 grounded-lock (can't stand on its own due to signal instability, no walking-to-se_rin/self-standing) + turn2 ambiguous phrase ("just lifting the body") removed + turn3 explicit first se_rin lift → restores the "collapsed, se_rin helps up" payoff. **#3** `opening.md` turn0 1-line non-identification/erasure awareness + turn3 se_rin line ("IX erases signals outside the baseline") to inject the IX-threat rationale narratively. **#2** `session.py` `_ending_narration_text` (authored `endings[].narration` / threshold narrative cause, numeric trace removed) → auto-exposed via `loop.state["ending_narration"]` → `StoryPanel.tsx` EndedPanel renders narrative instead of numbers + `types.ts`. **#5** `_commit_combat_turn` combat markers (`_last_combat_result`/`_last_combat_encounter`) + `scenario_context._combat_callback_note` injected into the full-render synopsis channel (1 turn post-combat, references aftermath/heat/ally-reaction in the first 1~2 sentences).
- Verified: `make check` EXIT=0 — 454 tests OK (skipped 2, +9: `test_ending_narration` 5 + `test_combat_callback` 4). #2/#5 plumbing unit tests locked in, opening.md loader parsing green. #1/#3 are LLM fidelity so remain for live QA.
- Blockers: none. Server restart needed for live QA (backend/directive/bundle changes).
- Next: user live QA confirms #1/#3 feel → commit & merge the result. Unimproved #4 map in-layer choice destinations / #6 skill-tree RPG node graph are a separate track (QA doc open `[!]`, data/wiring analysis done).

## 2026-06-16 — overnight review (seeds A-O) + P1 companion cutscene unlock system (gallery backend)
- Status: ① overnight 0616-0646 run (seeds A-O, 15 commits) morning review — `make check` re-measured green (417), 195 seed-related tests green, 3 features (L/M/N) diff read + fault-injection 3/3 RED (L double-count, N accumulation, A integrity) + live QA 4/4 (real neo-seoul data: route accumulation, replay idempotency, choice coexistence, cross-loop carry-over). Review PASS → **origin/main push done** (23 commits `6faf1e4..8d2fc77`). ② P1 cutscene unlock backend newly implemented (branch `feat/companion-cutscene-unlock`, commit `d8a8888`).
- Changed (P1): ① `scenario_directives.py` `CutsceneDirective` + `directives/companions/<name>.md` loader (parses `## CUT_ID (affection=N, flags=, image=)`). ② NEW `cutscenes.py` — `evaluate_unlocked_cutscenes` (deterministic: affection≥threshold ∧ flags⊆accumulated) + `cutscene_gallery` (unlocked=image+body / locked=stub) pure functions. ③ `progression.py` `MetaProgression.unlocked_cutscenes` cross-loop union (allies_met pattern) + final affection+flags evaluation at archive (`_unlocked_cutscenes_for_loop`) + grant note for new only + round-trip. ④ `options.py` `RunSummary.unlocked_cutscenes`. ⑤ `postgres_store.py` + NEW `migrations/007`. ⑥ `serializers.py` resolved `cutscene_gallery` in `memory_overview`. ⑦ NEW `se_rin.md` 2 cuts (threshold 2/4, placeholder portrait). ⑧ integrity `CutsceneIntegrityTest` (companion exists, flags producible, image exists, affection>0).
- Verified: `make check` EXIT=0 — mypy 118 files (+2)/frontend build + **443 tests OK** (skipped 2, +26). Fault-injection (ghost companion + bad image) RED confirmed. Live end-to-end: real `affection+flags→unlock→gallery (🔓/🔒)` chain works.
- Blockers: none. migration 006/007 not measured under no-DB `make check` (recommend `make db-migrate` before merge).
- Next: P1 remaining `[manual]` — frontend gallery view (payload ready) + P2 Se-rin dedicated art adoption. Branch merge is human-direct (agent main push blocked).

## 2026-06-16 — P0 affection runtime: serializer exposure contract regression test (seed O, `[auto:claude]`)
- Status: overnight `[auto:claude]` seed O — lock in whether the API serializer surfaces to the client the affection data generated by seeds L/M/N (route+choice+progression relationship accumulation). Measurement found **data exposure was already done** (snapshot `state` whole `to_json_dict`, memory overview `to_json_dict(overview)`), but **no test locked that contract** — added a regression guard against the same failure mode as relationship dead-data (seed L motive: a state-field filter silently dropping affection). green.
- Measurement: `snapshot_to_dict` exports `loop.state` wholesale via `to_json_dict`, so live `state["relationships"]` is already in payload `state.relationships` (`serializers.py:292`). `memory_overview_to_dict` is `to_json_dict(overview)` so `meta_progression["relationships"]` also round-trips (`serializers.py:27`, field filled by seed N). So the backend "display" contract is **already met** — remaining is the frontend gauge UI (player-facing visual feel → not unattended-verifiable, `[manual]`). The auto-slice is bounded to the regression test locking that contract (scope defense).
- Changed: `tests/test_api.py` `ApiRelationshipSerializerTest` 3 items + import bolstering (`snapshot_to_dict`/`memory_overview_to_dict`, `Choice`/`LoopPhase`/`LoopState`/`PlayerProfile`/`Scene`, `MemoryOverview`/`RuntimeSnapshot`). ① snapshot live exposure (`{se_rin:3,kai:1}` → `payload["state"]["relationships"]`) ② loop without affection doesn't create the key (gauge empty-state renderable, serializer doesn't fabricate the field) ③ memory overview cross-loop exposure (`meta_progression.relationships` round-trip). src unchanged (test-only).
- Verified: `make check` EXIT=0 — ruff/eslint/mypy (116 files)/frontend build + **417 tests OK** (skipped 2, +3).
- Blockers: none.
- Next: P0 affection runtime remaining is the frontend affection gauge UI (`[manual]` — visual QA needed). Remaining `[auto:claude]` seed near exhaustion (QA seed A-O done, 71=agy lane, 72=blocked). Real top priority is Neo-Seoul human QA (`[manual]`).

## 2026-06-16 — P0 affection runtime: progression cross-loop carry-over (seed N, `[auto:claude]`)
- Status: overnight `[auto:claude]` seed N — following seeds L/M (in-loop route+choice relationship accumulation), sum/carry each loop's final `loop.state["relationships"]` into meta progression at archive. Mirror the insight pattern (per-run tally += into previous total → DB persist → load at start_loop → inject into `state["meta_progression"]`) per-companion. The cross-loop foundation for P1 cutscene unlock gating. green.
- Measurement: the non-trivial point is **double-count avoidance**. In-loop live `state["relationships"]` already holds that loop's route+choice contribution summed, so extract it as the run summary and add it once to previous in `evaluate_meta_progression` (homologous to insight computed fresh from RunSummary.clues/won). Don't seed the live field into the next loop (insight also keeps no live `state["insight"]`); the cumulative lives only in `meta_progression.relationships` → no base re-accrual at new-loop archive. Prune 0 balances to match `route_runtime`'s fold/reconcile canonical form (negative delta net 0 → companion removed).
- Changed: ① `progression.py`: `MetaProgression.relationships: dict[str,int]` + `to/from_content` round-trip (`_relationship_tally` coerce/prune) + `evaluate_meta_progression` accumulates `_merge_relationships(previous, run_summary.relationships)` + `relationship:<name>:±N` grant note + `_run_summary_memory_from_archive` extracts `loop.state["relationships"]` + `_run_summary_from_memory` restore. ② `options.py`: `RunSummary.relationships` field. ③ `postgres_store.py`: `relationships` in `_PROGRESSION_JSON_COLS` + `_PROGRESSION_DICT_COLS` (skill_ranks/relationships default `{}`). ④ NEW `migrations/006_progression_relationships.sql` (`ADD COLUMN IF NOT EXISTS relationships JSONB DEFAULT '{}'`).
- Verified: `make check` EXIT=0 — ruff/eslint/mypy (116 files)/frontend build + **414 tests OK** (skipped 2, +8). `RelationshipCarryOverTest` 8 items: per-run accumulation, cross-run cumulative, negative-net-0 prune, empty-run no-change, content round-trip, malformed coerce, archive→run_summary→evaluate end-to-end round-trip, `apply_meta_progression_to_state` carry-over.
- Blockers: none.
- Next: API serializer/frontend affection gauge (already exposed in `MemoryOverview.meta_progression`) + P1 cutscene unlock (`directives/companions/<name>.md`). Real top priority is Neo-Seoul human QA (`[manual]`).

## 2026-06-16 — P0 affection runtime: session choice relationship accumulation (seed M, `[auto:claude]`)
- Status: overnight `[auto:claude]` seed M — following seed L (route perspective relationship), accumulate per-**choice** `effect.relationship` into `state["relationships"]` on the `session` choose path. The `Choice` model had no `effect` field, so authored choice-level affection had nowhere to land (prereq for P2/P3 companion cutscene choices) — added the field + accumulation wiring. green.
- Measurement: the non-trivial point is **ordering/idempotency vs route reconcile**. route_runtime's `advance_route` replays the visited path each turn and recomputes route contribution from scratch (`relationship_tally`), reflecting it into persisted state via `_reconcile_relationships`. So folding the choice delta into `state["relationships"]` in `_commit_scene` **just before advance_route** means reconcile adjusts only the route tally diff and thus preserves the (non-route) choice contribution → route+choice sum with no double-count (the producer that actually generates the contract L's `test_non_route_contribution_preserved` guarded). engine `_merge_state` is `dict(state)`, so `relationships` persists across turns. glass-library lacks route_map → advance_route is a no-op, so choice accumulation can be verified standalone.
- Changed: ① `mythos_core/models.py`: `Choice.effect: dict[str,Any]|None=None` (generic `to_json_dict`/`from_json_dict` round-trip, Director-generated choice is None). ② `route_runtime.py`: `fold_relationship(current, delta)` public helper (fold non-route delta, prune 0, skip abnormal values) + `__all__`. ③ `session.py`: `_choice_relationship(scene, choice_id)` helper (extract choice `effect.relationship`) + `_PreparedChoice.choice_relationship` + populate in `_prepare_choice` + `choose`/`stream_choose` pass to `_commit_scene` + `_commit_scene` injects via `fold_relationship` just before advance_route.
- Verified: `make check` EXIT=0 — ruff/eslint/mypy (116 files)/frontend build + **406 tests OK** (skipped 2, +6). `FoldRelationshipTest` 4 items (accumulation, negative/0 prune, abnormal-value/missing-current, route reconcile survival se_rin:3+kai:1) + `test_choose_accumulates_choice_relationship` (2 turns {se_rin:1}→{se_rin:2,kai:1} persists) / `test_choose_without_relationship_effect_leaves_state_clean` (choice without effect creates no relationships key).
- Blockers: none.
- Next: seed N (progression cross-loop carry-over — accrue/carry `relationships` into meta progression, insight pattern). API serializer/frontend gauge + P1 cutscene are follow-ups. Real top priority is Neo-Seoul human QA (`[manual]`).

## 2026-06-16 — P0 affection runtime: route_runtime relationship accumulation (seed L, `[auto:claude]`)
- Status: overnight `[auto:claude]` seed L — activate `effect.relationship` dead-data. Many neo-seoul perspectives author `effect: {relationship: {se_rin:±1, kai:1, lin_yue:1}}` but `advance_route` applied only `effect.flags`/`ending_influence` and ignored relationship → state never accumulated. Now accumulated into `state["relationships"][name]`. green.
- Measurement: the non-trivial point is **idempotency** — `advance_route` re-replays the whole `visited` path each turn and recomputes `flag_set`/`ending_tally` from scratch. flags are a set (re-add idempotent), tally is a dict recompute (safe), but `+=`-ing relationship into persisted state means **the same delta re-accrues every replay (double-count)**. → homologous to tally, recompute route contribution (`relationship_tally`) fresh each turn and store it on route_map, and reflect into `state["relationships"]` via a **reconcile that subtracts the previous route_tally and adds the new route_tally**. This gives replay idempotency + preserves non-route contribution (future seed M choice delta). Negative deltas (p_caution se_rin:-1) supported too, 0 balances pruned.
- Changed: ① `route_runtime.py`: resolve loop aggregates `effect.relationship` (dict, int cast, skip abnormal values) into `rel_tally` → store `new_route_map["relationship_tally"]` + `_reconcile_relationships(current, prev_route_tally, new_route_tally)` helper computes `new_state["relationships"]` (subtract prev, add new, prune 0). Added relationship ownership + idempotency note to module docstring. ② `tests/test_content_integrity.py`: moved `relationship` key from `PENDING_ROUTE_EFFECT_KEYS`→`CONSUMED_ROUTE_EFFECT_KEYS` (now actually consumed), PENDING is empty frozenset (anti-rot guard stays green).
- Verified: `make check` EXIT=0 — ruff/eslint/mypy (116 files)/frontend build + **400 tests OK** (skipped 2, +5). `RouteRelationshipTest` 5 items: delta applied (trusted_se_rin→se_rin:+1), negative delta (refused_se_rin→se_rin:-1), no replay double-count, non-route contribution (kai:5) preserved (advance+replay), determinism + monotonic path length.
- Blockers: none.
- Next: seed M (accumulate choice `effect.relationship` on the `session` choose path identically, L prereq met) / N (progression cross-loop carry-over). API serializer/frontend gauge + P1 cutscene are follow-ups. Real top priority is Neo-Seoul human QA (`[manual]`).
