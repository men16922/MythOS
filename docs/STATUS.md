# Project MythOS Status

Last updated: 2026-09-05

## Current Baseline

Project MythOS is past local-playable MVP into a state where a React SPA + FastAPI API + Streamlit demo coexist. The core runtime is shared through a single `RuntimeSessionService`.

Major implemented axes:

- **Shared runtime/UI**: `RuntimeSessionService` is the single orchestration boundary for FastAPI REST/WS, React/Vite, Streamlit, and CLI.
- **Persistence/infra**: PostgreSQL is authoritative; MinIO/GCS store assets; OTel/Jaeger/Cloud Trace cover observability. Redis was removed 2026-07-04, so images generate synchronously in-request.
- **Narrative**: local development uses Ollama 8B storyteller + 3B parser with repair/fallback; the cloud product uses Vertex Gemini controlled generation. Prompt directives, Story Bible snippets, deterministic session synopsis, shard rollup, and outcome metrics are integrated.
- **Cloud model operations**: narrative is full `gemini-3.5-flash`; image generation is GA `gemini-3.1-flash-image` on its model-specific `global` endpoint while Cloud Run/GCS remain `us-central1`. The prior 3.1 404 was a regional endpoint mismatch, not model unavailability.
- **Visuals**: local mflux/FLUX + Redux and cloud Gemini image share the provider/storage boundary; curated scene/cutscene/ending art and synchronous generation are active.
- **Gameplay**: deterministic tactical combat, direct party control, companion growth/equipment, enemy intents, skills/items, combat cinema, rewards, boons, market/recovery, and IX boss flow are implemented. The 2026-07-11..14 combat overhaul arc (telegraph→control→status→visuals→enemy roster→balance→portrait dock) is owner-QA-passed — `COMPLETED_SUMMARY.md` M59-M60.
- **Scenario/progression**: Neo-Seoul is the primary long-form scenario; route-node DAG + multi-perspective anchors + session memory, Story Bible, Codex, Run History, achievements, meta progression, save/load, and ending resolver are implemented. Glass Library is parity-ready but held.
- **Localization/CBT**: EN-default/KO bilingual UI+API+prompts, invite-gated stable identity, loop caps, Save/Load UX, and the Cloud Run closed beta are live. Teaser V2 published (YouTube, 2026-07-14).
- **Agent operations**: plugin-owned Overnight Harness V2, required lane-aware contracts, semantic/domain verifier evidence, and three-engine worktree tooling are active. Harness pin updated to **1.4.0** 2026-09-05 (`.claude/harness-config.json` `harness_root`; the prior 1.3.4 cache dir no longer exists, which had silently broken `HARNESS_ROOT` resolution to empty). 1.4.0 adds per-repo Claude model routing (actor `CLAUDE_MODEL=claude-sonnet-5`, critic `OVERNIGHT_CRITIC_MODEL=claude-fable-5-1`), now exported from the Makefile. Generic lifecycle skills remain plugin-owned; repo-local skills remain `$gameplay-qa`/`$codebase-design`. Remote publication is intentionally absent.

Latest verified baseline:

- **2026-09-05 REVIEW RESIDUALS 8/13 CLOSED (`9b958b2`, `4d10b35`), 1343 TESTS, DEPLOYED `mythos-api-00087-w7z`** — soft-defeat guard on requested archive, refusal clears pre-seeded `met_se_rin`, CC-aware intent preview, WS returns pooled connection per message, one snapshot builder, one-read progress facts, canvas latest-frame redraw + cached layout (browser-unverified). Open: `_commit_scene` reducer, `NarrationReveal`/`combatView.ts`, `_weapon_in_range` (owner), route replay, migration `008` on prod.
- **2026-09-05 REPO-WIDE REVIEW → 3 BUNDLES (`047fcf0` / `fb44514` / `f1d22d6`), 1340 TESTS, DEPLOYED `mythos-api-00086-4d5`** — production-facing: sentence-gap regex corrupted every scene; `MYTHOS_WORLD_ID` split (`world_mythos` written / `mythos-local` read) since 06-06 had hidden every archive from initial-loop scoring, the memory overview and compaction; boss-climax-ended loops never wrote `loop_archive`; unknown encounter ids raised after commit; reward insight committed outside the transaction; Codex lore bodies empty; mid-run language toggle re-picked the scenario. Combat: buffed moves dropped, ally targetable, corrode-blind preview, telegraph kill logged as hit. UI: scene-bound `visual_status`, socket-before-unmount, overlay-gated hotkeys, localized canvas text, lazy chunks (app.js 496→388 kB). Migration `008` (shard index) is owner-run on prod. Details PROGRESS_LOG.
- **2026-09-05 DEPLOYED `mythos-api-00085-mvr` — THE 08-09 HOLD IS LIFTED, 39-COMMIT FIX BUNDLE LIVE** — owner ordered the deploy ahead of the §3 verdict (DECISIONS 2026-09-05). Gate: lint/typecheck 0, 1325 tests OK, `app.js` in sync; live root/health 200, live/local `app.js` SHA-256 match, pins preserved (`gemini-3.5-flash` / `gemini-3.1-flash-image` @ `global`, 3600s), 0 WARNING+ in the first 30 min. The banked 08-08 arm is no longer build-comparable; the next promotion sample must be played on `00085`. Harness pin repaired to 1.4.0 the same session (`overnight-where` had resolved to empty).
- **2026-08-14 PER-TURN TOKEN USAGE NOW LOGGED — PER-LOOP COST STOPS BEING AN ESTIMATE** — closes the blocker the promotion eval below hit hours earlier, in the same session across midnight: no provider recorded token counts, so `cost_per_loop_usd` was unmeasurable for every sample. `mythos_narrative/usage.py` carries counts from the Gemini provider into the turn log that already keys on `loop_id`, via a `ContextVar` (a shared provider instance would leak one turn's counts into another). Two counting traps are pinned by tests: Gemini repeats `usage_metadata` **cumulatively** on stream chunks (per-chunk recording would multiply a turn's tokens by chunk count), and a turn billed twice (unparseable stream → non-streaming retry) must **sum**, since keeping the last under-reports the priciest turns. Caught before shipping: `JsonFormatter`'s field allowlist was silently dropping the new keys, so the LogRecord test passed while Cloud Logging would have received nothing — allowlist extended, formatter-level test added. `make check` **1285** (5 skipped). Ollama still reports nothing (local dev, free); no USD price table is applied — the log carries tokens only. Local-only.
- **2026-08-13 §3 SAMPLE ADMITTED AND SCORED — JUDGE VARIANCE IS ~1 POINT, SO THE SCORES CANNOT CARRY A PROMOTION** — the owner ruled the 22+25 two-build split acceptable, and `prod-people-help-20260808` joined the hash-frozen `promotion` lane (bank now 3). The first `--promotion` run ever executed returned **3/5** for the new sample and **2/5** for both 2026-07-28 samples — but those two are byte-identical to the pair that scored **3/5** in July, so the rubric moved a full point on unchanged input. That variance equals the gap being measured; the new sample's lead is not evidence. Companion metrics: length **pass** ×3, repetition **fail** ×3 (rule stated in the metrics file), and `cost_per_loop_usd` is the documented ~$1.0/loop applied uniformly because **no per-loop cost exists** — the narrative provider logs no token usage, so it cannot be recovered from Cloud Logging. Report `outputs/evals/20260814-005948/` (stamped past midnight, same session); measurement tool `scripts/eval/companion_metrics.py`. Every run now also writes `provenance.json` and heads its report with the judge argv/version and the **rubric SHA-256**, since the rubric is injected into the prompt verbatim and moves scores just like the engine does — so the *next* comparison is attributable, though the July/August pair is not, retroactively. How to stabilise the score is an open owner call (NEXT_PLAN). Local-only.
- **2026-08-09 PLURAL-KEYWORD GAP CLOSED; BLANKET INFLECTION PASS REJECTED ON MEASUREMENT** — ASCII matching is word-bounded, so every singular fails on its plural; sweeping all four axes found nine inflected forms present in the 292 banked labels, of which **seven change nothing and two would misclassify** (`decrypted` would read an adjective describing a destination as the action; `signals` would pull "disable the drone before it signals the grid" from control to data). Only `civilians`/`survivor`/`survivors` shipped — chipless 30 → 28, four labels change, and the two *"Lead the survivors…"* labels stop disagreeing with each other. `crowd` deliberately stays out (a place to hide in) and is now pinned by a test. `make check` **1270** (5 skipped). Local-only.
- **2026-08-09 VALUE-AXIS VOCABULARY WIDENED ON THE OWNER'S CALL** — the coverage-vs-precision residual is closed: the three measured families (traversal 47 labels, sabotage 22, fight 6) were adopted and `spoof → data` held, taking chipless **34% → 10%** over 292 banked EN labels at exactly one reclassification. A per-term audit run before implementation dropped three of the proposed terms (`brace`, `run`, `draw your weapon`) because each read a real label wrongly; `fight` is only safe because safety is scanned before control, now pinned by a test. Contrast improves on two arms (14/45→11/45, 10/45→6/45) and worsens by one scene on the third (4/44→5/44) — coverage's real cost, recorded not smoothed. `make check` **1268** (5 skipped). Open follow-up: the people axis lacks `civilians`/`survivor`. Local-only.
- **2026-08-09 `CURRENT OBJECTIVE` REPRODUCED AND FIXED (was filed not-reproducible)** — the earlier ruling-out measured the server, where `_chapter_goal` genuinely covers all five playable phases; the defect was client-side and the strip never went empty, it lost one line. `ObjectiveStrip` has two layouts and only the mobile/coarse one resolved 현재 목표 as `objective || chapter_goal`; desktop rendered act goal and current objective as independent rows, so an omitted `objective` — optional in the scene schema, defaulted nowhere — dropped the CURRENT row while ACT GOAL stayed. Both layouts now share one resolution. `make check` **1264** (5 skipped), locked by 4 source-level tests whose discrimination was verified against `HEAD`. Omission frequency is **not** claimed: `bank_loop.py` never exported the field. Local-only.
- **2026-08-09 KOREAN-ONLY MATCHING CLASS FULLY CLOSED (6 instances)** — the last two: a bare SFX marker whose `[A-Z0-9 _-]+` under `IGNORECASE` ran to the sentence end and **deleted the trailing clause** on the main JSON path (English only — Hangul had been acting as the boundary), and a combat trigger that matched only Korean phrases so EN narration raised `combat_imminent` with no fight behind it (local dual-model path only). Both reproduced, fixed, and locked; 5/5 sentences keep their clause, 7/7 combat triggering across both languages. `make check` **1260** (5 skipped). The frontend was then swept for the same class and came back **clean** — `termGloss.ts` already implements the rule independently, and `combatCinemaSkills.ts` resolves 39/39 on the real catalog (its unbounded aliases are deliberate: whitespace-stripping means `\bemp\b` would break `"EMP Pulse"` too), so only a latent trap for future skills is documented. **No agent-executable Priority 0 item remains.** Local-only.
- **2026-08-09 MATCHING RULE CONSOLIDATED; HAN'S PORTRAIT NO LONGER BOUND BY "CHANGE"** — third instance of the class: `visual_service` picked a character's reference image with an unbounded substring, so "The **chan**nel goes dead", "You **han**dle the terminal" and "Ex**chan**ge rates" all bound Han's portrait (and his appearance line when the word reached the prompt). The first fix regressed Korean — dropping one-syllable names made `한이…` undetectable — so the repo's existing particle-bounding rule was promoted instead. `mythos_core/text_match.py` now owns the rule and the four copies (value-axis chip, Se-rin flags, character art, companion detection) route through it; `_companions_in_text` gained the ASCII guard it never had. 9/9 on real scenario data, 13 new tests, `make check` 1250. Local-only; art effect unverified in a browser.
- **2026-08-09 ENGLISH PLAYERS COULD NOT REFUSE SE-RIN (GAMEPLAY)** — sweeping for the *class* the value-axis bug belonged to (Korean-only matching logic in an EN-default product) found it in `LoopEngine`: the opening's `met_se_rin`/`refused_se_rin` split reads the choice label with Korean-only keywords, and the branch is asymmetric, so on an EN loop **every** player was recorded as accepting — `Refuse her hand and slip into the alley alone` set `met_se_rin`, and `refused_se_rin` was unreachable. These flags gate route content, Se-rin's combat presence and ending branches, so the refusal arm of the opening was dead in the default language. EN keywords added, ASCII word-bounded, and an explicit refusal verb now outranks the noun it refuses; 12 probes, KO byte-identical, six regression tests, `make check` 1241. **Three more instances of the class remain open** (combat trigger, opening cinematic gate, unbounded character keyword) — see NEXT_PLAN. Local-only.
- **2026-08-09 VALUE-AXIS CHIP WAS KOREAN-ONLY — EN LOOPS ADVERTISED "HELP PEOPLE" ON ALMOST EVERY CHOICE** — `_choice_axis` matched a Korean keyword list, so on an EN loop no label could match and the axis fell to an `intent` fallback mapping `interact`→people unconditionally: measured over three banked EN arms, **96/98, 94/95 and 97/99 labels** resolved to "Help people" and **43/45 multi-choice scenes put every choice on one axis**. Bare-substring matching also let `"ix"` inside *Fix* classify a choice as control. EN vocabulary added on all four axes (KO widened too), ASCII keywords word-bounded, and both inventing fallbacks removed — an unreadable label now renders no chip, the rule junction destinations have followed since 07-19. Same-axis scenes **43/45 → 14/45**; `make check` 1235 (5 skipped). Owner call: 40/98 choices now show no chip where all showed one. Local-only.
- **2026-08-09 PORTRAIT ATTRIBUTION DEFECT FIXED; OBJECTIVE GAP NOT REPRODUCIBLE** — the 07-11 possessive guard (written for KO `린위에의 부하`) also rejected `X's voice`, the ordinary EN attribution form, so `Han's voice cracks.` attributed to **nobody**; combined with `administrator ix` being the only EN keyword, the IX line at turn 50 fell through to the one other name in the paragraph. Guard narrowed to allow a possessive on the speaker's own speech noun in both languages, IX keywords widened, 11-probe before/after with the owner's 07-11 regressions re-verified dead. Two more faults in the same module then closed the symptom: with two characters named the speaker was decided by scenario array order (now the name carrying a speech cue wins, with both portrait surfaces sharing one judgment), and `"…," X says.` was not recognised as dialogue at all — 33% of quoted spans in the arm rendered as prose instead of a callout. Final probe 16/16 including the owner's 07-11 negatives; `make check` 1231 (5 skipped). The missing `CURRENT OBJECTIVE` did **not** reproduce: `chapter_goal` covers all five playable phases. Local-only.
- **2026-08-08 TUTORIAL TIER WIDENED 1→4 ENCOUNTERS** — the early risk cap is keyed to combats *won*, so a losing player stayed on tier 1, where the single authored encounter left the 08-08 downgrade fix choosing between a repeat and nothing: measured 1 fight served / 19 ambient beats skipped out of 20. Three risk-1 encounters authored (reusing existing bestiary art, added to the `start_combat` prompt pool) turn that into 20/20 across four fights. The repo's greedy balance simulator rejected the first draft at 0.93-0.98 solo (0.95 ceiling); retuned to 0.58/0.72/0.77 with tier 2 unchanged at 0.27/0.28. `make check` 1225 (5 skipped), rendered evidence `outputs/live-qa/20260808-tier1-encounters/`. Local-only.
- **2026-08-08 RENDERER FIX DEPLOYED (`mythos-api-00084-nt2`) + FRESH ARM 47/47 BANKED** — 100% traffic, root/health 200, live/local `app.js` SHA-256 match, pins/timeout intact. Arm `loop_426b710d…` reached `ending_erasure` (Forced Erasure) at the IX boss after 59 scenes and **47/47 narrative success with 0 fallback**, banked as `scripts/eval/golden/prod-people-help-20260808.json`. Caveat: the generations split 22 (`00083-jt7`) + 25 (`00084-nt2`) across the mid-arm deploy. Only the owner's subjective ending/overall verdict remains.
- **2026-08-08 EN DIALOGUE APOSTROPHE SPLIT FIXED** — the scene renderer treated the ASCII apostrophe as a quote delimiter, so EN contractions split across dialogue callouts (`"You` + bubble `'re cutting it close…`), and a comma-terminated quoted line desynchronized quote pairing so attribution prose became the bubble. Word-internal apostrophes no longer delimit spans and non-speech spans are consumed whole; the real module was measured before/after on the production paragraph and across a nine-pattern probe with KO behaviour unchanged. `make check` 1195 (5 skipped). The §3 arm `loop_426b710d…` is parked live at 21/21 zero-fallback on `00083-jt7` pending this deploy.
- **2026-08-02 BOON MODAL 409 WEDGE FIXED (`mythos-api-00083-jt7`)** — the AMP-shard/echo overlay now treats a 409 already-consumed pick as consumption and closes instead of wedging on a stale offer; verified by a rendered local reproduction (server-side consume → stale click → 409 → close) and deployed at 100% traffic with live/local `app.js` SHA match. `make check` 1193 (5 skipped).
- **2026-08-02 NORMAL-TURN RETRY DEPLOYED (`mythos-api-00082-ffc`)** — diagnosed and fixed the retry gap that excluded the 08-01 arm: `RuntimeOptions.fast_mode=True` (the API default on all player-facing flows) made `_repair_enabled` veto the streamed parse-fail non-streaming retry, so the b55e933 whitespace-runaway mitigation had never fired in production. The streamed retry now uses a fast_mode-independent gate (explicit `repair_enabled=False` still wins), measured before→after (0 retries/fallback → 1 retry/success on the same unparseable-stream fixture) and locked by `StreamedParseFailRetryTest`. `make check` 1193 (5 skipped); deployed 100% traffic with pins/timeout preserved, root+health 200.
- **2026-08-01 FRESH ARM ATTEMPT EXCLUDED 13/14 + TYPED EVIDENCE VALIDATED** — a direct-play EN people/help arm on `00081-8lc` (`loop_8b7a…60dc2`) recorded 13 success + 1 fallback and was stopped/excluded per the 47/47 rule. The fallback carried typed `fallback_reason=parse_error` (first production capture), but the non-streaming retry did not fire on that normal turn despite `MYTHOS_FAST_MODE` being unset — a retry-gap diagnose now precedes the next paid arm. Post-defeat combat pacing held (no ambient re-entry within 3 narrative commits at tension 90–100) and no `Changed …` titles appeared, but the novelty reviser's own `New Vector at …` template titled 4 scenes and the identical `Patrol Ambush` encounter/copy served 3×. AMP-shard modal non-dismiss (409 on re-click) reproduced twice. The 07-31 deployed source/docs bundle is now committed (`272f89b`, push owner-run).
- **2026-07-31 REMEDIATION + GEMINI 3.1 DEPLOYED / DIRECT QA** — `mythos-api-00081-8lc` serves 100% traffic with narrative `gemini-3.5-flash`, image `gemini-3.1-flash-image` at `global`, and Cloud Run timeout 3600s. Narrative-clock/post-flee combat pacing, normalized location/motif novelty enforcement, and typed fallback logging are live. Focused 94, smoke-local, lint/typecheck/build, and final `make check` passed 1191 (5 skipped). Production health/root passed; six initial narrative calls were success/fallback 0; app-path 3.1 images generated, stored, delivered, and rendered.
- **2026-07-31 DEFERRED IMAGE STALL DIAGNOSED/REMEASURED** — before: the gameplay WS ended after 280.016s under the 300s service timeout, 3.1s before the successful deferred visual send, leaving `Preparing image generation…`. After timeout 3600s: CDP measured `snapshot`→`visual_status{succeeded,url}`, DOM switched to the new scene ID, and the new image rendered. The partial QA loop is operational evidence only, not a promotion sample; one fresh 47/47 zero-fallback arm and owner feel verdict remain.
- **2026-07-30 ENDING CONTRACT DEPLOYED + DIRECT PRODUCTION QA** — `mythos-api-00079-d4k` serves 100% traffic with `MODEL=gemini-3.5-flash` and `IMAGEN_MODEL=gemini-2.5-flash-image`; health/root returned 200 and live/local `app.js` SHA-256 matched. Predeploy focused tests passed 146; final `make check` passed 1182 (5 skipped). Fresh EN `loop_fde2…fec34` reached Forced Erasure, and Run History preserved authored EN ending narration plus lost/carried outcomes. Browser-observed boot, choices, a successful app-path image, combat/flee/return, and the final archive message passed. The sample is not promotable: Cloud Logging recorded 45/47 success and 2 fallback, while the late loop repeated drainage/vent/searchlight/escape/combat beats. Objective ending continuity passes; subjective ending/overall feel remains owner-manual. Product source was later pushed through `c89a87c`.
- **2026-07-29 ENDING ARCHIVE/CONTINUITY CLOSED LOCALLY** — IX `player_fled`→Forced Erasure now archives the authored ending narration and data-driven saved/lost/carried facts; old summaries backfill from durable ended-loop state and the existing recursive locale seam returns authored EN instead of a generic KO-only record. Run History renders all four parts. Focused runtime/API/content tests passed 146 + 84; the rendered KO Run History slice passed with screenshot `tests/playwright/outputs/03.5_run_history_outcome.png`; final `make check` passed 1182 tests (5 skipped). Local-only: no commit, push, deploy, production play, or subjective re-sign-off; `00078-rs9` still serves the pre-fix contract.
- **2026-07-29 §3 DELEGATED DIRECT VERDICT = HOLD** — the valid 47/47 production pair was reviewed from both full banks, Cloud Run request/log evidence, and production Run History. People/help and safety/evidence separate clearly at first but converge into repeated drainage/searchlight/escape/combat patterns. Both banks stop at IX `player_fled`; archived Forced Erasure records expose only the same generic KO summary even for EN and do not narrate saved/lost/carried outcomes. Three memorable scenes were found, but style durability and ending coherence did not pass. A suspected save/load defect was disproved: request logs showed the excluded `loop_f148…2350` was selected by an interleaved-button QA locator. No code, push, or deploy.
- **2026-07-29 M4 MOBILE MODAL QA CLOSED** — 390×844 rendered checks covered Boot Intro, Save/Load, History, Route Map, Boon, combat interstitial, and Combat Cinema, plus the current skill-loadout surface. Save/Load reproduced the shared defect: a gesture moved the page from `scrollY 337→37` behind the open overlay. The common blocking-surface contract now locks body scroll; content-heavy panels use bounded internal scroll, all rendered panels stayed inside the phone viewport, and post-fix Save/Load held `scrollY 315→315`. Focused UI/combat regressions passed 32/32, browser warnings/errors were 0, and `make check` passed 1179 tests (5 skipped). Local-only; no push/deploy or human feel claim.
- **2026-07-29 NARRATIVE EVAL SPLIT FROZEN** — `scripts/eval/SPLIT.json` hash-locks the two local loops as development samples and the two valid 2026-07-28 production loops as promotion-only samples; the fallback fixture is excluded. Default `make eval-narrative` cannot score promotion samples. Promotion requires explicit mode plus cost/loop, repetition, and length companion metrics, which are emitted with the rubric report. The pre-freeze 3/5 production scores remain a disclosed baseline, not a blind result. `make check` passed 1178 tests (5 skipped); no judge/model call.
- **2026-07-28 §3 VALID PRODUCTION PAIR COMPLETE** — same-player people/help `loop_c661…25bb` and replacement safety/evidence `loop_84df…33ef` both completed Forced Erasure and each passed a 47/47 Cloud Logging non-fallback audit on `mythos-api-00078-rs9`. The 89-scene and 61-scene EN transcripts are banked; paired rubric report `outputs/evals/20260728-030707/` scores both 3/5. The excluded `loop_f148…2350` remains invalid at 3/15 fallback. Only the owner's three subjective judgments remain.
- **2026-07-28 CLEAN REPAIR-0 ARM VALID — OWNER RETAINED STRICT 12 AND CLOSED REPAIR ROLLOUT** — a brand-new Python 3.13 worktree passed preflight (mypy 188) and base `make check` (1171, 5 skipped). All three actors produced scoped commits and self-reported green, but 37/28/27 turns exceeded 12; Harness 1.3.4 rejected before external verification, exactly compensated 3/3, and left the final tree clean. Valid actor wall/cost/tokens = 504.072s/$2.7305/4,927,960; total spend including one excluded wrong-runner dispatch = $3.1979. Final compensation-tree `make check` passed 1171 (5 skipped). Owner decision: keep `OVERNIGHT_REPAIR=0`, stop repair A/B, never tune/retry bank v1. Report: `docs/reports/2026-07-28-heldout-v1-clean-repair0-baseline.md`.
- **2026-07-27 FRESH-WORKTREE BASE GREEN (`9ffad61`)** — documented setup now installs required cloud test SDKs, pins NumPy `<2.5`, and preflights imports+mypy before dispatch. This removed the first cohort's base-red contamination.
- **2026-07-27 FROZEN-BANK COHORT EXECUTED — PRODUCTIVITY BASELINE INVALIDATED** — three repair-0/subagents-0 Claude actors reported 47/40/46 turns against 12; Harness rejected all three before verification, exactly compensated all commits, and left 0 dirty worktrees. Total actor wall/cost = 790.198s/$4.3584. The disposable base was mypy-red from NumPy 2.5.1, so the cohort proves turn fail-close only, not productivity or repair lift.
- **2026-07-27 TURN-BUDGET ACCEPTANCE GATE RELEASED + BANK RATIFIED** — Harness 1.3.4 compiles `budgets.turns`, parses final `num_turns`, binds the actor log by SHA-256, and rejects/compensates over-budget commits before verification; successful Claude output missing the metric also fails closed. Same real evidence now reads `exceeded|31|12`; upstream 136/136 plus package/manifest/policy gates and MythOS five-path graph smoke pass. The three docs/Python/UI tasks in `scripts/overnight/heldout-bank.md` are owner-ratified and frozen. No model call/push/deploy.
- **2026-07-26 DEV GRAPH EMPIRICAL BASELINE COMPLETE — OVERALL HOLD** — Harness 1.3.2 deterministic graph/pause/fault cohort is 110/110 over five repetitions; two real mission terminals agree with independent audit, false accepts/scope escapes/dirty leftovers/claims are 0, and the first gate-rejected commit was exactly compensated. The isolated retry `mission-20260726-191032-9345` was accepted after `make check` 1171 (5 skipped), scope/regression verifiers, ledger/trajectory audit, and human diff review; 223.234s, 1,951,605 reported tokens, $1.1495. Report: `docs/reports/2026-07-26-dev-graph-empirical-baseline.md`.
- **2026-07-26 RETRY CONTRACT ALIGNMENT RELEASED IN 1.3.3** — explicit `CONTRACT_RETRIES=0` now reaches external/built-in compilers and canonical provenance unchanged; provenance also states the Claude USD cap is per-invocation and mission cost is unenforced. Same measurement changed 0→1 to 0→0; Harness 128/128, package gates, MythOS five-path graph smoke, and direct compiler probe pass without a model call.
- **Expansion boundary** — repair rollout is closed at strict-contract 0/3; do not run repair-1 or tune/retry bank v1. Any future reopening requires a preregistered rule, new owner-ratified unseen bank v2, and fresh cost approval; fan-out still needs explicit multi-agent authorization.
- **2026-07-26 DEV GRAPH P1 INTEGRATION SMOKE GREEN (`make overnight-graph-smoke`)** — one offline command drives the released 1.3.0 controller and MythOS compiler through design-blocked, accepted, reverted, repaired, and paused disposable Git repositories. It proves base-red→candidate-green, immutable evidence hashes, ledger corruption/provenance drift rejection, deterministic balanced trajectories, exact compensation, durable pause, claim cleanup, worktree isolation, and identical output across two full runs. No real mission or network was used.
- **2026-07-26 HARNESS 1.3.0 LOCALLY RELEASED + MYTHOS PINNED (`bc48e8b`; tag `overnight-harness--v1.3.0`; harness 116/116; `make check` 1168)** — the tag-derived cache resolves through `make overnight-where`; init, ledger/state/trajectory/resume wiring, immutable provenance validation/comparison, package/manifest gates, and the full MythOS gate pass. Remote tag/marketplace publication was deliberately not performed, and the empty ledger proves wiring only. Detail: `docs/reference/2026-07-26-graph-engineering-after-harness-120.md`.
- **2026-07-26 DETERMINISTIC RESIDUALS DRAINED (`b10bf59`/`f90c3db`; `make check` 1168; AGY PASS)** — removed two stale `set-state-in-effect` suppressions so ESLint is warning-free; Su-ah's player-facing alias and KO/EN Bible title now use the already-canonical `기억의 대장장이` / `Blacksmith of Memory`. Legacy `잔향 가공사` remains keyword/glossary-only for old scenes. Runtime/ally/KO/EN integrity is regression-locked; evidence `outputs/live-qa/20260726-005229-post-commit/evidence-bundle.json`.
- **2026-07-26 FRONTEND GOD-COMPONENT TRACK CLOSED (`5247719`; `make check` 1167; AGY PASS)** — callback-safe fast/standard attack→impact→exit timeline, four-timer cleanup, and impact/finish cues moved to `useCombatCinemaTimeline`; timing profiles are source-locked. `useCombatCinema.ts` 181→125; official evidence `outputs/live-qa/20260726-004325-post-commit/evidence-bundle.json`. The remaining image fallback is intentionally inline (too shallow to justify another module).
- **2026-07-26 COMBATCINEMA SLICE A LANDED (`937d2fd`; `make check` 1166; AGY PASS)** — 19-card metadata catalog + exact-ID-first Korean/English alias policy moved behind pure `resolveCombatSkill()`; `useCombatCinema.ts` 419→181. Scenario coverage/exact-ID locks moved with the owner module; official evidence `outputs/live-qa/20260726-003850-post-commit/evidence-bundle.json`.
- **2026-07-26 APP.TSX DECOMPOSITION HALF CLOSED** — slice 18 B `useEpiphanyBanner` committed `6945a2f` and official AGY returned `PASS_CANDIDATE` (`outputs/live-qa/20260726-003032-post-commit/evidence-bundle.json`). Together with A `useSaveLoad`, App.tsx is 979→940; C `useTabs` is deliberately not taken because its interface-width/touch risk outweighs remaining depth.
- **2026-07-26 SLICE 18 A `useSaveLoad` LANDED LOCALLY (`e4ca67c`; `make check` 1166; AGY PASS)** — save/load modal/label/slot state, gated pre-connect prefetch+cancellation, and manual-slot restore-before-resume ordering moved behind one hook interface; App.tsx 979→956. Isolated rendered smoke measured modal close + `POST /save-slots/load` before `GET /loops/active`; official post-commit AGY returned `PASS_CANDIDATE` (`outputs/live-qa/20260726-001650-post-commit/evidence-bundle.json`). Branch is ahead 1; push not performed.
- **2026-07-25 PUSHED + EVAL-BANK FIRST REAL RUN + SLICE-18 SURVEY + CRITIC SMOKE** — pushed `9fffffe..8782d70`. 07-19 style pair located in the **local** DB (not prod) and banked; first full `make eval-narrative` = 1–2/5 **baseline** (fallback-arm boilerplate + since-fixed leaks; report `outputs/evals/20260725-234103/`). Slice-18 candidate doc landed (A `useSaveLoad` recommended; D reserved for the held-out bank). Cross-engine critic smoke on `046edc8`: codex REPAIR vs claude PASS — 1/1 disagreement, judged intended-design (comment added). Held-out task bank constructed (`scripts/overnight/heldout-bank.md`, compile-checked; owner ratifies composition).
- **2026-07-25 NARRATIVE TYPED REJECT EDGES + SOFT-REPAIR LEDGER (`make check` 1166 + smoke-local)** — graph-borrow into the product: every director fallback now records a typed reason (`blank_output`/`parse_error`/`provider_error` — the safety-filter-empty watch risk finally has a counter in metrics/logs/spans), validator soft-repairs surface as `LoopTransition.repairs` codes logged per turn instead of being silently absorbed, and `DESIGN.md` names the deterministic anchors (perimeter) the LLM can never override. Behavior-preserving; no new LLM repair calls (deliberate — unmeasured upstream). Detail: `docs/plans/2026-07-25-harness-120-adoption.md`.
- **2026-07-25 HARNESS 1.2.0 ADOPTED (`make check` 1161)** — plugin published 1.2.0 (marketplace cache + source-checkout pin both at 1.2.0; `overnight-where` resolves the pin). Repo-side adoption per `docs/reference/GRAPH_ADOPTION.md`: `.gitignore` +CLAIM, critic prompt → 3-value `PASS/REPAIR/FAIL`, typed verifier exits (`20`/`30`/`40` repairable `exit 4` with evidence, `10-diff-scope` deliberately all-revert), external compiler writes `budgets.revisions`, Makefile `OVERNIGHT_REPAIR_MODE=0` + `OVERNIGHT_CRITIC_ENGINE` knobs. Repair edge stays OFF until the held-out task bank exists (NEXT_PLAN WS5). Notes + MythOS borrow candidates: `docs/plans/2026-07-25-harness-120-adoption.md`.
- **2026-07-21 SLICES 16–17 LANDED (`make check` 1161; `68a6dc4`/`fe1585c`, push owner-run)** — App.tsx decomposition: slice 16 `useIntroSequencer` (B2 opening-variant race → hook) then slice 17 `useInviteGate` (closed-beta gate probe/fail-open/submit → hook, zero inputs). App.tsx 1050→**979** (under 1000). Both behavior-preserving; lint/build clean. Watch: slice-16's loop-2 non-fallback intro branch is live-only (post-commit AGY + owner §3). Decomposition deep-candidate pool now thin — a fresh App.tsx survey is needed before slice 18. Eval-bank owner-run script `scratch/bank-style-pair-loops.sh` prepared (prod-DB banking is classifier-blocked for the agent).
- **2026-07-21 QA REDUCTION RATIFIED + SLICE 15 LANDED (`make check` 1161)** — owner ratified 5(+1 chip) auto / 8 monitored / 3 human (checklist active surface 16→3; M65, DECISIONS 07-21) after V2 evidence completed 3/3 on `00078-rs9` (7/7 PASS, hashes 32/32, agent-run collection 7.8 min). App.tsx decomposition resumed: slice 15 `useCombatTutorial` extracted (1102→1050 lines), all tutorial paths verified in the combat sim (move/wait invariant, advance, match, skip). Local commits — push owner-run; rides the next deploy.
- **2026-07-20 DEPLOYED `mythos-api-00078-rs9` (`make check` 1161; 100% traffic; pushed `c51cf63`)** — custom icon set + skill loadout editor + aim corner badge live. `GameIcon` inline-SVG set replaces text-presentation glyphs (⚔❖◎▣✚✦❒★◆ etc.) across route map/legend, choice chips, interstitial, save slots, tactical key, market, turn-order, epiphany banners — the CBT "⚔ → thin ✕" report is resolved. Skill quick-slot setup moved off the bar into a dedicated 편성 modal (owner request; per-slot ⇄ picker removed, lock test re-pinned), and the aim(🎯) toggle became a crosshair corner badge on the card. Live root + `/api/v1/health` 200; live/local `app.js` SHA-256 match; model pins preserved (`gemini-3.5-flash` / `gemini-2.5-flash-image`). This deploy is the target for V2 release-bundle 3/3 (`scratch/run-release-calibration-00078.sh`, owner-run).
- **2026-07-20 V2 RELEASE EVIDENCE 2/3 + route_axis_chip SHIPPED** — the six-assertion contract repeated on `00077-8g9` (6/6 PASS, artifact hashes 25/25, report attention 1/clean 15→sample 3), and a 7th objective assertion (§2 junction value-axis chip vs destination semantics, independently re-encoded policy) was calibrated live (turn-4 junction PASS, hashes 5/5); live-QA checklist §2 keeps feel-only. `make check` green.
- **2026-07-19 DEPLOYED `mythos-api-00077-8g9` — §3 ROUTE LABEL→AXIS ALIGNMENT (`make check` 1158; 100% traffic)** — explicit junction picks now accrue the destination waypoint's axis (clue→evidence, rest/patrol→safety, combat→control), bootstrapping `insight_focus` in normal play, and `route:` choice chips derive from the destination's real semantics (anchor = perspective under current flags; no chip when axisless) instead of the keyword heuristic that mislabeled all four stored junction labels `단서 찾기`. Re-measured on `loop_5b212d`: replayed picks yield `evidence:1` (was 0); rn4 now chips `사람 돕기` matching its applied `p_rescue`. 10 new regressions; commit `fc4ebc5` (push pending, owner-run); live root/health 200, model pins preserved.
- **2026-07-19 LOCAL NARRATIVE FALLBACK + SAME-CHARACTER STYLE PAIR (`make check` 1148)** — blank cleaned narration now raises `NarrativeParseError` and falls back to an authored two-choice scene. Same Ghost/player completed two rendered 46-turn loops: people/help reached `people:2`, `p_rescue`, Safe Refuge 3; evidence-then-safety reached `people:1,safety:1`, Safe Refuge 2. Both boss escapes ended in Forced Erasure. Evidence-labeled choices failing to add an `evidence` tally, plus a `p_rescue` destination shown as `단서 찾기`, remains an objective alignment finding; second loop used local `fallback=1&image=0`, so deployed subjective feel is still open.
- **2026-07-19 Harness V2 OBJECTIVE QA LIVE-CALIBRATED (6/6 assertions)**: image arrival, companion join, exact party distribution, cutscene single-entry/return, choice arrival, and first-use gloss all have required PASS bundles with hashed screenshots/events. Eight required runs (choice 3 + five remaining) produced 0/8 observed false accepts; the authoritative six-bundle set took 507.7s/84.6s mean. Reporting retains the prior DB exception and samples 2/9 clean bundles, omitting 7. Formal auto-close remains **0/16** until three distinct release bundles confirm calibration.
- **2026-07-17 DEPLOYED `mythos-api-00076-jhc` (`make check` 1125; 100% traffic)** — B4 removes a false stat-check affordance: model-authored `(민첩 판정)` / `(Agility check)` tails render as narrative `[접근: 민첩]` / `[Approach: Agility]`; mechanics are unchanged. Local non-fallback Ollama rendered EN/KO with no clipping; live root + `/api/v1/health` return 200 and live/local `app.js` SHA-256 matches. Model pins preserved.
- **2026-07-17 DEPLOYED `mythos-api-00075-r5m` (`make check` 1124; 100% traffic)** — §3 deterministic same-seed style probe diverges people/help (`humanity_first`, Safe Refuge 5) from safety/evidence (`insight_focus` + `stability_focus`, Code Rewrite 4). Tied boss-perspective scores now choose authored `default_perspective` instead of array-first `p_rewrite`, locked by a Neo-Seoul boss regression test. Root app complete with browser errors 0; health 200. Human dull/sensitive balance verdict still requires two live loops.
- **2026-07-17 DEPLOYED `mythos-api-00074-q4w` (`make check` 1123; 100% traffic)** — second portrait-density pass removes the rotate nudge and legend popup, folds the encounter goal over the board top, replaces four core-action emoji with SVG assets, renders six existing skill PNGs in 50px slots, and fits targets/actions/skills/items inside the 30dvh shelf with no inner scroll. 390×844 touch probe: dock `clientHeight=scrollHeight=252`, goal visible at y=7, 4/4 action SVG + 6/6 skill PNG loaded, console errors 0. Live health/root + four SVG assets return 200. Evidence: `outputs/live-qa/manual-20260717-mobile-combat-density/`.
- **Owner real-device verdict (2026-07-17): §2 portrait combat feel OK** on `00074-q4w`; combat-density lane closed.
- **2026-07-15 (`make check` 1094; DEPLOYED `mythos-api-00068-76m`)** — live loop images failed with `404 NOT_FOUND` because `gemini-3.1-flash-image` was unavailable in this project/`us-central1` (not quota); a direct `gemini-2.5-flash-image` probe returned image bytes, so source/deploy defaults were pinned to 2.5 and deployed at 100% traffic. Watch the next real-loop asset record for app-path confirmation.
- **2026-07-14 session #20 (`make check` 1094; deploys `00064`→`00067`)** — key-beat hybrid env flipped + routing live-verified on `00066-blc` (key-beat 4/4 → 3.5, normal 1/1 → 2.5); 3.5 streaming whitespace-runaway silent fallback fixed (non-streaming retry + raw-evidence warning); **Neon idle-reap turn swallow fixed** (`456b522` pool checkout check + transaction pre-ping; probe 3/3 RECOVERED, `PostgresConnectionReapTest` locked) and deployed on `00067-x4d`. Protocol/rollback: `bin/docs/plans/2026-07-14-keybeat-hybrid-ab.md`.
- **2026-07-14 session #19 (DEPLOYED `00060`→`00063`; owner QA PASS)** — enemy art 20/20 codex regen, telegraph/spawn-variety fixes, balance verdicts, two-tier slice 4 turn-order strip, portrait action dock. Compressed in `COMPLETED_SUMMARY.md` M60.
- Earlier per-session baselines (07-06..07-13 deploys `00044`→`00059`): `bin/docs/archive/progress-2026-07.md` + `COMPLETED_SUMMARY.md` M35-M60.

## Active Focus

Authority plan: `docs/NEXT_PLAN.md`. Direction remains global-first EN/KO closed beta: Gemini/Vertex is the product path; Ollama/FLUX remains the local development path.

1. **§3 human re-sign-off — agent work exhausted.** The arm is complete, audited 47/47 zero-fallback, banked, and its evidence packet is written (`outputs/evals/20260808-owner-review/`). Only the **owner's ending/overall verdict** and the ruling on the two-build generation split remain; rubric scoring and `SPLIT.json` registration follow that ruling. Four fixes are committed but undeployed on purpose — deploying first would desync what the owner judges.
2. **Serving-research track (new 2026-08-30, unblocked).** MythOS as a workload for serving experiments — explicitly **not** production self-hosting, which the evidence puts far out of range (`docs/reference/2026-08-30-self-hosted-inference-and-mythos-as-research-platform.md`). Plan `docs/plans/2026-08-30-mythos-as-serving-research-workload.md`: P0 done, P1–P3 need no owner decision; P4 is the judge-noise decision the promotion track already carries.
3. **Manual/authority hold**: remote Harness publication, repair enablement, and read-only P2 fan-out remain owner-gated; S4 copy tone, variant intros, G2 twist tone, EN fresh-loop coherence and Glass Library remain manual/held.

## Open Risks

- **Local narrative path: the sampler was inert until 2026-08-30.** `OllamaJSONProvider` sent six options in `extra_body["options"]`, which the OpenAI-compatible endpoint discards — so `repeat_penalty`/`repeat_last_n` (the repetition tuning), `top_p`, `top_k`, `num_predict` and `num_ctx` had never applied. Fixed via `mythos_narrative/engine_options.py` (per-engine translation; `repeat_penalty` → the measured-working `frequency_penalty`; `top_k` undeliverable and dropped explicitly); the 30s client timeout, shorter than a 12–90s generation, is now 180s. Degradation 44% → 0% on a 9-call arm. **Consequence: any local repetition judgement made before this date was made against a model that never received the setting.** Production (Vertex Gemini) is a separate path and unaffected. Residual: the `vllm`/`llamacpp`/`mlx` rows in `engine_options` are **unverified** — run `make experiment ARGS=option-matrix` against an engine before trusting it.
- **World-id unification (2026-09-05) changes what production players see next loop.** Readers now find the archives written since 06-06, so `_initial_loop_scores` applies `recent_archives_*` adjustments to existing players' next loop for the first time, and the memory overview lists their past archives. Expected, but a behaviour change to watch on the next arm; `_player_rollup`/compaction will also run for the first time on players with >retention archives.
- **Context length is a model property, not a setting.** The compat endpoint takes no per-request window; Ollama reads it from the Modelfile. `OLLAMA_NUM_CTX` was removed rather than left as a knob that looks live and does nothing. A model whose window cannot hold ~7.8k prompt + 2k generation returns empty scenes that the fallback hides — pick a `*-64k` variant.
- **Harness/Dev Graph watch items**: local tag/cache is 1.4.0 while remote tag/marketplace remains 1.2.0. Turn excess now rejects acceptance but cannot stop the running Claude invocation; wall/USD caps remain mandatory. Held-out composition is ratified, but repair (`OVERNIGHT_REPAIR=0`) stays off until the frozen single-actor baseline is complete; read-only fan-out still requires explicit authorization. Cross-engine critic has one disagreement datum.
- **Image provider migrated 2026-07-31**: the former 3.1 404 was reproduced only at `us-central1`; the official `global` endpoint produced a real 1024² probe and two successful app-path production assets on `00080`/`00081`. Default/deploy now pin `IMAGEN_LOCATION=global`; 429 and safety-filter empties remain watch items.
- **3.5 whitespace runaway**: streaming controlled generation can emit trailing-whitespace runaway → JSON truncation; a same-model non-streaming retry is live (`b55e933`). Watch `streamed payload unparseable` warning frequency in prod logs — 07-19 audit: **0 occurrences in 5 days** (light traffic caveat).
- **Play-style consequence alignment — code-side RESOLVED and live since `00077-8g9`** (junction-pick axis accrual + destination-derived chips, regression-locked; see PROGRESS_LOG). Residual: axis-intent threshold 2 remains tunable; the 2026-07-29 deployed §3 verdict is HOLD because the contrast does not survive late-loop repetition.
- **Production narrative fallback watch (2026-07-28)**: excluded safety/evidence `loop_f148…2350` recorded 3/15 fallback; replacement `loop_84df…33ef` passed 47/47. Continue the full Cloud Logging zero-fallback audit before banking every future production sample.
- **Repetition — reviser fixed, upstream open (2026-08-08)**: the 07-31 enforcement was itself the largest repetition source. Its motif detector carried the scenario's premise vocabulary ("signal"/"grid") and fired on 91% of turns, and its single fixed template produced 21 `New Vector at …` titles, 13 of which then re-triggered the repeated-title signal. Both are fixed and locked (streak 91%→59%, revisions now rotate). Residual: the model itself repeats locations/motifs (title 32%, location streak 33%) — narrative prompt work, gated on the owner's §3 verdict. The identical `Patrol Ambush` encounter is also fixed: the downgrade picked the highest-weight affordable entry and the risk cap counts only combats *won*, so a player who never wins was locked to the single tier-1 encounter. Residual closed 2026-08-08: tier 1 now authors four encounters, measured 20/20 ambient beats served on the never-wins path (was 1 + 19 skips).
- **EN localization — CLOSED 2026-08-08**: all three banked EN transcripts (209 scenes) now localize to 0 Hangul, locked by six tests. The earlier "34/59 scenes" figure measured the banked file, which bypasses the serving boundary — the true player-facing residue was 11/59, fixed by backfilling 21 glossary entries the EN overlay already had plus a Hangul-isolated short-key pass. The ended-run `summary_text` was a separate fault (Korean-only deterministic path plus an LLM prompt hard-coded to Korean) and is now language-threaded with internal state tokens suppressed. Residual watch: the `획득` chip and `[8시 40분 54초]` timestamps did not reproduce in the served form — re-check on the next arm.
- **Runaway trickle was a QA-harness artifact, not a product defect (2026-08-08)**: hidden-tab timer throttling slowed the client reveal to 5–31 chars/min while the server finished the same turn in 7–26s; making the tab visible jumped it to 2,147 chars/min. **Do not build the previously planned server-side stream-stall watchdog on this evidence.** Automated play must keep the tab foregrounded or reload to resync.
- **Value-axis and objective display gaps (2026-08-08) — CLOSED 2026-08-09.** Every reported symptom turned out to be a deterministic defect, not authoring: the clue counter (the store's default `LIMIT 8` plus an ignored shard kind, feeding both the CLUE MATRIX gauge and the displayed Insight), combat-scene locations (raw encounter ids reaching `final_location`/save slots/image overlay), the same-axis choices and the evasion action labelled `Help people` (one Korean-only vocabulary collapsing onto an inventing fallback), the IX line on Han's portrait (a possessive guard rejecting `X's voice`), and `CURRENT OBJECTIVE` (desktop had no `objective || chapter_goal` fallback). `saved: []` was **not** a defect — an ally join is not one of the five authored `saved_by_flag` rescue flags. Residual: STATUS `Location` slugs on model-authored scenes only — prompt work, gated on the §3 verdict.
- **Fresh production fallback + repetition watch (2026-08-02)**: `loop_fde2…fec34` excluded at 45/47; `loop_8b7a…60dc2` excluded at 13/14. The normal-turn retry is now live on `00082-ffc` — watch `streamed payload unparseable, retrying non-streaming` warnings (finally reachable) and retry cost/latency on the next arm. Novelty-reviser `New Vector at …` template repetition and the 3× identical `Patrol Ambush` encounter/copy are watch items; the AMP-shard modal 409 wedge is fixed on `00083-jt7` (stale re-open itself can still occur once — it now self-closes on the next click).
- **Companion variant-loop thread closed 2026-07-10** (present = `unlock_flags∩flags` unified across all surfaces): owner clean-loop retest pending; in-flight/legacy loops need a new loop or one-time DB cleanup.
- **fallback = static combat is intended (2026-06-06 design)**: combat VFX only play at `?fallback=0`; open the sim WITHOUT `?fallback=1` to see animations.
- **ally-writeback**: fixed 2026-07-09 (`efa1c8f`, no more auto-promotion of story-flag allies); residual = one-time DB cleanup of players promoted by the old bug, on request.
- **agy browser attach flaky**: intermittent silent hang acquiring browser tools; gtimeout ceiling + verdict-rescue landed. Run live-QA direct from a supervising session; if attach fails twice, hand to human.
- **3.5-flash cost accepted (~$1.0/loop)**: hybrid rolled back 2026-07-17 (quality verdict); partial-2.5 audit found no viable spot (2 LLM touchpoints; thinking already 0). Remaining levers: prompt-cache hit monitoring · prompt diet round 2 (feel-sensitive, deliberately held) · re-evaluate at scale with golden-bank rubric scores.
- DB hygiene (low): `narrative_shards` raw rows retained post-rollup; prune only if size/query load bites.
- Some dated plan files may have stale status headers. Prefer `STATUS.md`, `NEXT_PLAN.md`, `PROGRESS_LOG.md` for current truth.
- Combat image quality varies per character. Prefer an action-sheet-based pipeline over independent pose generation.

## Source Of Truth

- Agent entry: `docs/AGENT_BRIEF.md`
- Architecture summary: `docs/DESIGN.md`
- Rolling plan: `docs/NEXT_PLAN.md`
- Latest short log: `docs/PROGRESS_LOG.md`
- Completed milestones: `docs/COMPLETED_SUMMARY.md`
- Decisions: `docs/DECISIONS.md`
- Long logs/design: `bin/docs/archive/`
