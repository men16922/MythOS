# Project MythOS - React + TS SPA 플레이 검증 체크리스트

이 문서는 Vite + React + TypeScript 기반 웹 SPA 클라이언트의 동작 정합성을 실제 플레이(Manual Play)로 최종 검증하기 위한 가이드라인입니다. 수동 플레이는 기본적으로 `make dev-up` 또는 `make api`로 띄운 `http://localhost:8000`을 사용합니다. 자동 E2E 스크립트는 테스트 전용 uvicorn 서버를 `http://127.0.0.1:8080/?fallback=1&image=0`에 임시로 띄웁니다.

---

## 최근 수동 QA 메모

2026-06-06 `make dev-up` 경로:

*   [x] 기본 접속: 부트 오프닝, 온보딩, 시나리오/아키타입 선택, 새 게임 시작 성공.
*   [x] 이어하기: 새로고침/복원 경로에서 기존 루프 복원 성공.
*   [/] 서사 흐름: 장면 전환 및 장면별 `내 행동` 기록 성공. 서사 기록 오버레이 진입점은 한 장면 기록만 있어도 보이도록 UI 수정 후 재확인 필요.

---

## 1. 온보딩 및 세션 생명주기 검증 (S1)

*   [~] **접속 세션 시작** (자동 E2E 일부 검증):
    *   [x] 이름 입력 칸에 텍스트가 정상 입력되는가? (기본값: `테스터`)
    *   [x] 시나리오 선택 드롭다운에서 다른 시나리오를 선택했을 때 하단의 아키타입 카드 목록이 즉각 갱신되는가?
    *   [x] 아키타입 카드를 클릭했을 때 연청색 테두리(`sel` 클래스)가 옮겨지며 선택 완료 상태가 되는가?
    *   [x] `fallback` 및 `이미지` 체크박스 상태가 정상 토글되는가?
    *   [x] `접속 · 루프 시작` 버튼을 눌렀을 때 오류 없이 2층 메인 대시보드로 자연스럽게 트랜지션되는가?
*   [x] **자동 이어하기 감지** (자동 E2E 검증):
    *   [x] 플레이 중인 브라우저를 새로고침하거나 `접속 종료` 버튼을 누르고 메인으로 복귀했을 때, 하단에 `이어하기 · [플레이어 ID]...` 버튼이 활성화되는가?
    *   [x] `이어하기` 클릭 시 기존 세이브 포인트의 씬 및 6종 게이지 상태가 그대로 보존된 채 서사 접속 탭으로 로드되는가?
*   [ ] **루프 종료 (Phase: Ended)**:
    *   체력이 다해 패배하거나 클라이맥스 엔딩 플래그에 도달했을 때 choices 카드들이 언마운트되는가?
    *   `여정 종료` 상태 배너에 획득한 엔딩의 식별 라벨(예: `엔딩 · 유리성의 사서`)이 일관되게 출력되는가?
    *   하단에 `새 접속 ▸` 버튼이 활성화되어 클릭 시 메인 온보딩 폼으로 복귀되는가?

---

## 2. 내러티브 스트리밍 및 타이프라이터 렌더러

*   [ ] **가속 타입라이터**:
    *   WebSocket 토큰 수신 시 `narration` 지문이 밀리지 않고 실시간 스트리밍으로 출력되는가?
    *   긴 문장일수록 청크 슬라이싱이 빨라져 답답함 없이 타이핑 효과가 완결되는가?
    *   텍스트 출력 중에 실시간으로 깜빡이는 블록 캐럿(`▌`)이 화면 끝에 유지되는가?
*   [~] **스트림 완료 트리거** (자동 E2E 검증):
    *   [x] 텍스트 출력이 완전히 끝나면 캐럿(`▌`)이 즉시 제거되고 그 아래 선택지(Choices) 혹은 전투 컨트롤이 자연스럽게 나타나는가?
    *   [x] 출력이 진행 중일 때는 하단 액션 버튼 노출이 철저히 잠금 처리되는가?
*   [ ] **핫키 커맨드**:
    *   키보드 숫자 패드 `[1] ~ [9]`를 눌렀을 때, narration 완료 조건 하에서 각각 해당하는 choices 카드가 자동 클릭되는가?
    *   입력 필드(`display-name`, `save-label` 등)에 커서가 활성화되어 있을 때는 핫키가 트리거되지 않도록 차단 처리되는가?

---

## 3. 자원 제약 및 행동 패널티 검증 (P2)

*   [ ] **선택지 요구 조건 (Requires)**:
    *   안정성(Stability)이 부족하거나 긴장도(Tension)가 한계를 넘은 상황에서 관련 요구조건(예: `[필요 안정성: 35]`)이 걸린 choices 카드가 불투명도 50% 수준으로 흐려지며 `disabled` 처리되는가?
*   [ ] **자원 비용 차감 (Cost)**:
    *   설명문 우측에 자원 소모치(예: `(안정성 -10, 긴장도 +5)`)가 노출되는가?
    *   해당 버튼을 눌러 다음 루프 턴으로 이행 시, aside의 STABILITY/TENSION 프로그레스 바 너비 및 수치가 올바른 기댓값으로 즉각 감산/가산 갱신되는가?

---

## 4. 전술 전투 Canvas 조작 및 로스터 (P3/S4)

*   [ ] **전술 전투 캔버스 그래픽 렌더링**:
    *   전투 씬 진입 시 격자무늬 보드판(`cols=8, rows=6`)이 캔버스 위에 디바이스 픽셀 비율(DPR)을 고려해 선명하게 그려지는가?
    *   아군 유닛(cyan), 동료 유닛(green), 적군 유닛(red) 블리(Dot)들이 이름 텍스트 및 하단 HP 비율 바와 함께 정상 드로잉되는가?
    *   현재 액티브 상태인 유닛의 Dot 외곽에 굵은 노란색 어텐션 링이 그려지는가?
    *   각 유닛이 방어 태세 시 겉면을 감싸는 쉴드 아크 홀더가 그려지는가?
*   [ ] **적 인텐트 가시화 (Into the Breach)**:
    *   적군 유닛 주변에 다음 턴 취할 행동(칼 아이콘 ⚔️ 및 타격 대상 타일 외곽 점선 렌더, 도주 🏃 또는 이동 👣)의 디테일 뱃지가 드로잉되는가?
*   [ ] **보드 클릭 이동 조작**:
    *   플레이어 유닛 턴 시 보드판 위에 연청색 반투명 사각형 격자(reachable 이동 가능 범위)가 오버레이되는가?
    *   그중 한 칸을 마우스로 클릭했을 때 즉각 `wait(이동)` 행동이 REST API를 타고 백엔드로 발송되어 해당 좌표로 블립이 순간이동하는가?
*   [ ] **동료 AI 자동 전략 (Shadowrun)**:
    *   로스터에 세린(Se-rin)이 있을 때 플레이어 체력이 낮아지면 세린이 턴을 잡아 자동으로 아군에게 `엄호 노이즈` 실드를 씌워주는가? (로그 확인)
    *   카이(Kai)가 턴을 잡았을 때 플레이어 근처의 적을 표적으로 삼아 `과부하 일격` 스킬을 정상 시전하는가?
*   [ ] **전술 전투 로그 및 정산**:
    *   아래 접이식 details 패널 혹은 전투 전용 로그 보드에 최신 로그가 타임스탬프(`[시:분:초]`)와 함께 위로 쌓이는가?
    *   전투가 종결(Outcome)되었을 때 `교전 종료 — 승리/도주 성공/패배` 정보가 적합한 배너 색상(패배 시 붉은색)으로 표시되며 후속 이행 버튼(`계속` 또는 `새 루프 시작`)이 정상 동작하는가?

---

## 5. Codex 기억의 별자리 및 슬롯 보관소 (S2/S3)

*   [x] **기억의 별자리 Codex** (자동 E2E 검증):
    *   [x] 획득한 단서(Clues) 리스트, 로어(Lore) 해금 내역, 인벤토리 아이템 및 기시감(Echoes)이 누락 없이 동적 카드로 정렬되는가?
*   [x] **세션 수동 SAVE/LOAD** (자동 E2E 검증):
    *   [x] 원하는 세션 설명(레이블)을 입력하고 `SAVE` 버튼을 눌렀을 때, 우측 저장 목록 슬롯에 로컬 타임 시간대와 함께 갱신되는가?
    *   [x] 임의의 슬롯 옆 `LOAD` 버튼을 클릭했을 때 저장 시점의 씬과 자원으로 즉시 롤백 복구되는가?
*   [ ] **여정 기록 보관소 (Run History)**:
    *   과거에 종료(Ended)되었던 완료 루프 히스토리(Turns 및 획득한 최종 엔딩 식별 레이블)가 리스트에 점선 형태 테두리로 안전하게 유지/출력되는가?

---

## 6. 개발자 모니터 & 시네마틱 오디오 (S5/S6)

*   [x] **Developer 디버그 탭** (자동 E2E 검증):
    *   [x] 4대 메트릭(Humanity, Insight, Resilience, Dominance) 스코어가 flags의 정규식 패턴 점수와 일치하게 계산되는가?
    *   [x] 현재 시나리오의 전체 endings 조건들이 통과(`Active`) 되었는지, 대기 중(`Condition`) 인지 매칭 트리거가 실시간 반영되는가?
*   [ ] **오디오 BGM/SFX**:
    *   씬 스냅샷에 명시된 `bgm_path` 자산 루프 트랙이 오토플레이 차단 없이 사용자 첫 클릭 상호작용 후 정상 loop 재생되는가?
    *   전투 시 공격/방어/이동 버튼 클릭 시 전용 SFX 사운드 파일이 로드/재생되는가?
*   [ ] **오프닝 연출**:
    *   `turn_index=0` 진입 시 이미지 위에 `kenburns-active` 효과가 붙어 서서히 줌인/줌아웃 카메라 무빙이 들어가며, 이미지 프레임 자체가 3.2초 동안 노이즈 글리치(`glitch-active`) 렌더 필터를 타는가?

---

## 7. 플레이라이트 자동 E2E 테스트 검증

웹 SPA 클라이언트의 핵심 접속 및 턴 진행 흐름을 자동으로 시뮬레이션하고 검증하기 위해 Playwright 기반의 E2E 테스트 환경이 구축되어 있습니다. 자동 E2E는 `scratch/run_playwright_test.py`가 테스트 전용 서버를 `http://127.0.0.1:8080/?fallback=1&image=0`에 띄워 실행합니다. 수동 테스트에 앞서 이 자동 검증 도구를 통해 기본적인 빌드와 WebSocket 데이터 흐름의 건전성을 빠르게 체크할 수 있습니다.

최근 검증:

*   [x] 2026-06-06 `make test-e2e` 통과.
*   [x] `outputs/e2e_react_play.png` 및 `outputs/e2e_react_play_turn1.png` 스크린샷 생성/육안 확인.
*   [x] 실패 시 non-zero exit 및 `outputs/e2e_failure.png` 진단 캡처 경로 확인.
*   [~] 자동 E2E는 `?fallback=1&image=0` 결정적 경로를 사용하므로 한국어 LLM 출력 품질과 실 이미지 UX는 별도 수동 플레이에서 확인 필요.

### 7.1. 선행 요구 조건 및 드라이버 설치
테스트를 실행하기 전에 Playwright 라이브러리와 Chromium 브라우저 바이너리가 필요합니다.
```bash
# dev 의존성 설치 및 playwright 드라이버 설치
make setup
.venv/bin/playwright install chromium
```

### 7.2. 테스트 자동 실행 방법
프로젝트 루트에서 다음 명령어를 통해 백그라운드 FastAPI 서버 기동 및 웹 브라우저 E2E 시나리오를 원클릭으로 검증할 수 있습니다.
```bash
make test-e2e
```
또는 직접 파이썬 스크립트를 지정해 구동할 수 있습니다.
```bash
.venv/bin/python scratch/run_playwright_test.py
```

---

## 8. 2026-06-06 세션 2 신규 기능 검증 (미커밋 배치)

아래는 세션 2에서 추가/변경된 기능들의 수동 검증 항목이다. (상세 변경: `docs/PROGRESS_LOG.md` 최상단 "세션 2")

*   [ ] **첫 진입 부트 오프닝 (BootIntro)**:
    *   앱 최초 진입 시 PROJECT MYTHOS 로고 + 타이핑되는 부트 라인 + 키아트 + 시그널 게이트가 뜨고, `접속 기동 · ENTER ▸`(또는 화면 클릭)로 온보딩으로 넘어가는가?
    *   세션 종료 후 메인 복귀 시에는 부트 오프닝이 다시 뜨지 않는가? (첫 진입 1회만)
*   [ ] **이어하기 활성 루프 선택**: 직전 루프가 종료(ENDED)된 상태로 `이어하기`를 누르면 409 대신, 활성 루프가 없으면 안내 후 온보딩으로 정리되는가?
*   [ ] **CHARACTER 패널 분기**: 주변 인물 없는 장면은 내 정보(스탯/속성/인벤토리), 세린/카이 등 대화 상대가 등장한 장면은 해당 인물 portrait+별칭/이름/역할이 표시되는가?
*   [ ] **장면 이미지 얼굴 일관성 (Redux)**: 캐릭터(세린)가 등장한 장면 이미지의 얼굴이 CHARACTER 포트레이트와 같은 인물로 보이는가? (이전엔 딴 사람)
*   [ ] **오프닝→첫 장면 연속성**: 신규 게임에서 세린 시네마틱 직후 첫 장면이 비 오는 C-17 골목/세린으로 자연스럽게 이어지는가(엉뚱한 변전소로 리셋되지 않음)?
*   [ ] **서사 기록 별도 화면**: 본문에는 직전 장면 1개만 보이고, `📜 이전 기록 전체 보기 (N)` 클릭 시 오버레이에서 전체 장면+`내 행동`이 보이는가?
*   [ ] **장면별 행동 기록**: 과거 장면 블록(인라인/오버레이)에 `▸ 내 행동: …`가 표시되고, 이어하기 후에도 복원되는가?
*   [ ] **전투 드래그&드롭**: 내 캐릭터를 집어(grab) 밝은 칸에 놓으면(drop) 이동하는가? 클릭만으로 순간이동하지 않는가?
*   [ ] **전투 패배→메인**: 패배 시 버튼이 `메인 화면으로 ▸`이고, 클릭하면 새 루프가 아니라 메인(온보딩)으로 복귀하는가?
*   [ ] **이미지 생성 속도**: 장면 이미지가 1024가 아닌 512로 생성되어 체감 지연이 줄었는가(워밍 후 ~8초)? 비동기로 텍스트는 막히지 않는가?

---

### 7.3. 자동 E2E 시나리오 검증 항목
`scratch/run_playwright_test.py` 스크립트는 다음 단계를 자동으로 밟아가며 각 요소의 정합성을 검증하고 스크린샷 결과물을 아카이브합니다.

*   [x] **FastAPI 로컬 서버 구동 및 접속**:
    *   127.0.0.1:8080 포트로 백그라운드 uvicorn API 서버 프로세스를 안전하게 바인딩하고 대기하는가?
    *   Playwright Headless Chromium 브라우저가 해당 주소로 진입했을 때 정상 페이지 로드가 진행되는가?
*   [x] **온보딩 인터랙션 자동 입력**:
    *   플레이어 이름 입력창(`#display-name`)을 찾아 `"플레이라이트봇"` 문구를 타이핑해 넣는가?
    *   첫 번째 아키타입 카드(`.arch-card:first-child`)를 클릭하여 직업을 Netrunner로 지정하는가?
    *   `#start`("접속 · 루프 시작") 버튼을 정상 트리거하는가?
*   [x] **대시보드 트랜지션 및 내러티브 스트리밍 대기**:
    *   게임 화면 레이아웃(`#play`)이 정상 로드될 때까지 브라우저 화면 요소를 감지하고 대기하는가?
    *   WebSocket 통신을 타고 넘어오는 타이프라이터 효과가 종료되어 선택지 버튼(`#choices button`)이 보드 위에 노출될 때까지 안전하게 스트리밍 타임아웃을 통제하며 기다리는가?
*   [x] **스크린샷 스냅샷 수집**:
    *   턴 0 (시작 씬)의 렌더링 상태를 `outputs/e2e_react_play.png` 파일로 안전하게 기록하는가?
    *   첫 번째 선택지(`#choices button:first-child`)를 자동 클릭해 API 및 씬 상태 전환 요청을 송신하는가?
    *   턴 1 씬 전환 완료 후, 갱신된 화면을 `outputs/e2e_react_play_turn1.png` 파일로 2차 기록하는가?
*   [x] **프로세스 해제**:
    *   검증 성공 후 브라우저 자원을 닫고 백그라운드 FastAPI 서버를 `terminate -> join(timeout) -> kill fallback`으로 누수 없이 안전하게 회수하는가?
