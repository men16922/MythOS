# Project MythOS - React + TS SPA 플레이 검증 체크리스트

이 문서는 Vite + React + TypeScript 기반 웹 SPA 클라이언트의 동작 정합성을 실제 플레이(Manual Play) 및 자동 E2E 테스트로 검증하기 위한 가이드라인입니다. 수동 플레이는 기본적으로 `make dev-up` 또는 `make api`로 띄운 `http://localhost:8000`을 사용합니다. 자동 E2E 스크립트(`tests/playwright/test_e2e_play_checklist.py`)는 테스트 전용 uvicorn 서버를 `http://127.0.0.1:8080/?fallback=1&image=0`에 임시로 띄워 핵심 플레이 경로의 정합성을 검증합니다.

---

## 최근 수동 QA 메모

2026-06-06 Playwright 기반 자동 E2E 테스트 및 수동 QA 검증 완료:

*   [x] 기본 접속: 부트 오프닝, 온보딩, 시나리오/아키타입 선택, 새 게임 시작 성공.
*   [x] 이어하기: 새로고침/복원 경로에서 기존 루프 복원 성공.
*   [x] 도입부 연출: 시네마틱 도입 오프닝 창(`session_intro`) 및 수락 처리 성공.
*   [x] 서사 흐름: 장면 전환, 장면별 `내 행동` 기록, `📜 서사 기록 전체 보기` 버튼 및 오버레이 확인.
*   [x] 텍스트 스트리밍: 체감 속도 양호.
*   [x] 자원 제약: 요구조건 미달 시 버튼 비활성화, 비용 표기 및 차감 감산 검증 완료.
*   [x] 캐릭터 패널: 현재 장면에 연동되어 대화 상대(정세린)의 별칭, 이름, 역할 정상 연동 확인.
*   [x] 전술 전투: cols=8, rows=6 캔버스 렌더링, 캐릭터 드래그앤드롭 이동 처리, 패배 시 결과 배너 노출 및 메인 화면 복귀 성공.
*   [x] 기록 보관소: 전투 패배 후 메인 온보딩 복귀 전, 기록 보관소 사이드바에 완료 루프 히스토리("유리성의 사서" 엔딩)가 안전하게 아카이빙되는 것 확인.
*   [x] Dev 탭 UI: 로컬 인프라 콘솔 세로 배치 확인. 개발자 콘솔은 동일 폭 카드 2열 그리드로 개선 완료.

---

## 1. 온보딩 및 세션 생명주기 검증 (S1)

*   [x] **접속 세션 시작** (자동 E2E 검증 완료):
    *   [x] 이름 입력 칸에 텍스트가 정상 입력되는가? (기본값: `테스터`)
    *   [x] 시나리오 선택 드롭다운에서 다른 시나리오를 선택했을 때 하단의 아키타입 카드 목록이 즉각 갱신되는가?
    *   [x] 아키타입 카드를 클릭했을 때 연청색 테두리(`sel` 클래스)가 옮겨지며 선택 완료 상태가 되는가?
    *   [x] `fallback` 및 `이미지` 체크박스 상태가 정상 토글되는가? (E2E 주입 파라미터 확인)
    *   [x] `접속 · 루프 시작` 버튼을 눌렀을 때 오류 없이 2층 메인 대시보드로 자연스럽게 트랜지션되는가?
*   [x] **자동 이어하기 감지** (자동 E2E 검증):
    *   [x] 플레이 중인 브라우저를 새로고침하거나 `접속 종료` 버튼을 누르고 메인으로 복귀했을 때, 하단에 `이어하기 · [플레이어 ID]...` 버튼이 활성화되는가?
    *   [x] `이어하기` 클릭 시 기존 세이브 포인트의 씬 및 6종 게이지 상태가 그대로 보존된 채 서사 접속 탭으로 로드되는가?
*   [x] **루프 종료 및 패배 복귀** (자동 E2E 검증 완료):
    *   [x] 전투 패배 시 텍스트 및 choices 카드가 적절히 언마운트/숨김 처리되고 전투 컨트롤 패널로 전환되는가?
    *   [x] `교전 종료 — 패배` 상태 배너(`.combat-outcome.lose`)가 일관되게 출력되는가?
    *   [x] 하단에 `메인 화면으로 ▸` 버튼(`#cc-return-main`)이 활성화되어 클릭 시 메인 온보딩 폼으로 복귀되는가?

---

## 2. 내러티브 스트리밍 및 타이프라이터 렌더러

*   [x] **가속 타입라이터**:
    *   [x] WebSocket 토큰 수신 시 `narration` 지문이 밀리지 않고 실시간 스트리밍으로 출력되는가?
    *   [x] 긴 문장일수록 청크 슬라이싱이 빨라져 답답함 없이 타이핑 효과가 완결되는가?
    *   [x] 텍스트 출력 중에 실시간으로 깜빡이는 블록 캐럿(`▌`)이 화면 끝에 유지되는가?
*   [x] **스트림 완료 트리거** (자동 E2E 검증):
    *   [x] 텍스트 출력이 완전히 끝나면 캐럿(`▌`)이 즉시 제거되고 그 아래 선택지(Choices) 혹은 전투 컨트롤이 자연스럽게 나타나는가?
    *   [x] 출력이 진행 중일 때는 하단 액션 버튼 노출이 철저히 잠금 처리되는가?
*   [x] **핫키 커맨드**:
    *   [x] 키보드 숫자 패드 `[1] ~ [9]`를 눌렀을 때, narration 완료 조건 하에서 각각 해당하는 choices 카드가 자동 클릭되는가? (자동 E2E 테스트 완료 - 수동 키다운 및 대시보드 클릭 트리거 우회 확인)
    *   [x] 입력 필드(`display-name`, `save-label` 등)에 커서가 활성화되어 있을 때는 핫키가 트리거되지 않도록 차단 처리되는가? (자동 E2E 검증 완료)

---

## 3. 자원 제약 및 행동 패널티 검증 (P2)

*   [x] **선택지 요구 조건 (Requires)** (자동 E2E 검증 완료):
    *   [x] 안정성(Stability)이 부족하거나 긴장도(Tension)가 한계를 넘은 상황에서 관련 요구조건(예: `[필요 안정성: 35]`)이 걸린 choices 카드가 불투명도 50% 수준(opacity 0.6 이하)으로 흐려지며 `disabled` 처리되는가?
*   [x] **자원 비용 차감 (Cost)** (자동 E2E 검증 완료):
    *   [x] 설명문 우측에 자원 소모치(예: `(안정성 -10, 긴장도 +5)`)가 노출되는가?
    *   [x] 해당 버튼을 눌러 다음 루프 턴으로 이행 시, STABILITY/TENSION 프로그레스 바 수치가 올바른 기댓값으로 즉각 감산/가산 갱신되는가? (UI 데이터 연동 확인)

---

## 4. 전술 전투 Canvas 조작 및 로스터 (P3/S4)

*   [x] **전술 전투 캔버스 그래픽 렌더링** (자동 E2E 일부 검증):
    *   [x] 전투 씬 진입 시 격자무늬 보드판(`cols=8, rows=6`)을 그리는 `<canvas id="combat">` 태그가 돔 상에 정상 렌더링되는가?
    *   [x] 아군 유닛, 동료 유닛, 적군 유닛 블립 및 체력 바의 선명도와 그래픽 렌더링 품질 (검증 완료)
*   [x] **적 인텐트 가시화 및 동료 AI 자동 전략 (Shadowrun)** (검증 완료):
    *   [x] 적 유닛 주변 행동 디테일 뱃지 렌더링 및 동료 캐릭터(세린, 카이)의 스킬 자동 시전 정상 작동 여부
*   [x] **보드 드래그앤드롭 이동 조작** (자동 E2E 검증 완료):
    *   [x] 마우스로 캐릭터 블립을 드래그하여 이동 가능한 칸(Reachable)으로 옮겼을 때, `POST /api/v1/combat/action` API 호출이 트리거되며 해당 좌표로 정상 이동하는가?
    *   [x] 단순 클릭만으로는 이동 액션이 트리거되지 않느가?
*   [x] **전술 전투 로그 및 정산** (자동 E2E 검증 완료):
    *   [x] 전투가 패배로 종결되었을 때 `교전 종료 — 패배` 정보가 붉은색 패배 배너(`.combat-outcome.lose`)로 표시되고 `메인 화면으로 ▸` 버튼이 활성화되는가?

---

## 5. Codex 기억의 별자리 및 슬롯 보관소 (S2/S3)

*   [x] **기억의 별자리 Codex** (자동 E2E 검증):
    *   [x] 획득한 단서(Clues) 리스트, 로어(Lore) 해금 내역, 인벤토리 아이템 및 기시감(Echoes)이 누락 없이 동적 카드로 정렬되는가?
*   [x] **세션 수동 SAVE/LOAD** (자동 E2E 검증):
    *   [x] 원하는 세션 설명(레이블)을 입력하고 `SAVE` 버튼을 눌렀을 때, 우측 저장 목록 슬롯에 로컬 타임 시간대와 함께 갱신되는가?
    *   [x] 임의의 슬롯 옆 `LOAD` 버튼을 클릭했을 때 저장 시점의 씬과 자원으로 즉시 롤백 복구되는가?
*   [x] **여정 기록 보관소 (Run History)** (자동 E2E 검증 완료):
    *   [x] 과거에 종료(Ended)되었던 완료 루프 히스토리(Turns 및 최종 엔딩 "유리성의 사서")가 우측 사이드바의 `#history-panel` 내에 점선 테두리 카드로 안전하게 로드되어 출력되는가?

---

## 6. 개발자 모니터 & 시네마틱 오디오 (S5/S6)

*   [x] **Developer 디버그 탭** (자동 E2E 검증):
    *   [x] 4대 메트릭(Humanity, Insight, Resilience, Dominance) 스코어가 flags의 정규식 패턴 점수와 일치하게 계산되는가?
    *   [x] 현재 시나리오의 전체 endings 조건들이 통과(`Active`) 되었는지, 대기 중(`Condition`) 인지 매칭 트리거가 실시간 반영되는가?
*   [x] **오프닝 도입부 시네마틱** (자동 E2E 검증 완료):
    *   [x] 신규 세션 시작 즉시 `session_intro` 도입 연출 인트로 창(`<IntroPanel>`)이 렌더링되며 접속 수락 버튼(`.intro-accept-btn`)이 작동하는가?
*   [x] **오프닝 렌더러 특수효과 및 오디오 BGM/SFX** (검증 완료):
    *   [x] 이미지 위에 `kenburns-active` (줌인/줌아웃) 및 노이즈 글리치(`glitch-active`) 렌더 필터가 시각적으로 자연스럽게 적용되는가?
    *   [x] 씬 스냅샷의 `bgm_path` 및 액션 시 SFX 사운드 파일이 브라우저에서 올바르게 로드되어 재생되는가?

---

## 7. 플레이라이트 자동 E2E 테스트 검증

웹 SPA 클라이언트의 핵심 플레이 룰셋과 복잡한 상태 천이를 자동으로 시뮬레이션하기 위해 Playwright 기반의 통합 E2E 테스트가 구축되어 있습니다. 

*   **테스트 스크립트**: [test_e2e_play_checklist.py](file:///Users/men1692/Desktop/local/MythOS/tests/playwright/test_e2e_play_checklist.py)
*   **임시 테스트 서버**: `http://127.0.0.1:8080/?fallback=1&image=0`
*   **최근 검증**:
    *   [x] 2026-06-06 `test_e2e_play_checklist.py` 통과 및 exit code 0 확인.
    *   [x] 테스트 단계별 9개의 스크린샷 결과물이 [tests/playwright/outputs/](file:///Users/men1692/Desktop/local/MythOS/tests/playwright/outputs/) 경로에 안전하게 아카이브되었음을 육안 확인.
    *   [x] 예외 상황 발생 시 돔 분석용 HTML (`e2e_failure_diagnostics.html`) 및 진단 스크린샷 (`e2e_failure_diagnostics.png`) 캡처 경로 동작 확인.
    *   [~] 해당 자동 테스트는 결정론적(Deterministic) 가상 모킹 데이터를 사용하므로 실제 LLM(Ollama) 및 FLUX 이미지 생성 퀄리티는 수동 플레이를 병행하여 확인 필요.

### 7.1. 선행 요구 조건 및 드라이버 설치
테스트를 실행하기 전에 Playwright 라이브러리와 Chromium 브라우저 바이너리가 필요합니다.
```bash
# dev 의존성 설치 및 playwright 드라이버 설치
make setup
.venv/bin/playwright install chromium
```

### 7.2. 테스트 자동 실행 방법
프로젝트 루트에서 다음 명령어를 통해 백그라운드 FastAPI 서버 기동 및 웹 브라우저 E2E 시나리오를 원클릭으로 검증할 수 있습니다.
```bash
# E2E 플레이 검증 테스트 실행
.venv/bin/python -u tests/playwright/test_e2e_play_checklist.py
```

---

## 8. 2026-06-06 세션 2 신규 기능 검증

아래는 세션 2에서 추가/변경된 기능들의 검증 항목이다. (상세 변경: `docs/PROGRESS_LOG.md` 최상단 "세션 2")

*   [x] **첫 진입 부트 오프닝 (BootIntro)** (자동 E2E 검증 완료):
    *   [x] 앱 최초 진입 시 PROJECT MYTHOS 로고 + 타이핑되는 부트 라인 + 키아트 + 시그널 게이트가 뜨고, `접속 기동 · ENTER ▸`(또는 화면 클릭)로 온보딩으로 넘어가는가?
    *   [x] 세션 종료 후 메인 복귀 시에는 부트 오프닝이 다시 뜨지 않는가? (첫 진입 1회만)
*   [x] **이어하기 활성 루프 선택**: 직전 루프가 종료(ENDED)된 상태로 `이어하기`를 누르면 409 대신, 활성 루프가 없으면 안내 후 온보딩으로 정리되는가?
*   [x] **CHARACTER 패널 분기** (자동 E2E 검증 완료):
    *   [x] 주변 인물 없는 장면은 내 정보(스탯/속성/인벤토리), 세린/카이 등 대화 상대가 등장한 장면은 해당 인물 portrait+별칭/이름/역할이 표시되는가?
*   [x] **장면 이미지 얼굴 일관성 (Redux)** (검증 완료):
    *   [x] 캐릭터(세린)가 등장한 장면 이미지의 얼굴이 CHARACTER 포트레이트와 같은 인물로 보이는가? (이전엔 딴 사람)
*   [x] **오프닝→첫 장면 연속성** (검증 완료):
    *   [x] 신규 게임에서 세린 시네마틱 직후 첫 장면이 비 오는 C-17 골목/세린으로 자연스럽게 이어지는가(엉뚱한 변전소로 리셋되지 않음)?
*   [x] **서사 기록 별도 화면**: 본문에는 직전 장면 1개만 보이고, `📜 서사 기록 전체 보기 (N)` 클릭 시 오버레이에서 전체 장면+`내 행동`이 보이는가?
*   [x] **장면별 행동 기록**: 과거 장면 블록(인라인/오버레이)에 `▸ 내 행동: …`가 표시되고, 이어하기 후에도 복원되는가?
*   [x] **전투 드래그&드롭** (자동 E2E 검증 완료):
    *   [x] 내 캐릭터를 집어(grab) 밝은 칸에 놓으면(drop) 이동하는가? 클릭만으로 순간이동하지 않는가?
*   [x] **전투 패배→메인** (자동 E2E 검증 완료):
    *   [x] 패배 시 버튼이 `메인 화면으로 ▸`이고, 클릭하면 새 루프가 아니라 메인(온보딩)으로 복귀하는가?
*   [x] **이미지 생성 속도**: 장면 이미지가 1024가 아닌 512로 생성되어 체감 지연이 줄었는가(워밍 후 ~8초)? 비동기로 텍스트는 막히지 않는가?

---

### 7.3. 자동 E2E 시나리오 검증 항목
`tests/playwright/test_e2e_play_checklist.py` 스크립트는 다음 단계를 자동으로 수행하며 각 요소의 정합성을 검증하고 9개의 고품질 스크린샷 결과물을 아카이브합니다.

*   [x] **기본 리소스 서빙 및 온보딩 폼 진입**:
    *   [x] 부트 인트로 오프닝(`.boot-intro`) 노출 여부 및 진입 버튼 작동 검사 (`01_boot_intro.png`)
    *   [x] 온보딩 설정창(`#onboarding`) 활성화 및 닉네임("테스터"), 아키타입("Netrunner") 기본 매핑 검증 (`02_onboarding.png`)
*   [x] **도입부 시네마틱 및 대시보드 스트리밍**:
    *   [x] `session_intro` 시네마틱 도입 창(`.intro-accept-btn`) 대기 및 수락 버튼 검증 (`02.5_session_intro.png`)
    *   [x] 메인 게임 화면 레이아웃(`#play`) 및 WebSocket 턴 0 서사 텍스트 타이프라이터 효과 스트리밍 검사 (`03_dashboard_turn0.png`)
*   [x] **핫키 입력 차단 및 선택지 제약조건**:
    *   [x] 세션 저장창에 입력 포커스 시 단축키 숫자 `1`이 씹히고 글자가 제대로 입력되는지 예외 케이스 검증
    *   [x] 턴 1 진행 시 요구조건 불만족(Requires) 선택지의 비활성화(`disabled` 및 opacity 0.6 이하) 상태 검증 (`04_dashboard_turn1.png`)
    *   [x] 비용 표기 `(안정성 -10, 긴장도 +5)` 가 선택지 버튼 텍스트에 포함되어 있는지 검증
    *   [x] Interlocutor 정보(정세린, 물거미, 첫 인도자) 정상 바인딩 검증
*   [x] **전투 캔버스 및 드래그앤드롭 유닛 이동**:
    *   [x] 교전 씬 진입 시 격자무늬 전술 보드 `<canvas id="combat">` 렌더링 확인 (`05_combat_entered.png`)
    *   [x] 플레이어 유닛을 마우스 드래그를 이용해 (2,3) -> (3,3) 격자로 이동시켰을 때 전투 액션 API POST 트리거 검증 (`06_combat_moved.png`)
    *   [x] 단순 클릭 시에는 유닛 이동 명령이 전송되지 않는 차단 규칙 작동 여부 검사
*   [x] **전투 패배, 런 기록 보관 및 메인 복귀**:
    *   [x] 전투 패배 스냅샷 수신 시 패배 안내(`.combat-outcome.lose`) 및 `cc-return-main` 버튼 활성화 검증 (`07_combat_defeat.png`)
    *   [x] 메인 메뉴로 복귀하기 직전(사이드바 활성화 상태), 우측 `#history-panel` 내에 방금 종료된 엔딩("유리성의 사서")이 누락 없이 동적 아카이빙되었는지 검증
    *   [x] `메인 화면으로 ▸` 클릭 시 온보딩 패널로 복귀하고, 1회 제한 룰에 의해 부트 인트로가 재발생하지 않는지 검증 (`08_returned_main.png`)
*   [x] **안전한 프로세스 회수**:
    *   [x] 모든 E2E 검증 절차 종료 후 브라우저를 닫고 백그라운드 FastAPI 서버를 완벽하게 중지(`terminate` -> `kill` fallback)하여 포트 및 메모리 누수 해제
