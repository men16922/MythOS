# Neo-Seoul Live QA Checklist

최종 갱신: 2026-06-18

이 문서는 `neo-seoul`을 사람이 직접 플레이하면서 판단할 수 있는 항목만 다룬다. 자동 테스트, JSON 검증,
빌드/린트, API 동작 확인은 포함하지 않는다. 완료된 체크 항목은 제거하고, 남은 문제와 다음 플레이에서 볼
항목만 유지한다.

기록 방식:

- `[ ]` 미확인 또는 다음 플레이에서 확인 필요
- `[~]` 일부 개선됨, 장기 플레이 체감 재확인 필요
- `[!]` 문제 있음, 수정 또는 설계 판단 필요

## 2026-06-18 자동 검증 완료 — 라이브 QA는 "feel"에만 집중

이날은 코드/DB가 아니라 *체감*만 남기려고, 아래 feel-backing 배선을 **자동 검증(회귀 0, green)** 했다.
라이브 QA에서 "배선이 동작하나"는 다시 볼 필요 없고 **"느낌이 사는가"만** 보면 된다.

- **DB 영속(migration 006/007 적용 + round-trip PASS)**: 호감도(`relationships`)·컷씬 언락
  (`unlocked_cutscenes`) 컬럼이 실 Postgres에 적용되고 persist→load 왕복 보존 확인. → 관계/컷씬이
  루프·세션 간 보존됨(§4 관계 잔향·동료 호감도의 데이터 토대).
- **substrate 9종 green**: 막 목표 스트립(`_chapter_goal`), junction 행선지 의미
  (`_route_destination_meaning`), 전투 배너 ⚑/🎁(`_encounter_meta`), soft defeat, 오프닝
  `image_sequence`/`image_pre`, `core_stake` 노출 — 전부 자동 테스트로 미회귀 확인.
- **신규 회귀 가드 2종**(`tests/test_route_meaning_and_goals.py`): route 노드 `type`→의미 클로저 +
  chapter_gate `player_goal` 완전성. 앞으로 콘텐츠 추가 시 막 목표/행선지 라벨이 조용히 비거나
  generic으로 새면 빨개진다.
- **서버 가동 중**: `make dev-up`(API+SPA+비주얼 워커) + Ollama 실서사 경로. http://127.0.0.1:8000/ 에서
  바로 플레이 가능.

→ **라이브 QA 초점**(순수 사람 판단, 자동검증 불가): §3 선택 결과 체감 · §4 세린/린위에/카이/IX 존재감 ·
§5 전투의 서사적 동기 · §6 페이스/감각 변화 · §7 진행도 보상 체감 · §8 엔딩 잔향. 발견은 각 항목에
`[!]`/메모로 적어 피드백 주면 다음 세션에서 처리한다.

## 0. 2026-06-14 세션 수정 — 다음 플레이(`make dev-up`)에서 확인할 것

이번 세션에서 코드로 처리한 항목들. 실사람 플레이로 체감/회귀를 확인한다(자동 검증은 통과).
세부 근거는 §2/§5/§6 메모 및 `PROGRESS_LOG.md` 2026-06-14 참조.

- `[x]` **왜 위험한가(core_stake)**: 초반(connect/explore)에 "비식별 신호 = 관리자 IX의 최적화/소거 대상"
  위협이 stakes 스트립 최상단에 보이는가. 이후 막에서 사라지는가(후반 nagging 방지).
- `[~]` **목표·방향 명확성**: 목표 스트립의 "이번 막" 목표 + Codex "루트 흐름"의 엔딩 의미 글로스 +
  junction 선택지의 행선지 의미(순찰/전투/시장…)가 길잡이가 되는가.
- `[~]` **장면·위치 다양성**: 같은 구역에서 2턴 이상 머물 때 장면이 실제로 전진(이동/새 인물·위협/국면 전환)
  하는가. 이전의 "같은 골목 3턴 반복"이 사라졌는가.
- `[~]` **전투-서사 연결**: 교전 배너의 ⚑배경/🎯학습/🎁승리 보상이 전투를 이야기와 연결하고 보상 의미를
  주는가. (보상 결과 패널과 reward_intent 연동은 후속.)
- `[~]` **신규 이미지 경로**: `make dev-up`(워커 포함)에서 사전제작 이미지가 없는 장면의 새 이미지가 실제로
  뜨는가. 워커 중단 시 90초 후 원인 안내가 보이는가(영구 "생성 중…" 멈춤/이후 이미지 차단 해소).
- (이전 세션 잔여, 재확인) `[~]` 오프닝 포트레이트(`player-noise` 시작)·세린 조우 명확화, 세린 반말 말투,
  주요 장면 이미지-대본 정합, 전투 패배 후 포획/회복 흐름.

## 1. 최근 스크린샷 메모

- `[!]` `screenshots/스크린샷 2026-06-14 오전 12.35.34.png`: 전투 패배 뒤 후속 장면이 이어졌지만,
  이후 관리망 추적도 99로 강제 정정 조건에 걸려 여정 종료가 표시된 상태다. 패배 자체가 즉시 종료를 만든
  것이 아니라, 회복 후 누적 상태가 종료 조건에 도달한 것으로 설명되어야 한다.

### 1.1 오프닝/초반 장면 정합 (2026-06-14 오전 5.40~5.41 스크린샷)

- (해설, 버그 아님) **"주황색 표시"의 정체** = `.anchor-scene` 골드 프레임(`index.css` 1845-1848,
  `inset 0 0 0 2px var(--gold #ffd479)` + 앰버 글로우). 사전 저작 **앵커(임팩트 비트) 큐레이트 이미지**에만
  붙어 "이건 동적 생성이 아니라 핵심 장면"임을 표시한다. 어두운 사이버펑크 위라 주황으로 보임. 동적 mflux
  이미지엔 안 붙는다. → 의도된 표식이나, "표식의 의미"가 플레이어에게 안 읽히면 제거/약화 검토 여지.
- (해소, 버그 아님) **세린 머리색**: scene 1 앵커에서 빨강으로 보이던 건 네온 조명 탓 — 의도된 검은 머리.
  재생성 불필요.
- `[~]` **세린 과다 노출 / scene 1 리빌 누수 — 수정 적용(2026-06-14)**: 오프닝 시네마틱(도착→손→추격) 직후
  scene 1 앵커 이미지가 또 세린이라 정식 출연(scene 2 패널) 전에 4번 노출됐다. → 앵커에 `image_pre`
  (`scenes/opening_first.png`, 쓰러진 주인공 포커스) 필드 추가. **장면 지문에 세린이 잡히기 전에는
  `opening_first`+주인공 포트레이트, 세린이 등장(지문에 언급=패널에 세린)하면 `opening_escape`로 전환** —
  이미지·포트레이트가 동일 신호(`detectSceneCharacter`)로 자동 동기화. 배선: `scenario.json`(image_pre)·
  `route_map.py`(필드 복사)·`StoryPanel.tsx`/`sceneCharacter.ts`(전환). 다음 플레이에서 scene 1=주인공/scene
  2부터=세린 컷 확인.
- `[~]` **scene 1 대본 ↔ 이미지 불일치 — 수정 적용(2026-06-14)**: 첫 플레이에서 scene 1 이미지는
  `opening_first`(쓰러진 주인공)인데 대본은 "지하 상가 통로에서 빛을 정찰하며 몸을 낮추는" 일반 장면이라 장소·
  순간이 어긋났다. 근본 원인: turn 0 director 지시(`ONBOARDING_ACT1_SHOT1`)와 `_opening_continuity_notes`가
  **세린을 scene 1에 강제**(새 설계와 충돌)했고, 8B가 그걸 무시하면서 이미지와도 어긋난 장면이 나왔다. →
  **turn 0 = '추락한 신호, 홀로 깨어남'**(세린·구조자 미등장, 비 내리는 네온 골목 젖은 바닥에서 각성 =
  `opening_first` 정합)으로 재작성, **세린의 등장/손 내밈/대사는 turn 1(SHOT2)로 이동**(=`opening_escape` 전환
  시점과 일치). `scenario_context.py` 두 함수 수정.
- `[x]` **scene 1 신호 충돌·장소 드리프트 — 해소·라이브 검증(2026-06-14)**: gemma4는 이미지를 보지 않음(텍스트만)
  → 정합은 지시 fidelity 문제. (1) 공유 `STORY_SYSTEM_PROMPT` few-shot이 "세린의 손을 잡고…"/"드론…"을 박아
  8B가 매번 세린·드론을 끌어옴 → first-scene **INSTRUCTION**(user 메시지 맨 끝, 비절단)에 "오프닝 확립 장면 —
  다른 인물/구조자/적/추격/전투 금지, 시스템 few-shot 무시, DIRECTIVE NOTES 따르라" 추가(`prompts.py`
  `OPENING_FIRST_SCENE_INSTRUCTION`, `build_first_story_messages`/`build_first_scene_messages`). (2) 세린 objective
  prime는 turn 1부터. (3) **`starting_location: data-layer-01`** 을 본 8B가 scene 1을 지하 주차장/회랑으로 옮기던
  드리프트 → turn 0 INSTRUCTION+노트에 "location_id 무시, 야외 빗속 네온 거리, 쓰러져 각성" 강제. 실제 Ollama
  2회 재생성 검증: 둘 다 빗속 네온 골목 홀로 각성(지하/주차장/세린 0). Option B(AI 유지+신호 정리) 채택.
- `[x]` **오프닝 4비트화 — 인트로 3컷을 인게임 페이오프로(2026-06-14)**: "예고→경험" 구조. 인트로 시네마틱
  3컷(이미 티저로 노출)을 인게임 scene 2~4의 저작 source로 직접 재사용 + 앞에 scene 1(쓰러진 각성) 추가.
  매핑: scene 1=홀로 각성(`opening_first`) / scene 2=SHOT01 도착 / scene 3=SHOT02 첫 접촉(`met_se_rin`) /
  scene 4=SHOT03 추격+첫 전투(`patrol_ambush`). `scenario_context.py`가 `ui_copy.session_intro.cinematic_shots`
  title/body를 턴별 지시로 사용(게이팅 turn≤3). 라이브: turn1 제목 "빗속에서 세린이 당신을 발견한다"·세린 도착
  확인. 잔여(경미): turn1서 "회랑" 단어 1회 누수·제목 "Changed " 접두 아티팩트, 풀 4턴 사람 플레이 체감.
- `[x]` **장면별 이미지 시퀀스 + 인트로 화면 리뉴얼(2026-06-14)**: (A) 오프닝 앵커에 `image_sequence`(턴별 컷)
  추가 — scene1 `opening_first`→scene2 `serin-arrival`→scene3 `first-contact`→scene4 `drone-chase`. 프론트가
  `active_scene.turn_index`로 인덱싱(`scenario.json`/`route_map.py`/`types.ts`/`StoryPanel.tsx`). 인트로 3컷 티저와
  인게임 이미지가 비트별로 1:1 = "예고→경험" 시각 연결. (B) 인트로 시작 화면 간결화: SYS-01~04 용어 타일 제거,
  copy를 평이한 오리엔테이션(지금/곧/목표 3줄)으로 재작성, 별도 작전목표 패널 제거, 버튼 "세린의 손을 잡는다"→
  "깨어난다"(scene1=홀로 각성과 정합). `scenario.json session_intro` + `IntroPanel.tsx`. 풀 4턴 사람 플레이 체감 남음.
- `[!]` **오프닝 시네마틱 ↔ 인게임 시점 불일치(남음, 더 큰 건)**: 프리게임 montage는 도착→구조(손)→옥상 드론
  추격(이미 도주 중)까지 보여준 뒤, 인게임은 "홀로 각성"에서 시작 → montage가 앞질러 스포일. 개선안(미적용):
  montage를 **collapse 직전까지의 cold-open**으로 재배치(추격 컷은 후반 비트로 이동)해 montage→scene1(각성)→
  scene2(세린)→scene3(추격) 시간순 연속화. scene-1 대본·이미지는 위에서 정렬됐고, montage 재구성은 후속.

## 2. 첫 5분 Hook

- `[x]` 플레이어가 자신이 “비식별 신호”이고 관리자 IX에게 지워질 위험이 있다는 점을 이해한다.
  메모: "왜 위험한지 모르겠다" 피드백 → `playability.core_stake`(비식별 신호=IX 최적화/소거 대상)를 초반
  phase(connect/explore) stakes 스트립 최상단에 상시 노출, 이후 막에선 드롭(2026-06-14). 문구는 brief
  PREMISE 도출 — codex 폴리시 여지. 실플레이 체감 재확인 필요.
- `[~]` 첫 선택이 단순 진행 버튼이 아니라 사람/증거/안전/통제 중 무엇을 택하는지 느껴진다.
  메모: 선택지가 직관적이지 않고 “무슨 의미인지” 모호했다. -> 조금 나아진듯함
- `[x]` 첫 장면 이후 다음 목표가 명확하다. **결말 경향 명료화(2026-06-14)**: "루트 흐름"의 엔딩 라벨
  (안정적 귀환/시스템 각성/고결한 희생/강제 최적화)이 2단어 추상명뿐이라 "이게 대체 무슨소린데?"였음.
  각 엔딩에 시나리오 narration에서 압축한 **의미 한 줄 글로스** 추가(`RouteNarrative.tsx`, 내부 `condition`은
  비노출). **막 목표 정합(B-part2-i 완료)**: `session_design.chapter_gates`에 phase별 플레이어용 `player_goal`
  추가→serializer `_chapter_goal`(loop.phase 매칭, 내부 flag gate는 비노출)→StoryPanel 목표 스트립에
  "이번 막" 안정 목표를 "현재 목표"(장면별 LLM objective) 위에 상시 표시. **행선지 의미 노출(B-part2-ii
  완료)**: route junction 선택지 라벨을 `{행선지}(으)로 향한다 — {type별 의미}`로 강화(순찰=조용히 이동/조우
  적음, 전투=교전, 시장=보급·거래, 정비=회복 등, `_route_destination_meaning` in `session.py`).

## 3. 선택의 맛

- `[x]` 매 장면 선택지가 서로 다른 가치 판단처럼 보인다: 사람, 증거, 안전, 통제.
  메모: `Perception` 체크 같은 내부식 표현은 플레이어에게 설명 없이 노출되지 않아야 한다.
- `[~]` 선택 후 NPC 태도, 안정도, 추적도, 단서, Codex/Shard 중 하나 이상의 변화가 체감된다.
  메모: 수치 변화 표시는 보이지만, 장기 플레이에서 이야기 변화로 느껴지는지 확인 필요.
- `[~]` 선택 결과가 너무 늦게 오거나 너무 추상적으로 느껴지지 않는다.
  메모: 첫 토큰 지연은 프롬프트 prefill 병목으로 추정된다. 현재 8B 모델 기준 실제 체감 재측정 필요.
- `[!]` 실패/우회 선택도 손해만 주지 않고 다른 정보나 톤을 준다.

## 4. 캐릭터 존재감

- `[!]` 세린은 단순 안내자가 아니라 자기 판단과 감정으로 플레이어를 밀거나 붙잡는다.
  메모: 캐릭터성은 있지만 비슷한 말 반복과 목적 불명 피드백이 있었다. -> 세린의 캐릭터가 어느 순간 사라짐
- `[ ]` 린위에는 거래의 이득과 부채를 동시에 느끼게 한다.
- `[ ]` 카이 RX-09는 보상 장비가 아니라 “기계도 기억할 수 있는가”라는 테마를 연다.
- `[ ]` 관리자 IX는 추상 시스템이 아니라 플레이어 선택을 정정하려는 압력으로 느껴진다.

## 5. 전투 만족감

- `[~]` 전투가 갑자기 끼어든 미니게임이 아니라 추적, 작전 실패, 구출, 동료 보호, 보상 중 하나의 결과로 느껴진다.
  메모: 전투 배너에 `narrative_trigger`(⚑ 배경 — 왜 이 교전인가)를 노출(2026-06-14, `_encounter_meta`+StoryPanel). 실플레이 체감 재확인.
- `[ ]` 중반 이후 전투는 target priority, 방어 timing, 이동/거리, 스킬 선택 중 하나를 요구한다.
- `[~]` 승리 후 무엇을 얻었는지 보인다: 통찰, 단서, 아이템, 관계 변화, 다음 루프 정보.
  메모: 통찰/아이템 외 보상 의미가 약하다는 피드백 → 전투 배너에 `reward_intent`(🎁 승리 보상의 의미) 노출(2026-06-14). 실제 보상 결과 패널 연동 체감 재확인.
- `[~]` 패배/도주/손실도 이야기 흐름을 끊지 않고 후속 결과로 이어진다.
  메모: soft defeat가 적용되어 포획/회복 후 계속 진행 가능해야 한다.

## 6. 페이스와 작전 지도

- `[~]` 작전 지도에서 현재 위치, 다음 이동지, 접근 중인 접촉, 이동 위험이 이해된다.
  메모: 선택지가 지도상 순찰/전투/단서와 어떻게 이어지는지 이해하기 어렵다는 피드백이 있었다. -> 개선됨: junction 선택지에 행선지명 + type별 의미(순찰/전투/시장/정비/단서…) 표기(2026-06-14).
- `[!]` 초반 10장면 안에 전투가 강제로 반복되어 서사 진행을 막지 않는다.
- `[~]` 10-15분마다 플레이 감각이 바뀐다: 추격, 은신처, 조사, 거래, 작전, 전투, 동료화, 최종 대면.
  메모: route 노드 anti-stickiness로 같은 노드 안에서도 장면이 전진(골목→배관→서비스 통로→정비)하도록 수정(2026-06-14). 실플레이 체감 재확인 필요.
- `[x]` 같은 톤의 텍스트가 길게 이어져 피로해지지 않는다. 매 장면과 선택마다 이동/상황 변화가 있어야 한다.
  메모: 멀티턴 in-process 검증서 위치 고착(이전 3턴 동일 골목) 해소·반복 문장 0. 추상어 반복도 0(레지스터 reframe).
- `[~]` 전투와 서사 템포가 서로 방해하지 않는다. -> 전투 배너 배경/보상 노출로 연결성 보강(2026-06-14). 실플레이 체감 재확인.

## 7. 진행도와 재플레이 동기

- `[ ]` Stability/Tension/Temporal Decay/Zone Risk/Clue Matrix가 무엇을 바꾸는지 플레이 중 이해된다.
- `[ ]` 통찰/스킬/해금 보상이 다음 선택이나 전투를 바꿀 것처럼 느껴진다.
- `[!]` SKILL TREE 상태가 이해된다: 해금됨, 습득 가능, 통찰 부족, 선행 필요. -> 이해는 잘되는데, RPG 스킬트리처럼 보여졌으면해. ex) Path Of Exile / Diablo
- `[~]` 기록 보관소는 “지난 루프”로 이해되고, 빈 상태일 때도 왜 비어 있는지 알 수 있다.
- `[~]` Echo/Shard가 단순 로그가 아니라 세계가 플레이어 선택을 기억한다는 감각을 준다.

## 8. 엔딩 잔향

- `[ ]` 엔딩에서 무엇을 구했고 무엇을 잃었는지 선명하다.
- `[ ]` 세린, 린위에, 카이 중 누구와 어떤 관계가 남았는지 기억난다.
- `[ ]` 관리자 IX와의 대면이 플레이어 선택의 총합처럼 느껴진다.
- `[ ]` 다음 루프에 남는 Echo/Shard/해금이 구체적이다.
- `[ ]` 엔딩 후 “다른 선택을 해보고 싶다”는 동기가 생긴다.

## 9. 최종 판정

- `[ ]` 첫 플레이어가 30-60분 동안 목표를 잃지 않고 따라갈 수 있다.
- `[ ]` 플레이 후 기억나는 장면이 최소 3개 이상 있다.
- `[ ]` 불편했던 지점이 “버그/혼란”보다 “더 보고 싶다/더 다듬으면 좋겠다”에 가깝다.
- `[ ]` 현재 상태로 다른 사람에게 플레이를 맡겨도 기본 재미를 설명하지 않고 전달할 수 있다.
